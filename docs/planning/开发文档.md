# 开发文档

## 0. 开发原则

本文档基于 [`需求文档.md`](../requirements/需求文档.md) 制定，用于指导后续图鉴审核、积分、重审、报错和权限体系开发。

规则来源说明：

- [`DECISIONS.md`](../ai/DECISIONS.md) 是当前唯一真实规则源。
- 本文档只记录开发阶段、落地顺序和暂停点；如与 `DECISIONS.md` 冲突，以 `DECISIONS.md` 为准。
- 本文档属于执行计划层，承接已确认需求，用于拆分阶段、暂停点、验收标准和开发顺序。
- 本文档不承载 Final 决策；未写入 `DECISIONS.md` 的待确认事项不得直接进入实现。
- 从需求进入开发前，必须先确认需求来源、已确认规则、未确认事项、技术影响和风险分类。

开发原则：

1. 功能以当前 [`需求文档.md`](../requirements/需求文档.md) 为准。
2. 遇到需求风险、规则冲突、字段缺失或会改变用户行为的地方，先暂停并向产品确认，不直接开发。
3. 优先实现数据闭环，再实现界面和视觉权益。
4. 所有积分、贡献、投票、审核结果必须可追溯。
5. 线上数据变更前必须先确认迁移影响，涉及批量更新时单独确认。

## 0.1 当前实际阶段

本节用于标注截至 2026-07-14 的实际开发状态，作为阶段规划前的当前基线说明；下方阶段一至阶段七仍保留为后续规划，不代表当前已经全部完成。

### 当前已完成

- AI 协作工作区：已建立 `docs/ai/`，用于记录当前任务、任务队列、决策、报告和测试清单。
- 数据库运维脚本隔离：已将 production 写库脚本从可提交路径移出，保留只读盘点脚本，降低误提交和误执行风险。
- 衣柜刷新数量稳定：已修复图鉴分页加载、云端衣柜合并保存、大幅缩水拦截等问题，避免几千件衣柜刷新后异常缩水。
- 分类 + 短编号录入衣柜：已支持用户按分类选择并输入短编号批量录入衣柜。
- 正式库已有但资料不全补全：已支持管理员处理正式库已有但字段不全的 pending 项，避免唯一约束重复失败。
- 提交者衣柜写回：缺失图鉴入库或补全后，相关有效提交者衣柜可自动获得入库部件。
- 管理员审核卡片文案优化：已按产品口径替换技术感文案，不再使用“版本、变体、字段组合、冲突版本、一致提交”。
- 人工回归 8 项全部通过：衣柜保存、分类短编号录入、审核卡片文案、正式库补全、pending 状态、提交者衣柜写回、套装选择与刷新、生产环境 Vue DevTools 均已通过 Preview / production 人工回归。
- 缺陷文档已收口：已将回归通过相关缺陷状态收口为“已关闭”，并保留历史记录和验证记录。
- 需求文档已收口：已补充“当前实现状态”，标注已实现、未实现 / 暂缓和待决策事项。
- 缺失图鉴草稿恢复：已完成用户隔离、24 小时有效期、离线失败保留、成功清理和 development 人工验收，并通过 PR #74 合入 `develop`。

### 当前阶段重点

- 积分 / 贡献者第一阶段：只读审计当前 migration、生成类型和入库 / 补全 RPC，先形成拆分、验证和 rollback，再申请数据库授权。
- 回归保护：优先保障已验证的提交、审核入库和提交者衣柜写回链路稳定，不在首个数据库 PR 中接入写路径。
- 陪审团、重审、报错、排行榜和权益继续后置，不与积分 / 贡献者基础阶段混合。

### 下一步候选

1. 确认本文件第 0.3.3 节列出的积分 / 贡献者第一阶段边界和待确认项。
2. 另行授权 development 只读前检：project ref、当前 schema / types、重复业务键、RLS / grants、函数权限和 Security Advisor。
3. 前检通过后，才创建只含基础表、约束和默认拒绝策略的首个 database PR。

## 0.2 Rule Lock Phase 1：规则冻结摘要

本节只摘要规则冻结结果，完整可执行定义以 [`DECISIONS.md`](../ai/DECISIONS.md) 为准。

### Final

- 积分权威来源为 `points_ledger`。
- 图鉴 / 衣柜条目审核通过并成功入库后，符合最终入库数据的有效提交者获得积分；自动入库 / 管理员仲裁 +10、正式库空字段补全 +5、重审修正 +8、报错通过 +5。
- 第一版积分状态默认 `awarded`，审核通过后立即生效。
- 积分只追加流水；扣回追加负数流水，不修改或删除历史记录。
- 前 5 有效提交者按提交时间排序取前 5。
- 前 3 展示贡献者从前 5 有效提交者中按提交时间排序取前 3。
- 匿名历史数据不补造真实用户身份；缺编号数据不进入积分判断分支。
- 缺套装但编号完整、核心字段完整的数据允许正式入库，同时创建 `re_review_items`，状态为待补套装。
- `re_review_items` 是待补套装状态的权威追踪来源。

### User Decision Required

- 重审项是否允许普通用户提交候选修正版。
- 同一用户对同一服装在不同来源事件中是否可以再次计分；该口径决定跨来源幂等约束。
- 自动入库当前会删除来源 pending：后续改为保留 approved 记录，还是保留不可变来源快照；实现前必须确认一种权威追溯方式。
- 用户账号删除后，贡献署名和积分流水如何匿名化保留；该口径决定用户外键删除策略。

### Rejected Ideas

- 废弃积分审核通过后默认 `pending`。
- 废弃 `profiles` 作为权威积分总分来源。
- 废弃前 3 展示贡献者按积分、信用等级或最终一致度排序。
- 废弃缺套装但编号完整的数据必须留在 pending。

## 0.3 阶段包：积分、贡献者、重审池、报错修正最小开发计划

本阶段只做规划，不实现功能、不修改业务代码、不修改数据库。下一阶段开发应先在 development 环境完成 migration、RPC、RLS 和前端入口验证，生产执行前必须按数据库安全方案完成备份和确认。

### 0.3.1 积分流水 `points_ledger`

第一版目标：

- 新增权威积分流水，记录积分发放、扣回和来源。
- 支持从流水汇总用户总分、月积分和等级。
- 入库、补全、重审修正、报错修正产生积分时，只追加流水，不直接覆盖用户资料总分。

不做什么：

- 不做排行榜页面。
- 不做 Lv4 月榜首体验权益。
- 不做积分缓存表。
- 不删除 `profiles.total_points` 等历史字段，但明确它们不作为权威来源。

依赖哪些现有表 / RPC / 页面：

- 依赖 `profiles` 识别用户和角色。
- 依赖 `pending_clothes`、`clothes`、`submit_clothing_contribution` 和 `approve_pending_clothes_arbitration` 识别积分来源。
- 依赖后续 `clothing_contributions` 关联贡献者和积分来源。
- 前端第一版可只在管理端或个人中心展示只读结果，不影响现有录入流程。

已确认第一版口径：

- 积分状态默认 `awarded`。图鉴审核通过后，积分立即生效。
- 自动入库 / 管理员仲裁 +10，正式库空字段补全 +5；重审修正 +8、报错通过 +5 在后续阶段接入。
- 扣回只追加负数流水，不删除或修改原流水。

开发风险：

- 如果积分和贡献者不共用同一批“有效提交者”判定，会导致署名和得分不一致。
- 如果 `profiles.total_points` 被继续当作权威来源，会和流水汇总冲突。
- 如果积分默认自动发放，错误入库后的扣回流程必须同时可用。

建议开发顺序：

1. 先与 `clothing_contributions` 一起建立基础表、来源约束和追加式保护，不建立 public view，不接入现有 RPC。
2. 再单独建立最小只读查询面，并验证 Data API 暴露、table grants、RLS 和角色矩阵。
3. 最后按“正式库补全 → 管理员仲裁 → 自动入库”逐条接入写路径，每条路径独立验证和 rollback。

### 0.3.2 贡献者记录 / 前 5 有效提交者 / 前 3 展示

第一版目标：

- 新增贡献者记录，保存 `clothes_id`、`user_id`、来源 pending、贡献类型和积分状态。
- 入库或补全时记录前 5 个有效提交者。
- 预留前端展示前 3 位贡献者的数据来源。

不做什么：

- 不做复杂贡献者徽章。
- 不做贡献者主页。
- 不做动态彩名、首页鸣谢、广告免除。
- 不把匿名历史数据强行补成真实贡献者。

依赖哪些现有表 / RPC / 页面：

- 依赖 `pending_clothes.submitted_by` 识别提交者。
- 依赖 `clothes` 作为正式入库目标。
- 依赖 `submit_clothing_contribution`、`approve_pending_clothes_arbitration`、`complete_existing_clothes_from_pending` 的入库 / 补全结果。
- 依赖 `user_wardrobes` 写回逻辑保持“有效提交者”和“衣柜写回用户”一致。

来源追溯要求：

- 管理员仲裁和正式库补全当前会把 pending 标记为 `approved`，可保留 `source_pending_id`。
- 自动入库当前会删除匹配 pending，不能直接依赖可空外键；在确认保留 pending 或不可变来源快照前，不接入贡献 / 积分写入。
- 排名必须先按提交时间稳定排序，再按用户去重并取前 5；不得使用无序 `array_agg(distinct submitted_by)` 直接生成贡献排名。

需用户确认：

- 匿名历史数据是否展示为“匿名历史数据”，以及是否参与前 5 / 前 3。

已确认第一版口径：

- 前 5 有效提交者：与最终入库数据一致的提交者，按提交时间排序，取前 5 个获得积分。
- 前 3 展示贡献者：从前 5 有效提交者中，按提交时间排序，取前 3 个用于页面展示。

开发风险：

- 当前生产存在 `submitted_by is null` 的历史 pending，不能强行计入真实贡献者。
- 如果管理员仲裁选择的 pending 集合含有少数错误提交者，贡献者记录可能被污染。
- 贡献者写入和衣柜写回如果分散在前端，会出现漏写或重复写。

建议开发顺序：

1. 先建立 `clothing_contributions` 去重约束和只读查询。
2. 再把自动入库、管理员仲裁、正式库补全统一写入贡献者记录。
3. 最后接入前 3 展示位置。

### 0.3.3 2026-07-14 第一阶段执行说明与验证交接

#### 0.3.3.1 本阶段目标与范围

- 业务目标：在不破坏现有提交、审核入库和衣柜写回链路的前提下，让有效贡献者和积分奖励可追溯、可重算。
- 当前交付：只读审计结论、database PR 拆分顺序、development 验证清单和未来业务验收步骤。
- 当前不做：不创建 migration、不连接或查询远程数据库、不写 Supabase、不改业务代码、不做历史回填、不进入 production。
- 目标 development project ref：`tfwejruvdahonacyldrg`；仅在后续单独授权后使用。

#### 0.3.3.2 当前事实与缺口

| 检查项 | 当前事实 | 结论 |
| --- | --- | --- |
| 基础表 | migrations 和生成类型中均无 `clothing_contributions` / `points_ledger` | 尚未实现，不从旧 PR #17 原样恢复 |
| 自动入库 | `submit_clothing_contribution` 收集匹配提交者、写回衣柜后删除匹配 pending | 来源会消失；追溯策略未确认前不得接入积分 |
| 管理员仲裁 | `approve_pending_clothes_arbitration` 读取 `p_pending_ids` 的提交者并标记 `approved` | pending 可保留，但 RPC 需在服务端验证候选与最终入库数据一致 |
| 正式库补全 | `complete_existing_clothes_from_pending` 校验名称、分类、短编号和 pending 数量后标记 `approved` | 可作为第一条写路径候选，但仍需稳定排序、幂等和贡献 / ledger 同事务验证 |
| 贡献排序 | 现有 RPC 使用 `array_agg(distinct submitted_by)`，未形成可证明的“按提交时间去重后前 5”顺序 | 必须改为按最早提交时间稳定排序 |
| 生成产物 | `src/types/supabase.ts` 和 `supabase/schema.sql` 未包含现有 `complete_existing_clothes_from_pending` | 本地 schema / types 已落后，任何 database PR 前必须从 development 重新生成并核对 |
| RLS / grants | `schema.sql` 仍显示 `pending_clothes` grants 较宽，旧 PR #17 又计划 revoke 底层表后开放 `security_invoker` view | 必须先做角色矩阵和 Data API 暴露验证，不能只看 RLS policy |
| 编号口径 | 当前 RPC 与 Final 决策均禁止空值、`N` 和非数字短编号 | `game_id = N` 不再是计分唯一键方案，缺编号直接不进入积分分支 |

#### 0.3.3.3 database PR 拆分顺序

1. **development 只读前检（无 PR / 无写入）**：确认 project ref、migration list、schema / types 漂移、`clothes(category, game_id)` 重复键、现有 RLS / grants / function execute 和 Security Advisor。
2. **DB-1 基础事实表**：只新增 `clothing_contributions`、`points_ledger`、外键 / 唯一约束 / 必要索引和 RLS 默认拒绝；不建 public view，不开放客户端写入，不改现有 RPC，不回填历史。
3. **DB-2 最小只读面**：单独增加“用户读自己的积分”和“贡献者公开展示候选”的查询接口；分别验证 Data API 暴露、table grants、RLS、`security_invoker` 和 anon / authenticated / admin / super admin。
4. **DB-3 正式库补全接入**：只改 `complete_existing_clothes_from_pending`，同事务写贡献、积分和原有衣柜写回；验证 +5、前 5、重试幂等和旧功能不退化。
5. **DB-4 管理员仲裁接入**：只改 `approve_pending_clothes_arbitration`，增加服务端候选一致性校验后写贡献与 +10 积分。
6. **DB-5 自动入库接入**：先确认来源保留策略，再改 `submit_clothing_contribution`；自动入库、来源保存、衣柜写回、贡献和 +10 积分必须同事务完成。
7. **business / frontend PR**：数据库链路稳定后再展示积分和前 3 贡献者；不与 database PR 混合。
8. **历史回填**：单独设计、单独授权；匿名或来源不完整记录不得补造用户身份。

每一项均以前一项在 development 验证通过为前置；任一项失败即停止，不继续下一个 PR。

#### 0.3.3.4 development 验证门禁

后续每个 database PR 必须提供并执行：

1. **环境与前检**：显示目标为 development `tfwejruvdahonacyldrg`；记录 migration 前表 / 函数 / policy / grant 状态、预计影响行数和旧函数定义备份。
2. **结构与安全**：验证新表启用 RLS、客户端无直接写权限、外键和唯一约束生效；运行 Security / Performance Advisor，记录新增与遗留告警。
3. **角色矩阵**：分别验证 anon、authenticated 普通用户、admin、super admin 的 SELECT / INSERT / UPDATE / DELETE / RPC；禁止普通用户伪造贡献或积分。
4. **幂等与事务**：同一请求重复执行不得重复贡献、重复积分或重复衣柜写回；任一子写入失败必须整体回滚。
5. **业务一致性**：贡献用户、积分用户和衣柜写回用户必须来自同一批有效提交者；按最早提交时间去重后最多 5 人，展示最多 3 人。
6. **生成与构建**：重新生成 `supabase/schema.sql`、`src/types/supabase.ts`、`docs/database/schema.md`，运行前端 build，并确认 diff 不包含 env、token、真实用户数据或临时报告。
7. **rollback 演练**：新表 / 字段优先停用而非删除；RPC 恢复旧函数定义；错误积分追加反向流水，不修改或删除历史。

#### 0.3.3.5 用户业务验收说明（未来交付模板）

本节是未来功能完成后的验收模板，当前没有可执行 Preview，不要求用户现在操作。进入人工验收前，Codex 必须先补齐具体 Preview、测试账号 / 角色、测试数据 ID 和清理方式，并一次性交付以下步骤：

| 场景 | 用户操作 | 预期结果 |
| --- | --- | --- |
| 自动入库 | 使用 5 个测试用户按时间顺序提交完全一致的数据 | 第 5 次后入库；前 5 有效用户各有 1 条 +10；衣柜均写回；来源可追溯 |
| 管理员仲裁 | 管理员选择一致的 pending 进行仲裁 | 只奖励与最终数据一致且按时间排序的前 5；每人 +10；错误候选不计入 |
| 正式库补全 | 管理员用 pending 补全正式库空字段 | 不覆盖已有非空字段；有效用户每人 +5；pending 保留为 approved |
| 重试幂等 | 在网络重试或重复点击条件下重复同一操作 | 不新增重复贡献、积分或衣柜项；页面给出明确结果 |
| 权限 | 普通用户尝试直接写贡献 / 积分或读取他人私有流水 | 写入被拒绝；只能读取允许范围；管理员能力与角色一致 |
| 回滚 | 在 development 触发预设失败点 | 服装、pending、衣柜、贡献和积分保持同一事务结果，无半完成状态 |

用户反馈格式：

```text
环境 / Preview：
测试账号角色：
场景：
步骤编号：
实际结果：
预期结果：
页面提示或截图：
是否可稳定复现：
测试数据 ID：
是否允许保留测试记录：
```

在用户反馈前，任务状态保持“待用户业务验收”；收到反馈后再更新缺陷 / 看板 / PR 结论。测试数据默认只保留在 development，删除或保留必须按本次授权执行。

### 0.3.4 重审池 `re_review_items`

第一版目标：

- 承接缺套装、字段冲突、字段缺失、被报错后需要修正的数据。
- 记录来源、原因、状态、原始 payload、处理人和处理结果。
- 让“暂不入库 / 需要补全 / 需要管理员确认”的数据不再混在普通 pending 中。

不做什么：

- 不做完整陪审团投票。
- 不做普通用户直接覆盖正式库。
- 不做复杂的多候选版本编辑器。
- 不强行迁移所有历史 pending。

依赖哪些现有表 / RPC / 页面：

- 依赖 `pending_clothes` 当前状态流转。
- 依赖 `clothes` 关联正式库已有条目。
- 依赖 `ClothesEntryForm.vue` 作为后续重审修正表单，但本阶段不开发 UI。
- 依赖管理员审核入口后续展示重审池列表。

需用户确认：

- 重审项是否允许普通用户提交候选修正版，还是只允许管理员处理。

已确认第一版口径：

- 缺套装但编号完整的数据允许正式入库，同时创建 `re_review_items`，状态为待补套装。

开发风险：

- 待补套装状态必须在绑定正式套装后自动关闭，不能依赖管理员手动收口。
- 如果重审项直接更新正式库，会破坏审计链路。
- 如果没有清晰关闭状态，重审池会变成新的积压池。

建议开发顺序：

1. 先定义 `re_review_items` 原因和状态。
2. 再接入字段冲突、缺套装、报错转重审三类来源。
3. 最后规划管理端重审池 UI。

### 0.3.5 报错修正 `correction_requests`

第一版目标：

- 允许用户对正式库服装提交错误报告和建议修正。
- 记录报错原因、建议 patch、提交者、处理状态和审核人。
- 报错通过后进入管理员修正或重审流程，而不是直接改正式库。

不做什么：

- 不做留言板优化建议。
- 不做点赞排序。
- 不做自动覆盖正式库。
- 不做复杂处罚或即时扣分。

依赖哪些现有表 / RPC / 页面：

- 依赖 `clothes` 作为被报错对象。
- 依赖 `profiles` 和角色判断处理管理员权限。
- 依赖后续 `re_review_items` 承接非空字段修改和争议修正。
- 前端后续需要报错入口和管理员处理入口，但本阶段只规划。

已确认边界：

- 正式库空字段补全走管理员审核；非空字段修改或争议修正进入审核库和陪审团路径。
- 报错通过奖励 +5；如需扣回历史积分，只能追加负数流水。
- 第一版不使用“低风险 / 高风险字段分级表”作为执行规则。

开发风险：

- 如果报错修正可以直接覆盖正式库，容易绕过审核。
- 如果所有报错都进入管理员终审，可能形成积压。
- 如果积分扣回规则未确认，报错修正会影响贡献者权益争议。

建议开发顺序：

1. 先建立 `correction_requests` 记录表和提交 RPC。
2. 再接入管理员处理状态。
3. 最后把非空字段修改和争议修正转入 `re_review_items`。

### 0.3.6 阶段建议开发顺序

1. `clothing_contributions` 与 `points_ledger`：先保证贡献和积分来源可审计。
2. 入库 / 补全 RPC 接入贡献者记录和积分流水：保证当前已验证主流程不退化。
3. `re_review_items`：承接不能直接入库或需要补全的数据。
4. `correction_requests`：建立正式库报错入口，并将非空字段修改和争议修正导入重审池。
5. 陪审团投票、排行榜、普通管理员轮换后置，等待本阶段闭环稳定后再拆下一阶段。

## 1. 当前基线

### 1.1 已有核心表

| 表名 | 当前用途 | 开发影响 |
| --- | --- | --- |
| `clothes` | 正式服装库 | 缺少 `needs_suit_review` 或等价状态字段 |
| `suits` | 正式套装库 | 需要套装名称唯一性保护 |
| `pending_clothes` | 待审核服装提交 | 已承接缺失项补录和自动入库 RPC |
| `pending_suits` | 待审核套装提交 | 已承接套装申请 |
| `profiles` | 用户资料和角色 | 已有 `role`；不保存权威积分总分，等级从积分汇总计算 |
| `user_wardrobes` | 用户衣柜 | 暂不进入审核主线 |
| `user_quotas` | 用户额度 | 暂不进入审核主线 |
| `stages` | 关卡权重 | 暂不进入审核主线 |

### 1.2 已有核心前端模块

| 模块 | 当前职责 | 后续改造方向 |
| --- | --- | --- |
| `src/views/AdminView.vue` | 管理员审核入口 | 增加重审、积分、终审状态展示 |
| `src/components/MissingItemsQueue.vue` | 图鉴缺失项补录 | 保持与仲裁入库表单复用 |
| `src/components/ClothesEntryForm.vue` | 服装录入通用表单 | 继续作为补录、仲裁、重审共用表单 |
| `src/composables/useAudit.js` | 审核状态与提交逻辑 | 拆分复杂流程，接入新 RPC |
| `src/api/adminService.js` | 管理员数据操作 | 接入重审、投票、积分相关接口 |

## 2. 阶段一：数据库基础层

### 2.1 目标

建立后续审核闭环所需的数据基础，保证贡献、积分、投票、重审都能被记录和审计。

### 2.2 计划新增或调整

建议新增：

| 表 / 字段 | 用途 |
| --- | --- |
| `re_review_items` | 标记并追踪“缺套装但编号完整”的正式服装，以及其他重审项 |
| `clothing_contributions` | 记录贡献者、来源 pending、奖励状态 |
| `points_ledger` | 权威积分流水，支持发放、待定、扣回和月榜冻结 |
| `jury_votes` | 陪审团投票记录，支持权重、去重、防自审 |
| `correction_requests` | 用户报错和正式库修正流程 |
| `admin_terms` | 普通管理员月度任期记录 |

### 2.3 建议约束

- `suits.name` 建议唯一或做标准化唯一约束。
- `jury_votes` 对同一用户、同一审核对象只能投一次。
- `clothing_contributions` 对同一 `clothes_id + user_id + contribution_type` 做去重。
- `points_ledger` 是积分唯一权威来源，不直接覆盖积分，只追加流水。
- `profiles` 不维护权威总分；总分通过 view / RPC 汇总，后期可加可重建缓存。
- `admin_terms` 需要保存任期月份、来源、开始时间、结束时间和状态。

### 2.4 暂停点

开始开发前需用户确认：

1. 套装名称唯一性是否按原始名称，还是按 trim / 统一大小写 / 简繁处理后的标准名。
2. 是否允许先建表但不接 UI，用于逐步迁移。

已确认：

- 缺套装待补状态使用 `re_review_items` 追踪。
- 积分使用方案 C：`points_ledger` 为权威来源，view / RPC 汇总，缓存只作展示加速。

### 2.5 验收标准

- Supabase migration 可重复追踪。
- [`schema.md`](../database/schema.md) 和 `supabase/schema.sql` 可更新。
- 新表具备 RLS 或访问控制策略。
- 不影响现有图鉴查询、缺失项补录、管理员入库功能。

## 3. 阶段二：提交与自动入库闭环

### 3.1 目标

完善现有提交 RPC，使“5 次一致自动入库”“前 5 个有效提交者奖励”“缺套装标记”形成闭环。

### 3.2 开发内容

- 调整 `submit_clothing_contribution` RPC。
- 入库时写入 `clothing_contributions`。
- 根据最终入库数据匹配有效提交者。
- 对前 5 个有效提交者生成已发放积分流水，第一版默认 `awarded`。
- 对缺套装但编号完整的数据写入正式库，并创建 `re_review_items` 追踪套装待补状态。
- 对字段不一致的 5 次提交转入人工仲裁或重审。

### 3.3 暂停点

遇到以下情况暂停并请求用户决策：

1. 自动入库来源 pending 是保留为 `approved`，还是写入不可变来源快照。
2. 同一用户对同一服装在不同来源事件中是否允许再次计分。
3. 用户账号删除后，贡献署名和积分流水如何匿名化保留。

### 3.4 验收标准

- 同一用户重复提交不会污染 5 次一致判断。
- 5 次完全一致可自动入库。
- 入库后能追踪前 5 个有效提交者。
- 积分流水能找到来源 pending 记录。
- 缺套装数据不会丢失待补状态。

## 4. 阶段三：陪审团投票机制

### 4.1 目标

实现用户赞同 / 驳回机制，并按信用等级计算净胜票。

### 4.2 开发内容

- 新增投票 API 或 RPC。
- 根据用户积分等级计算投票权重。
- 防止用户审核自己的提交。
- 防止同一用户重复投票。
- 根据净胜票触发状态流转：
  - 字段完整且候选唯一，净胜达到 5 可入库。
  - 字段缺失或候选冲突，净胜达到 5 进入管理员终审或重审完成流程。
  - 净胜达到 -3 进入重审。

### 4.3 暂停点

遇到以下情况暂停并请求用户决策：

1. Lv4 的“可信权重 +3”是否只用于陪审团投票，不参与自动 5 次入库。
2. 管理员投票是否算普通投票，还是作为终审动作单独记录。
3. 候选数据冲突时，用户是否只能赞同 / 驳回，还是可以选择某个候选版本。

已确认：

- 陪审团阈值使用“达到阈值”触发，不要求恰好等于阈值。

### 4.4 验收标准

- 用户不能投自己的提交。
- 投票权重按等级生效。
- 投票记录可审计。
- 达到阈值后状态自动流转。
- 字段缺失数据不会被普通投票直接推入正式库。

## 5. 阶段四：重审与报错机制

### 5.1 目标

将缺编号、字段冲突、正式库报错从普通 pending 中拆出，形成可修正、可追踪、可关闭的重审流程。

### 5.2 开发内容

- 建立 `re_review_items` 流程。
- 建立 `correction_requests` 流程。
- 重审项支持补全字段后重新进入 pending。
- 报错通过后可更新 `clothes`。
- 核心字段错误触发积分待定或扣回流程。

### 5.3 暂停点

遇到以下情况暂停并请求用户决策：

1. 报错修正哪些字段时不扣分，哪些字段时需要进入积分待定或扣回。
2. 被重审推翻的数据是否保留原贡献者署名。
3. 重审项是否允许普通用户直接编辑，还是只能提交候选修正版。

已确认：

- 重审净胜达到 5 且字段完整后，进入正式库。

### 5.4 验收标准

- 重审项来源可追踪。
- 重审修正不会直接覆盖正式库而无记录。
- 报错处理流程有明确状态。
- 积分调整有流水。

## 6. 阶段五：管理员制度

### 6.1 目标

实现普通管理员和超级管理员的职责边界，避免自审和越权。

### 6.2 开发内容

- 普通管理员可处理低风险项。
- 高风险项、争议项、自己参与过的项不展示。
- 超级管理员可最终裁决。
- 超级管理员可手动授权或取消普通管理员。
- 月度管理员轮换记录写入 `admin_terms`。

### 6.3 暂停点

遇到以下情况暂停并请求用户决策：

1. “低风险项”和“高风险项”的具体判定规则。
2. 普通管理员是否能驳回数据，还是只能提交终审建议。
3. 异常刷分用户是否能进入管理员候选。

已确认：

- 普通管理员月度轮换第一版按规则自动执行，并写入 `admin_terms`。

### 6.4 验收标准

- 管理员看不到自己参与过的审核项。
- 普通管理员不能处理争议项和高风险项。
- 超级管理员可做最终裁决。
- 任期变更可追踪。

## 7. 阶段六：积分、排行榜与权益

### 7.1 目标

基于积分流水生成等级、排行榜、月榜冻结和权益展示。

### 7.2 开发内容

- 从 `points_ledger` 通过 view / RPC 汇总用户总积分。
- `profiles` 不保存权威积分总分；如需性能优化，可建立可重建缓存。
- 生成当月榜、上月榜、总榜。
- 每月 1 日按 UTC+8 冻结上月榜。
- 计算 Lv1 至 Lv4 等级。
- 接入投票权重、贡献者排序、首页鸣谢、广告免除权。

### 7.3 暂停点

遇到以下情况暂停并请求用户决策：

1. 月榜冻结用数据库任务、手动触发，还是前端读取时计算。
2. 上月前 10 的权益是“广告免除权”还是“首页滚动鸣谢”。当前需求文档中两处表述不完全一致。
3. Lv4 当月榜首体验卡是否和累计 10000 分永久权益共用同一字段。

### 7.4 验收标准

- 总积分可由流水复算。
- 月榜可按 UTC+8 冻结。
- 等级和投票权重一致。
- 权益展示不影响核心审核流程。

## 8. 阶段七：前端整合

### 8.1 目标

把新增审核能力接入现有界面，同时保持表单复用和操作一致性。

### 8.2 开发内容

- 图鉴缺失项、图鉴仲裁入库、重审项继续复用 `ClothesEntryForm.vue`。
- 管理后台增加重审池入口。
- 增加陪审团投票入口。
- 增加报错入口。
- 服装详情页增加贡献者展示。
- 用户中心增加积分、等级、权益展示。

### 8.3 暂停点

遇到以下情况暂停并请求用户决策：

1. 当前没有服装详情页时，贡献者展示先放在哪里。
2. 陪审团入口放首页、后台，还是单独页面。
3. 重审池是否对普通用户开放。
4. 报错入口是否所有登录用户可见。

### 8.4 验收标准

- 现有缺失项补录不退化。
- 现有管理员仲裁入库不退化。
- 投票、重审、报错入口清晰。
- 移动端和桌面端布局不重叠、不截断。

## 9. 测试与验收策略

### 9.1 数据库测试

- migration 可应用。
- RLS 不泄露非授权数据。
- 重复提交、重复投票、重复积分能被约束拦截。
- 自动入库 RPC 对 5 次一致、字段不一致、缺套装、缺编号分别有明确结果。

### 9.2 前端测试

- `npm run build` 必须通过。
- 缺失项补录流程可用。
- 管理员入库流程可用。
- 套装申请审核流程可用。
- 标签、星级、属性、套装字段不混乱。

### 9.3 线上数据安全

- 所有线上 schema 修改先走 migration。
- 批量清洗或批量状态迁移前必须单独确认。
- 操作前保留可回滚思路或备份方案。
- 不直接在前端绕过数据库约束。

## 10. 推荐开发顺序

建议按以下顺序推进：

1. 确认第 0.4 节的剩余暂停点。
2. 完成 development 只读前检和生成产物刷新。
3. 新增贡献 / 积分基础事实表和默认拒绝策略。
4. 单独建立最小只读查询面。
5. 依次接入正式库补全、管理员仲裁、自动入库 RPC。
6. 数据库闭环稳定后，再做前端只读展示。
7. 重审、报错、陪审团、排行榜和权益按后续独立阶段推进。

## 11. 当前待用户决策清单

以下问题需用户确认后才能进入实现：

1. 自动入库来源 pending 的权威保留策略。
2. 同一用户对同一服装跨来源事件的重复计分规则。
3. 用户账号删除后的贡献署名与积分流水匿名化保留方式。
4. 后续重审项是否允许普通用户提交候选修正版。
5. 排行榜、管理员轮换和视觉权益不属于当前阶段，进入对应阶段前另行确认。
