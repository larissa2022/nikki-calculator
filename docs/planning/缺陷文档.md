# 缺陷文档

本文档用于记录已经复现或高度可信的产品缺陷、技术缺陷和待验证异常。它不替代需求文档；需求文档记录“应该怎么做”，缺陷文档记录“当前哪里没有按预期工作”。

## 缺陷列表

| 编号 | 日期 | 模块 | 严重度 | 状态 | 问题 |
| --- | --- | --- | --- | --- | --- |
| BUG-20260721-017 | 2026-07-21 | 图鉴管理 / 正式库补全 | 高 | 已修复 | 真实管理员补全已有正式服装时，审核端只发送 5 个非零属性键，而 pending 保存 10 个属性键（含反向属性 0 分），数据库 JSON 全等校验将语义相同的数据误判为不一致并回滚。 |
| BUG-20260715-016 | 2026-07-15 | 图鉴管理 / 服装申请审核 | 中 | 已关闭 | 后台已有服装申请驳回方法，但管理员候选页没有入口，无法完成“驳回 → 重提 → 审核入库”业务链。 |
| BUG-20260628-014 | 2026-06-28 | 衣柜录入 / 图鉴加载 | 高 | 已关闭 | 用户录入几千件衣服后刷新页面，衣柜显示数量降到一百多件；风险点包括图鉴未分页拉全量，以及录入/保存时用不完整本地数组整包覆盖云端衣柜。 |
| BUG-20260625-001 | 2026-06-25 | 注册 / 登录 | 中 | 已关闭 | 注册流程缺少前端状态校验：当邮箱已存在或已通过确认链接激活时，前端仍容易让用户停留在“等待验证码/重新发送验证码”的认知里，缺少明确提示与引导。 |
| BUG-20260625-002 | 2026-06-25 | 缺失图鉴提交 | 高 | 已关闭 | 套装选择存在交互异常：无套装选项，选择后不展示，且可能导致页面卡死。 |
| BUG-20260625-003 | 2026-06-25 | 缺失图鉴提交 | 中 | 已关闭 | 短编号缺少前端必填校验；游戏内必然存在短编号，未填写时应提示错误。 |
| BUG-20260625-004 | 2026-06-25 | 缺失图鉴提交 | 中 | 已关闭 | 页面刷新或网络波动会导致已填写内容丢失，需要评估本地草稿保存或恢复机制。 |
| BUG-20260625-005 | 2026-06-25 | 数据库 / 权限 | 高 | 已修复 | `profiles.role` 当前为 text，并使用 `user`、`admin`、`super_admin` 区分角色；已在 dev 引入 `role_level` 双轨兼容，权限冒烟验证无误。 |
| BUG-20260627-006 | 2026-06-27 | 缺失图鉴提交 / 用户衣柜 | 中 | 已关闭 | 用户提交的缺失图鉴在正式入库后，不会自动加入提交者的用户衣柜。 |
| BUG-20260627-007 | 2026-06-27 | 套装典藏馆 / 数据刷新 | 中 | 已关闭 | 页面刷新慢，且“全部套装”列表经常不刷新或刷新不及时。 |
| BUG-20260627-008 | 2026-06-27 | 缺失图鉴提交 / 所属套装下拉 | 低 | 已关闭 | 所属套装下拉偶发显示“暂无套装数据”，刷新页面后套装数据出现；初步判断为页面假死或异步加载状态未及时刷新。 |
| BUG-20260627-009 | 2026-06-27 | 缺失图鉴提交 / 网络状态 | 中 | 已修复 | 提交缺失图鉴时按钮长时间停留在“正在提交申请”，刷新页面后可正常提交；初步判断为网络连接假死或请求超时提示不足。 |
| BUG-20260627-010 | 2026-06-27 | 页面导航 / 状态保留 | 低 | 待观察 | 刷新页面后会回到首页，未保留用户刷新前所在页面；不影响核心功能，但不符合用户连续操作逻辑。 |
| BUG-20260627-011 | 2026-06-27 | 注册 / 登录 | 中 | 已关闭 | 认证流程中进行已注册邮箱注册、未验证邮箱登录、验证码错误 / 过期、验证码重发等操作时，弹窗状态偶发跳回登录页，表现类似闪退。 |
| BUG-20260627-012 | 2026-06-27 | 注册 / 登录 / 重置密码 | 低 | 待观察 | 重置密码实际成功，但前端仍提示网络请求超时；不影响密码更新结果，但会误导用户重复操作。 |
| BUG-20260627-013 | 2026-06-27 | 个人中心 / 用户资料 | 中 | 已修复 | 修改昵称后，页面仍显示旧的匿名搭配师代号或邮箱；已改为专用 RPC 更新昵称并本地同步 profile，用户验证无误。 |
| BUG-20260711-015 | 2026-07-11 | 衣柜录入 / 短编号录入 | 低 | 已关闭 | 短编号对应多件服装时，提示使用“归一化短编号、图鉴条目”等内部术语，用户无法理解原因和下一步操作。 |

## BUG-20260721-017 正式库补全误判属性数据不一致

### 现象与影响

- 负责人在 PR #87 Preview 中使用 pending `23` 补全正式服装 `db3_acceptance_20260721_01`，连续三次返回“补全正式库失败”。
- Supabase API 日志记录三次 RPC `400`；Postgres 原始错误均为“存在与本次正式库补全最终数据不一致的待审核记录，已拒绝自动通过”。
- 每次失败后 pending、正式服装、贡献、积分和衣柜均保持原值，事务回滚有效，但第一条积分写路径无法完成真实业务验收。

### 根因

- 缺失图鉴提交使用 `calculateItemScores()` 保存固定 10 个属性键，未选中的反向属性明确保存为 `0`。
- 图鉴审核页另行重建分值，只发送 5 个选中属性键；数值语义相同，但 JSON 结构不同。
- DB-3 为防止错误 pending 被自动通过，使用 `pending.scores is not distinct from p_scores` 严格核对；自动事务 fixture 使用了相同的精简 JSON，未覆盖真实前端表示差异。

### 修复与验证

- 提交端与审核端改为共用同一标准分值构造函数，统一生成 10 属性 JSON；数据库严格校验保持不变。
- 前端补充真实“连衣裙 + 五项完美”的回归样本，必须生成与 pending `23` 相同的 10 属性结构。
- 补全错误映射增加服务端实际不一致文案，后续异常不再退化成无技术原因的通用提示。
- 2026-07-21：本地测试 8 项与生产构建通过；同一真实 pending / 正式服装 / 管理员身份的 development 事务 rehearsal 成功生成贡献、`+5` 积分、衣柜写回和 pending 通过，随后整体 rollback，未消耗人工验收数据。
- 2026-07-21：负责人已在修复版 Preview 使用同一夹具复验；development 回读确认 pending 已批准、正式服装已补全，并形成 1 条贡献、1 条 `+5` 积分流水和衣柜写回，五项事实一致，缺陷状态收口为“已修复”。

## BUG-20260715-016 管理员服装申请缺少驳回入口

### 现象与影响

- 管理员能看到并打开 `pending_clothes` 服装候选，但页面只有“一键入库并结案”，没有驳回按钮。
- 后台已有 `rejectPendingItem`，未接入候选页，导致管理员无法先驳回错误申请，DB-0 业务验证无法继续。

### 修复与验证

- 2026-07-16：PR #77 在当前候选提示区增加“驳回当前服装申请（N 条）”，仅处理当前选中候选对应的 pending ID。
- 驳回前显示申请数量并二次确认；成功后给出结果提示、清空旧选择并刷新列表；状态已变化时不静默成功。
- development 管理员与最高站长各完成一次真实驳回；普通用户两次重提均成功，最终由管理员审核入库并同步提交者衣柜。
- 数据库终态为 `rejected / rejected / approved`，正式服装与衣柜写回均存在；合并前验证状态为“已验证，待 PR #77 合并”。
- 2026-07-16：PR #77 已合入 `develop`，合并后页面、数据加载、衣柜展示和控制台冒烟通过，状态收口为“已关闭”。

## BUG-20260711-015 短编号无法唯一匹配时提示难以理解

### 现象

- 用户按分类和短编号录入衣柜时，部分编号在图鉴中对应多件服装。
- 浏览器弹窗提示“存在多个归一化短编号相同的图鉴条目，本次已跳过这些编号”。
- 用户看不懂“归一化短编号”和“图鉴条目”，也不知道应该如何继续录入。

### 影响

- 安全跳过逻辑本身有效，但错误文案会让用户误以为输入格式错误或系统故障。
- 用户不知道其他可识别编号仍会继续录入，也不知道可以改用名称录入。
- 影响“快速录入衣柜”核心流程的可理解性。

### 期望行为

- 使用普通玩家能理解的语言说明：图鉴中有多件服装使用相同编号，系统无法确定具体服装。
- 明确告知这些服装没有加入衣柜，其他可识别编号不受影响。
- 给出可执行的下一步：改用“按名称录入”。

### 修复记录

- 2026-07-11：优化短编号冲突弹窗，移除“归一化短编号、图鉴条目”等内部术语。
- 2026-07-11：同步优化录入结果报告，统一说明未录入原因、其他编号不受影响和按名称录入的处理方式。
- 2026-07-12：本地 production build 通过；development 页面加载正常，无错误覆盖层或控制台错误。
- 2026-07-12：development 当前无真实重复编号样本，使用仅限临时浏览器会话的响应拦截构造重复编号；混合输入冲突编号与有效编号后，冲突项被安全跳过，有效项正常录入。
- 2026-07-12：弹窗和结果报告均使用玩家可理解的说明，明确冲突项未加入衣柜、其他编号不受影响，并引导改用“按名称录入”；状态收口为“已关闭”。

## BUG-20260628-014 刷新后衣柜显示数量大幅减少

### 现象

- 用户已录入几千件衣服。
- 刷新页面后，衣柜显示数量变成一百多件。
- 用户感知为已录入衣服消失。

### 影响

- 直接影响普通玩家衣柜录入和高分计算路径。
- 用户会误判为云端衣柜数据丢失，信任风险高。
- 若在错误状态下继续操作，可能进一步触发基于不完整图鉴的录入、套装补齐或统计误差。

### 初步判断

- `user_wardrobes.owned_clothes` 是云端保存的用户衣柜 ID 数组。
- 页面刷新后需要先加载完整 `clothes` 图鉴，再用衣柜 ID 反查展示；旧实现单次查询可能受 Supabase 默认返回上限影响。
- 更高风险点是衣柜保存使用整包 JSON 数组覆盖：如果云端同步未完成、页面假死、用户一次录入很多衣服，前端可能用不完整的本地数组覆盖原有几千件云端衣柜。
- 录入和套装补齐本质上是“加法操作”，不应使用整包覆盖语义。

### 修复方向

1. 将 `clothes` 全量读取改为分页循环读取，直到最后一页不足 1000 条。
2. `stages` 读取也统一走分页函数，避免后续关卡数据增长出现同类问题。
3. 从云端同步衣柜时，对 `owned_clothes` 做数组校验、字符串化和去重。
4. 录入和套装一键补齐改为“读取云端当前衣柜 + 合并新增 ID + 去重保存”。
5. 普通覆盖保存增加大幅缩水拦截，避免几千件被异常覆盖为少量数据。
6. 本次不修改生产数据库，不做批量数据修复。

### 修复记录

- 2026-06-28：`useWardrobe.loadData()` 改为分页读取完整 `clothes` 与 `stages`。
- 2026-06-28：`syncWardrobeFromCloud()` 读取云端衣柜后对 ID 数组做规范化处理。
- 2026-06-28：`saveWardrobeToCloud()` 增加 `merge` 保存模式；录入和套装补齐使用合并保存，避免覆盖云端已有衣柜。
- 2026-06-28：普通覆盖保存增加大幅缩水拦截，检测到云端几千件将被覆盖为少量数据时阻止保存并提示刷新。
- 2026-06-28：批量录入匹配过程分批让出主线程，降低大量输入时页面假死概率。
- 2026-06-28：本地 `npm.cmd run build` 通过。
- 验证记录：2026-06-30 Preview / production 人工回归通过，衣柜保存与刷新后数量稳定，未复现几千件衣柜刷新后异常缩水。

## BUG-20260625-001 注册流程缺少前端状态校验

### 现象

- dev 项目注册测试时，用户曾收到确认链接并完成激活。
- 之后继续按“验证码注册”理解流程时，前端没有清晰提示该邮箱已经存在或已经确认。
- 用户会误以为“自己不能给自己发邮件”或“验证码没有发送成功”。

### 影响

- 新用户注册体验不稳定，容易卡在错误路径。
- 测试人员难以判断问题来自 Supabase 邮件模板、邮箱投递、账号已确认，还是前端流程。
- 如果生产环境也存在同类状态，可能导致重复注册咨询和用户流失。

### 期望行为

- 注册提交后，如果 Supabase 返回邮箱已注册、已确认、重复注册或验证码无法重发，应展示明确中文提示。
- 对已确认账号，应引导用户直接登录。
- 对未确认账号，应允许重新发送注册验证码。
- 前端文案需要区分“账号已存在”“账号未验证”“验证码已发送”“邮件发送频率限制”。

### 初步判断

- 当前代码使用 `signUp`、`verifyOtp({ type: 'signup' })` 和 `resend({ type: 'signup' })`。
- Auth 配置已经能完成注册，但前端缺少足够细的错误分支和用户�n8�kh��춻�q�^us.update({ username })` 逻辑。
- 新建 migration，revoke `update_profile_username(text)` 的 authenticated execute 权限。
- 不删除用户已设置的 `profiles.username` 数据。

## 2026-06-27 16:50 - add_profile_role_level

### 基本信息

| 项目 | 内容 |
| --- | --- |
| 变更类型 | schema / function / trigger |
| 目标环境 | development，生产待确认 |
| migration 文件 | `supabase/migrations/20260627165000_add_profile_role_level.sql` |
| 关联缺陷 | `BUG-20260625-005` |
| 关联方案 | `docs/planning/角色权限数字化迁移方案.md` |
| 执行人 | Codex |
| 确认人 | 用户 |

### 变更目的

- 将 `profiles.role` 字符串权限迁移为 `profiles.role_level` 数字权限。
- 保留旧 `role` 字段，进入双轨兼容期。
- 防止非法字符串、拼写错误或前后端判断不一致导致权限风险。

### 影响范围

| 对象 | 影响 |
| --- | --- |
| 表 | `profiles` |
| 字段 | 新增 `role_level smallint not null default 0`，约束值只能为 `0 / 1 / 2` |
| 函数 | 新增 `profile_role_to_level(...)`、`profile_role_level_to_text(...)`、`sync_profile_role_fields()` |
| RPC / 权限函数 | 替换 `is_super_admin()`、`is_admin_or_super_admin()`，优先识别 `role_level` 并兼容旧 `role` |
| Trigger | 新增 `sync_profile_role_fields`，同步 `role` 与 `role_level` |
| 前端模块 | `useAuth.js`、`adminService.js`、`useAudit.js`、`AdminView.vue`、`UserManageBoard.vue` |
| 线上用户影响 | 未推生产前无影响；推生产后角色字段进入兼容期，不删除旧字段 |

### 变更内容

- 新增 `role_level` 并从旧 `role` 回填：`user -> 0`、`admin -> 1`、`super_admin -> 2`。
- 新增 check 约束 `profiles_role_level_check`。
- 新增同步触发器，兼容旧代码写 `role` 和新代码写 `role_level`。
- 新用户默认写入 `role = 'user'`、`role_level = 0`。
- 前端新增 `src/utils/roles.js`，统一角色映射和权限判断。

### 执行结果

| 环境 | 结果 |
| --- | --- |
| 本地 migration 文件 | 已创建 |
| `supabase.cmd migration list` | 成功；确认 `20260627165000` 仅存在本地，目标 linked project 为 dev：`tfwejruvdahonacyldrg` |
| development | 用户已手动执行 `supabase db push`；复查 `supabase.cmd migration list` 显示 Local / Remote 均包含 `20260627165000` |
| 类型生成 | 待重新执行；当前 `src/types/supabase.ts` 的 `profiles` 类型块尚未出现 `role_level` |
| 本地构建 | `npm.cmd run build` 成功 |
| production | 未执行 |

### 后续命令

```powershell
npm run db:types
npm run build
```

PowerShell 如遇 `npm.ps1` 执行策略限制，可改用：

```powershell
npm.cmd run db:types
npm.cmd run build
```

### 复查 SQL

```sql
select role, role_level, count(*)
from public.profiles
group by role, role_level
order by role_level;

select id, email, role, role_level
from public.profiles
where role = 'super_admin' or role_level = 2;

select public.is_super_admin();
select public.is_admin_or_super_admin();
```

### 回滚方案

- 前端回滚到读取旧 `profiles.role` 字段。
- 新建 migration，将 `is_super_admin()` 和 `is_admin_or_super_admin()` 恢复为旧字符串判断。
- 保留 `role_level` 字段不删除，避免破坏数据。
- 如需同步旧字段，可执行：

```sql
update public.profiles
set role = case role_level
  when 2 then 'super_admin'
  when 1 then 'admin'
  else 'user'
end;
```

## 2026-06-27 14:30 - sync_approved_clothes_to_submitter_wardrobes

### 基本信息

| 项目 | 内容 |
| --- | --- |
| 变更类型 | RPC |
| 目标环境 | development，生产待确认 |
| migration 文件 | `supabase/migrations/20260627143000_sync_approved_clothes_to_submitter_wardrobes.sql` |
| 关联缺陷 | `BUG-20260627-006` |
| 执行人 | Codex |
| 确认人 | 待确认 |

### 变更目的

- 缺失图鉴正式入库后，自动把新 `clothes.id` 加入所有有效提交者的 `user_wardrobes.owned_clothes`。
- 自动入库和管理员仲裁入库使用同一套数据库写回逻辑。
- 避免只给参考数据提交者写回衣柜，遗漏同一缺失项的其他有效提交者。

### 影响范围

| 对象 | 影响 |
| --- | --- |
| 表 | 无结构变更 |
| 字段 | `user_wardrobes.owned_clothes` 会在入库成功后追加正式 `clothes.id` 并去重 |
| RPC | 新增 `add_clothes_to_submitter_wardrobes(...)`、`is_admin_or_super_admin()`、`approve_pending_clothes_arbitration(...)`；替换 `submit_clothing_contribution(...)` |
| RLS / Policy | 无 policy 变更；通过 `security definer` RPC 集中执行跨用户衣柜写回 |
| 前端模块 | `adminService.submitArbitration` 改为调用 `approve_pending_clothes_arbitration` |
| 线上用户影响 | 未推生产前无影响；推生产后有效提交者会自动获得入库部件 |

### 变更内容

- `add_clothes_to_submitter_wardrobes(user_ids, clothes_id)`：内部函数，对多个用户衣柜执行 upsert，追加并去重 `clothes_id`。
- `submit_clothing_contribution(...)`：5 次一致自动入库时，收集已有匹配 pending 的提交者和当前触发提交者，并同步写回这些用户衣柜。
- `approve_pending_clothes_arbitration(...)`：管理员仲裁入库时，收集 `p_pending_ids` 对应的所有原提交者，完成正式入库、pending 标记通过和衣柜写回。
- 前端管理员仲裁不再直接 `insert clothes`，改为调用 RPC。

### 执行结果

| 环境 | 结果 |
| --- | --- |
| 本地构建 | `npm.cmd run build` 成功 |
| development | `supabase.cmd db push` 成功 |
| production | 未执行 |

### 回滚方案

- 新建 migration，恢复上一版 `submit_clothing_contribution(...)` 函数定义。
- 删除或 revoke `approve_pending_clothes_arbitration(...)` 的 authenticated execute 权限。
- 前端 `adminService.submitArbitration` 回滚为旧版直接插入逻辑，或改用临时人工审核流程。

## 2026-06-27 09:30 - require_numeric_game_id

### 基本信息

| 项目 | 内容 |
| --- | --- |
| 变更类型 | RPC |
| 目标环境 | development，生产待确认 |
| migration 文件 | `supabase/migrations/20260627093000_require_numeric_game_id.sql` |
| 关联缺陷 | `BUG-20260625-003` |
| 执行人 | Codex |
| 确认人 | 待确认 |

### 变更目的

- 缺失图鉴提交时，短编号必须填写。
- 短编号只允许数字。
- 防止用户绕过前端直接调用 RPC 写入空值、`N` 或非数字短编号。

### 影响范围

| 对象 | 影响 |
| --- | --- |
| 表 | 无结构变更 |
| 字段 | `pending_clothes.game_id`、`clothes.game_id` 写入值收紧为数字字符串 |
| RPC | 替换 `submit_clothing_contribution(...)` |
| RLS / Policy | 无 |
| 前端模块 | `MissingItemsQueue.vue`、`ClothesEntryForm.vue` |
| 线上用户影响 | 未推生产前无影响；推生产后非数字短编号提交会被拒绝 |

### 变更内容

- RPC 内 trim `p_game_id` 为 `v_game_id`。
- `v_game_id` 为空时抛出“短编号不能为空”。
- `v_game_id` 非数字时抛出“短编号只允许填写数字”。
- 自动入库与待审核写入统一使用 `v_game_id`。

### 执行结果

| 环境 | 结果 |
| --- | --- |
| 本地构建 | `npm.cmd run build` 成功 |
| development | 待执行 `supabase db push` |
| production | 未执行 |

### 回滚方案

- 新建 migration，恢复上一版 `submit_clothing_contribution(...)` 函数定义。
- 前端可回滚 `MissingItemsQueue.vue` 和 `ClothesEntryForm.vue` 的短编号校验逻辑。

## 2026-06-25 22:20 - apply_existing_migrations_to_development

### 基本信息

| 项目 | 内容 |
| --- | --- |
| 变更类型 | schema / RPC / RLS / function |
| 目标环境 | development |
| migration 文件 | `supabase/migrations/20260418113144_remote_history_placeholder.sql` 等 6 条 |
| 关联需求 | `docs/requirements/需求文档.md` 第 3 节 |
| 关联技术方案 | `docs/planning/技术实现文档.md` 第 9 节 |
| 执行人 | Codex |
| 确认人 | 用户 |

### 变更目的

- 将本地已有 migrations 应用到 development 项目。
- 让 dev 项目具备当前代码依赖的基础表、RPC、RLS 和标签清洗函数。

### 影响范围

| 对象 | 影响 |
| --- | --- |
| 表 | `clothes`、`pending_clothes`、`pending_suits`、`suits`、`profiles`、`app_errors` 等 |
| 字段 | 补齐 `pending_clothes`、`clothes`、`profiles` 的当前代码依赖字段 |
| RPC | `submit_clothing_contribution`、`normalize_known_clothing_tags` 等 |
| RLS / Policy | 补齐 pending suits、pending clothes、profiles、app errors 相关基础策略 |
| 前端模块 | 无直接代码改动 |
| 线上用户影响 | 无，目标为 development |

### 变更内容

- 首次 `supabase db push` 应用前 4 条 migrations 后，在 `20260624150000_normalize_known_tags.sql` 失败。
- 失败原因：旧基础 schema 中 `pending_clothes` 尚无 `tags` 列。
- 已修正 `20260624150000_normalize_known_tags.sql`，使清洗语句在列不存在时安全跳过。
- 新增 `20260625141000_dev_schema_catchup.sql`，补齐 development 缺失的表、字段、索引、触发器和基础 policy。
- 第二次推送因 `is_super_admin()` 创建顺序晚于 policy 失败。
- 已修正 `20260625141000_dev_schema_catchup.sql`，提前创建 `is_super_admin()`。
- 第三次 `supabase db push` 成功。

### 执行结果

| 项目 | 结果 |
| --- | --- |
| `supabase db push` | 成功 |
| `supabase migration list` | 成功，Local / Remote 均包含 6 条 migration |
| `npm run build` | 成功 |
| `npm run db:types` | 成功，已根据 development 项目更新 `src/types/supabase.ts` |
| `npm run db:dump` | 用户本机执行成功，已更新 `supabase/schema.sql` |
| `docs/database/schema.md` | 已根据 dev schema dump 更新 |

### 当前 migration 状态

```text
20260418113144 -> applied
20260418121055 -> applied
20260624143000 -> applied
20260624144500 -> applied
20260624150000 -> applied
20260625141000 -> applied
```

### 待补动作

- [x] 执行 `npm run db:types`，重新生成 dev 类型。
- [x] 执行 `npm run db:dump`，更新 `supabase/schema.sql`。
- [x] 根据新 schema 更新 `docs/database/schema.md`。

### 回滚方案

- 目标为 development，如迁移结果不可用，可重建 dev 项目后重新执行 migrations。
- 不影响 production。

## 2026-06-25 22:07 - dev_environment_connection_verified

### 基本信息

| 项目 | 内容 |
| --- | --- |
| 变更类型 | environment / verification |
| 目标环境 | development |
| migration 文件 | 无 |
| 关联需求 | `docs/database/数据库开发安全方案.md` |
| 关联技术方案 | `docs/database/环境信息.md` |
| 执行人 | 用户 |
| 确认人 | Codex |

### 变更目的

- 确认本地 Supabase CLI 已可访问 development 项目。
- 确认 `npm run db:types` 默认连接 development 项目。
- 确认前端构建仍然通过。

### 执行结果

| 项目 | 结果 |
| --- | --- |
| `supabase link --project-ref tfwejruvdahonacyldrg` | 成功 |
| `npm run db:types` | 成功，连接 development 项目 |
| `npm run build` | 成功 |
| dev public schema | 当前为空，尚未同步本地 migrations |
| `supabase migration list` | 成功；Remote 为空，Local 有 5 条待应用 migration |

### 备注

- `src/types/supabase.ts` 已由 development 项目生成。
- 当前 dev 项目是新项目，public schema 尚无业务表。
- 下一步应在 development 上执行 `supabase db push`，应用 5 条本地 migrations。

后续按时间倒序记录，最新变更放在最前面。

## 4. 待补安全前置项

在正式数据库开发前，需要完成：

- [x] 建立 development Supabase 项目。
- [x] 明确 production 项目 ref：`fopyjewbsvusftpqbtml`。
- [x] 明确 development 项目 ref：`tfwejruvdahonacyldrg`。
- [x] 本地 Supabase link 已切换到 development。
- [ ] 完成生产库首次备份。
- [ ] 验证备份可恢复。
- [ ] 明确本地 `.env.local` 连接开发库。
- [ ] 明确生产部署环境变量不在本地误用。
- [ ] 建立第一条开发库 migration 验证记录。

## 5. 常用检查命令

查看 migration 状态：

```powershell
supabase migration list
```

导出 schema：

```powershell
npm run db:dump
```

生成类型：

```powershell
npm run db:types
```

生成生产类型：

```powershell
npm run db:types:prod
```

前端构建：

```powershell
npm run build
```

确认当前 Supabase 链接：

```powershell
supabase status
```

## 6. 数据变更预检示例

批量更新前：

```sql
select count(*)
from public.<table_name>
where <condition>;

select *
from public.<table_name>
where <condition>
limit 20;
```

批量更新后：

```sql
select count(*)
from public.<table_name>
where <condition>;
```

## 7. RPC 变更记录要求

如果变更 RPC，必须记录：

- RPC 名称。
- 旧函数定义备份位置。
- 新函数参数。
- 新函数返回值。
- 权限 grant / revoke。
- 是否 `security definer`。
- 是否校验当前用户。
- 是否校验角色。
- 是否校验自审。
- 回滚 SQL。

## 8. RLS 变更记录要求

如果变更 RLS，必须记录：

- 表名。
- 是否启用 RLS。
- 新增 / 修改 / 删除的 policy。
- anon 用户结果。
- authenticated 普通用户结果。
- admin 用户结果。
- super_admin 用户结果。
- 是否存在越权读取风险。
- 是否存在误封正常功能风险。

## 2026-06-29 - production_pending_batch_20260628

| 项目 | 内容 |
| --- | --- |
| 环境 | production |
| SQL 文件 | `docs/database/production_pending_batch_20260628.sql` |
| 变更类型 | 生产 pending_clothes 积压第一批低风险处理 |
| 执行结果 | `inserted_clothes = 4`、`approved_pending = 26`、`re_review_pending = 8` |
| 影响表 | `clothes`、`pending_clothes`、`user_wardrobes` |
| 入库范围 | `饰品-头饰-发饰 #004`、`饰品-头饰-发饰 #005`、`饰品-头饰-发饰 #006`、`妆容 #1108` |
| 暂缓范围 | `上装 #2176`、`连衣裙 #4015`，因套装归属 / 星级冲突需人工确认 |

### 处理说明

- 多数一致版本写入正式图鉴 `clothes`。
- 对应多数提交记录更新为 `approved`。
- 少数冲突记录更新为 `re_review`，保留后续人工复核入口。
- SQL 内部合并写回有效提交者的 `user_wardrobes.owned_clothes`，避免正式入库后提交者衣柜未同步。
- 本次未处理 `submitted_by is null` 的历史 / 匿名 pending 数据。

### 执行后校验

```text
inserted_clothes  | 4
approved_pending  | 26
re_review_pending | 8
```
