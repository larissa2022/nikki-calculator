# 需求文档

## 0. 文档说明

本文档用于整理图鉴录入、审核、积分、权限和反馈机制。当前整理仅做结构合并、规则归类和数据实体梳理，不改变既有功能口径。

规则来源说明：

- 产品规则的唯一真实来源为 [`DECISIONS.md`](../ai/DECISIONS.md)。
- 本文档保留需求背景和功能范围；如与 `DECISIONS.md` 冲突，以 `DECISIONS.md` 为准。

已暂缓或已由其他模块覆盖的需求：

- ~~新增用户名称：新注册用户填写用户名，不可重复；未填写时显示为匿名搭配师_7A2B。~~
- ~~新增修改信息模块：允许修改名称和密码，名称不可重复，密码需要邮件验证。~~
- ~~活跃用户：当月至少有 5 次提交行为或审核行为。~~

## 0.1 当前实现状态

本节用于标注截至 2026-06-30 的实现状态，只做当前版本收口，不改变下方原始需求口径。

### 已实现

- 按分类 + 短编号录入衣柜：已实现，并通过 Preview / production 人工回归。
- 衣柜刷新后数量稳定：已实现。已修复图鉴分页加载、云端衣柜合并保存和大幅缩水拦截，并通过 Preview / production 人工回归。
- 正式库已有但资料不全补全：已实现。管理员可处理正式库已有但字段不全的 pending 项，避免唯一约束重复失败，并通过人工回归。
- 提交者衣柜写回：已实现。缺失图鉴入库或补全后，相关有效提交者衣柜可自动获得入库部件，并通过人工回归。
- 管理员审核卡片文案口径：已实现。页面不再使用“版本、变体、字段组合、冲突版本、一致提交”，改用“资料一致情况、最多人认可、不同意见、可按多数入库、需要继续收集”等口径，并通过人工回归。
- 生产环境禁用 Vue DevTools：已实现，并通过 production 人工回归。

### 未实现 / 暂缓

- `points_ledger` 积分流水：未实现 / 暂缓。当前仅确认它应作为积分权威来源，暂不直接开发。
- `jury_votes` 陪审团投票：未实现 / 暂缓。
- `re_review_items` 重审池：未实现 / 暂缓。
- `correction_requests` 报错修正：未实现 / 暂缓。
- 排行榜：未实现 / 暂缓。
- 普通管理员轮换：未实现 / 暂缓。

### 待决策区

- 正式库已有但资料不全时，是只补空字段，还是允许覆盖已有字段。
- 自动入库、管理员仲裁、正式库补全、重审修正、报错修正是否都使用 +10 分，还是按来源配置不同分值。
- 积分扣回是否只追加负数流水，不删除原流水。
- 匿名历史数据是否展示为“匿名历史数据”，以及是否参与前 5 / 前 3。
- 待补套装状态什么时候关闭。
- 重审项是否允许普通用户提交候选修正版，还是第一版只允许管理员处理。
- 报错修正哪些字段属于低风险，哪些字段属于高风险。
- 报错通过是否奖励报错者积分。
- 报错导致原数据被推翻时，原贡献者积分是否进入 pending 或扣回。

### 已确认规则

- 积分权威来源为 `points_ledger`。
- 图鉴 / 衣柜条目审核通过并成功入库后，符合最终入库数据的有效提交者获得 +10 分。
- 第一版积分状态默认 `awarded`，审核通过后立即生效。
- 前 5 有效提交者：提交数据与最终入库数据一致，且 `submitted_by` 可识别为真实用户；按提交时间排序取前 5 个获得积分。
- 前 3 展示贡献者：从前 5 有效提交者中按提交时间排序取前 3 个用于页面展示。
- 缺套装但编号完整、核心字段完整的数据允许正式入库，同时创建 `re_review_items`，状态为待补套装。
- `re_review_items` 是待补套装状态的权威追踪来源。

### 废弃设计

- 废弃：图鉴审核通过后的积分默认先进入 `pending` 等管理员确认。
- 废弃：使用 `profiles` 保存权威积分总分。
- 废弃：前 3 展示贡献者按积分、信用等级或最终一致度排序。
- 废弃：缺套装但编号完整的数据必须留在 pending，不能进入正式库。

## 0.2 下一阶段最小可落地范围

本阶段只整理开发计划，不实现功能、不修改数据库、不变更现有线上行为。下一阶段建议围绕“积分 / 贡献者 / 重审池 / 报错修正”做最小闭环，先保障数据可追溯，再扩展投票、排行榜和权益展示。

### 积分流水 `points_ledger`

- 第一版目标：记录通过审核、补全、重审修正、报错修正等行为产生的积分流水；`points_ledger` 作为积分唯一权威来源。
- 不做什么：第一版不做排行榜、不做 Lv4 权益、不做积分缓存、不直接依赖 `profiles.total_points` 作为权威分数。
- 第一版口径：图鉴审核通过后积分默认 `awarded` 并立即生效；扣回规则见待决策区。

### 贡献者记录

- 第一版目标：入库时记录前 5 个有效提交者；服装详情或后续展示最多展示前 3 位贡献者。
- 不做什么：第一版不做复杂荣誉、动态彩名、首页鸣谢、广告免除。
- 第一版口径：前 5 有效提交者从与最终入库数据一致的提交者中按提交时间排序取前 5；前 3 展示贡献者从前 5 中按提交时间排序取前 3。

### 重审池 `re_review_items`

- 第一版目标：承接缺套装、字段冲突、字段缺失、被报错后需要修正的数据，形成可追踪状态。
- 不做什么：第一版不做完整陪审团投票闭环，不允许普通用户直接覆盖正式库。
- 第一版口径：缺套装但编号完整的数据允许正式入库，同时创建 `re_review_items`，状态为待补套装。

### 报错修正 `correction_requests`

- 第一版目标：用户可对正式库条目提交错误报告和建议修正，由管理员或后续重审流程处理。
- 不做什么：第一版不做公开留言板、不做建议点赞排序、不自动改正式库。
- 待决策：哪些字段修正属于低风险，哪些字段修正会触发积分待定或扣回。

## 1. 用户、积分与荣誉体系

### 1.1 积分规则

积分来源：

1. 新增部件通过审核后，提交数据与最终录入数据完全一致的用户获得积分，积分 +10。
2. 多人提交制下，符合最终入库数据的提交者都可获得积分。
3. 参与仲裁且仲裁结果正确，积分 +10。
4. 同一用户对同一 `category + game_id` 只能计分一次。
5. 自动入库只奖励前 5 个有效提交者；与最终入库数据一致的提交者按提交时间取前 5。
6. 重复提交、低质量提交不计分。
7. 重审改正者和原始错误提交者的积分应区分记录，具体分值见待决策区。
8. 被重审推翻的数据，相关积分处理方式见待决策区。

积分数据来源：

- `points_ledger` 是积分的唯一权威来源。
- `profiles` 不保存权威总分，避免用户资料表与积分流水不一致。
- 总分、等级、排行榜通过 SQL view / RPC 汇总生成。
- 后期如有性能压力，可增加积分缓存表，但缓存只用于展示加速，必须能从 `points_ledger` 重建。

### 1.2 积分排行榜

排行榜类型：

- 总榜：累计积分排名。
- 当月榜：按自然月统计，时区为 UTC+8。
- 上月榜：每月 1 日冻结上月榜。

排行榜权益：

- 上月排名前 10 的活跃用户网站首页滚动鸣谢。
  - 活跃用户：当月至少有 5 次提交行为或审核行为。

- 当月榜首可获得 Lv4 限时体验权益，下个月 1 日回收。

### 1.3 高信用玩家等级

| 等级 | 名称 | 达成条件 | 投票权重 | 核心权益 |
| --- | --- | --- | --- | --- |
| Lv1 | 见习搭配师 | 累计 500 分 | 1 票 | 铜色新星徽章；服装详情页贡献者署名资格 |
| Lv2 | 资深收集者 | 累计 2000 分 | 2 票 | 银色放大镜徽章；静态彩名；贡献者名单优先展示 |
| Lv3 | 图鉴守护者 | 累计 5000 分 | 3 票 | 金色流光盾牌徽章；动态彩名；可选进场提示 |
| Lv4 | 奇迹编年史官 | 累计 10000 分或当月榜首 | 3 票 | 至尊皇冠徽章；首页滚动鸣谢；专属寄语；可信权重 +3 |

Lv4 特殊规则：

- 累计 10000 分获得的 Lv4 权益为永久权益。
- 当月榜首获得的 Lv4 权益为体验权益，下个月 1 日回收。
- 可信权重 +3 仍需至少 1 位其他用户或管理员确认。
- 不允许审核自己的提交。

激励展示：

- 服装详情页展示贡献者名单，最多展示前 3 位。
- 前 3 位展示贡献者从前 5 个有效提交者中按提交时间排序取前 3。
- Lv2 至 Lv4 权益可在个人中心或图鉴大盘中以未解锁状态展示，用于激励用户继续贡献。
- 专属寄语限制为不超过 15 个字，并展示在用户参与贡献且得分最高的服装详情页。

## 2. 管理员制度

### 2.1 普通管理员

来源与任期：

- 普通管理员每月 1 日轮换。
- 由上月月积分最高的 5 位活跃用户构成。
- 任期结束后自动收回权限。

权限范围：

- 可处理低风险审核项。
- 不可处理高风险项、争议项、自己参与过的项。
- 不显示自己提交或参与过的数据审核入口。
- 无数据库后台管理权限。
- 不能直接编辑正式库历史数据。
- 只能通过审核流程产生新增或状态变更。

### 2.2 超级管理员

权限范围：

- 拥有普通管理员所有权限。
- 可对高风险项和争议项做最终裁决。
- 拥有用户管理入口，可手动授权或取消普通管理员权限。

## 3. 服装录入与审核机制

### 3.1 数据库分层

现有正式库：

- `clothes`：正式衣物。
- `suits`：正式套装。

现有待审核库：

- `pending_clothes`：待审核衣物提交。
- `pending_suits`：待审核套装提交。

规划重审库：

- 用于保存字段缺失、字段冲突或被报错的数据。
- 允许用户补全或修正字段。
- 修正后重新进入 `pending` 流程。

规划贡献与积分记录：

- `clothing_contributions(clothes_id, user_id, source_pending_id, contribution_type, awarded_points)`：记录正式入库后的贡献者和奖励。
- 积分流水表：用于记录积分发放、待定、扣回和来源。

### 3.2 服装录入规则

录入方式：

- 新增服装按照 `分类 + 短编号` 的形式录入。
- 名称、分类、短编号、星级、五组属性、套装状态为核心字段。
- 特殊标签为选填字段。

部件自动仲裁：

1. 编号和套装信息完整，且 5 次提交完全一致，自动进入正式库。
2. 缺套装但编号完整，可进入正式库，并创建 `re_review_items` 记录追踪待补套装状态。
3. 缺编号的数据不参与自动仲裁，直接进入重审或补全流程。
4. 累计 5 次提交但字段不一致，进入人工仲裁或重审。

缺套装追踪：

- 缺套装但编号完整的数据允许进入正式库。
- 待补套装状态通过 `re_review_items` 追踪。
- 如后续新增 `clothes.needs_suit_review`，它只作为查询加速或冗余标记，权威流程仍以 `re_review_items` 为准。

套装自动仲裁：

1. 套装名称 3 次相同提交可直接入库。
2. 套装名称存在相似但不完全相同的情况，进入仲裁池，由用户选择或管理员裁决。

### 3.3 陪审团制度

陪审团行为：

- 用户只能点赞同或驳回。
- 高信用玩家的投票权重按等级计算。
- 用户不能审核自己的提交。

流转规则：

1. 字段完整且候选数据唯一，净胜达到 5 可入库。
2. 字段缺失或候选数据冲突，净胜达到 5 后进入管理员终审或重审完成流程。
3. 净胜达到 -3 进入重审。

### 3.4 重审制度

进入重审的情况：

- 缺编号。
- 字段缺失。
- 5 次提交后字段仍不一致。
- 陪审团净胜 -3。
- 正式库数据被用户报错后需要修正。

重审要求：

- 用户需要补全或审核部件信息。
- 名称、分类、短编号、星级、五组属性、套装状态必须明确。
- 特殊标签可为空。

退出重审：

- 重审修正后进入 `pending`。
- 净胜票达到 5 且字段完整时，进入正式库并移除重审列表。

### 3.5 审核状态流转

```text
pending
  -> approved -> clothes
  -> re_review
  -> rejected

re_review
  -> pending
  -> admin_review
  -> abandoned

admin_review
  -> approved
  -> re_review
  -> rejected
```

## 4. 用户反馈与报错机制

### 4.1 留言板

留言板分为两个模块：

- 错误报告：优先级最高。
- 优化建议：支持点赞机制，高赞优先更新。

优化建议流程：

```text
玩家提议 -> 开发者审核评估 -> 计划中 / 技术无法实现
```

高信用玩家的建议点赞权重沿用积分和投票权重体系。

### 4.2 报错功能

报错流程：

```text
用户报错
  -> 生成 correction request
  -> 陪审团 / 管理员审核
  -> 通过后更新 clothes
```

积分规则：

- 报错通过后，原贡献者积分默认不调整。
- 如果报错导致原数据被重审推翻，积分进入待定或扣回流程。

## 5. 商业化机制

### 5.1 广告机制（待定）

- 可插入广告。
- 上月排行榜前 10 的活跃用户可获得广告免除权。

### 5.2 打赏机制

- 打赏机制待定。

## 6. 数据实体整理

### 6.1 当前已存在数据表

| 表名 | 用途 | 备注 |
| --- | --- | --- |
| `clothes` | 正式服装库 | `needs_suit_review` 可选；权威待补状态由 `re_review_items` 追踪 |
| `suits` | 正式套装库 | 套装名称唯一性需要数据库约束配合 |
| `pending_clothes` | 待审核服装提交 | 当前支持 `status`、`submitted_by`、`suit_id` |
| `pending_suits` | 待审核套装提交 | 当前支持 `status`、`submitted_by` |
| `profiles` | 用户资料和角色 | 当前支持 `role`；不保存权威积分总分 |
| `user_wardrobes` | 用户衣柜 | 与审核机制弱相关 |
| `user_quotas` | 用户次数额度 | 与积分机制弱相关 |
| `stages` | 关卡权重 | 与图鉴审核弱相关 |

### 6.2 为满足需求建议新增的数据表

| 表名 | 用途 |
| --- | --- |
| `re_review_items` | 重审池，保存待修正或有争议的数据 |
| `jury_votes` | 陪审团投票记录，支持权重、去重和防自审 |
| `points_ledger` | 积分流水，支持发放、待定、扣回、冻结月榜 |
| `clothing_contributions` | 贡献者署名、奖励来源、前 5 有效提交者记录 |
| `correction_requests` | 用户报错和正式库修正流程 |
| `feature_requests` | 留言板优化建议、点赞和处理状态 |
| `admin_terms` | 每月普通管理员任期、来源和回收记录 |

建议新增视图 / 缓存：

| 对象 | 用途 |
| --- | --- |
| `user_points_summary` | 从 `points_ledger` 汇总用户总积分 |
| `monthly_points_summary` | 从 `points_ledger` 汇总自然月积分 |
| `leaderboard_snapshots` | 冻结上月榜或历史榜单 |
| `user_points_cache` | 可选展示缓存，不作为权威积分来源 |

## 7. 需求审核与逻辑风险

### 7.1 高优先级风险

1. “净胜 +5 可入库”和“字段缺失/候选冲突需管理员终审或重审”需要在实现中严格区分，否则用户投票可能把缺字段数据直接推入正式库。
2. 积分扣回规则必须流水化。`points_ledger` 已确认为权威来源，任何扣回都应追加负数流水，不直接修改用户总分。
3. Lv4 可信权重 +3 与“自动入库前 5 个有效提交者”之间需要防刷规则，否则单个高信用用户可能过度影响仲裁。

### 7.2 中优先级风险

1. 管理员“每月前 5 活跃用户自动轮换”需要排除处罚用户、异常刷分用户和当月无登录用户。
2. “原贡献者积分默认不调整”和“被重审推翻的数据积分待定或扣回”存在边界冲突。建议区分普通报错修正文案/标签与核心字段错误。
3. 套装 3 次相同入库需要去重约束，否则相同套装可能重复进入 `suits`。
4. 同一用户对同一 `category + game_id` 只能计分一次，但 `game_id = N` 或缺编号时无法形成稳定唯一键，需要单独规则。

### 7.3 低优先级风险

1. 高信用等级里的视觉权益较多，建议等核心审核闭环稳定后再实现。
2. 留言板建议点赞沿用投票权重可能导致高信用用户过度影响产品排期，建议仅用于排序参考。
3. 广告免除权依赖商业化方案，当前可以保留为待定，不宜阻塞审核主流程。

### 7.4 下一步建议

优先实现顺序建议：

1. 建立 `points_ledger`、积分汇总视图和 `clothing_contributions`，先保证积分和署名可审计。
2. 建立 `re_review_items`，承接缺套装但可入库的数据、字段冲突和其他重审状态。
3. 建立 `jury_votes`，把陪审团净胜票、权重和防自审固化到数据库。
4. 建立 `correction_requests`，把正式库报错从普通 pending 中拆开。
5. 最后实现管理员轮换、排行榜冻结和高信用视觉权益。
