# 数据库变更记录

## 0. 使用与轮转规则

本文档是数据库变更记录的常驻入口，保留完整历史索引、所有未结束事项和最近的完整业务链详情。

- 默认读取路径固定为“常驻索引 → 一个详情位置 → 一个目标章节”，不得自动读取全部归档。
- 常驻正文最多保留 10 条详细记录；达到边界时移动最旧的完整已结束业务组，不拆分同一业务上下文。
- 未完成 migration、待验证、待 rollback 或仍有清理门禁的记录不得归档。
- 后续 patch migration 在常驻文档新增记录并链接原历史卷，不修改旧记录。
- 新记录的模板、通用命令与 RPC / RLS 记录要求见[数据库开发安全方案](数据库开发安全方案.md#13-数据库变更记录维护)。

## 1. 当前环境与安全入口

- Supabase project ref、环境映射及非敏感连接事实见[环境信息](环境信息.md)。
- development 验证、production 备份、执行门禁和 rollback 要求见[数据库开发安全方案](数据库开发安全方案.md)。
- 本文档只记录已发生的变更与验证事实，不重复承担环境清单或操作手册职责。

## 2. 完整变更索引

| 日期 | change / migration | 环境 | 状态 | 详情位置 |
| --- | --- | --- | --- | --- |
| 2026-08-12 19:04 | `add_homepage_monthly_thanks` | development | 已应用；本地从零重放、development 回滚 fixture、权限、类型与 Advisor 回读通过；目标 PR #124 Preview 业务验收通过 | [DB-18 首页月度鸣谢](#2026-08-12-1904-db-18-首页月度鸣谢第一版) |
| 2026-08-10 20:33 | `reverse_overturned_contribution_points` | development | 已应用；本地 / development DB9/10/12/16/17 回滚 fixture、权限、类型、Advisor 与专用账号登录回读通过；验收数据已准备，待目标 PR 最新 Preview 业务验收 | [DB-17 原贡献积分扣回](#2026-08-10-2033-db-17-报错推翻原资料后的原贡献积分扣回) |
| 2026-08-04 21:37 | `add_level_benefits_v1` | development | 已应用；等级奖励、加权陪审、Lv2 候选门槛和分级只读 RPC 的本地 / development 回滚 fixture、权限、类型及 Advisor 回读通过；待目标 PR Preview 业务验收 | [DB-16 等级功能权益](#2026-08-04-2137-db-16-等级功能权益第一版) |
| 2026-08-03 19:48 | `db15_reject_expired_candidate_exclusion` | development | 已应用；过期有效期失败关闭，当前/历史排除与撤销回滚 fixture、权限、Security Advisor 及目标 PR 最新 Preview 第 4 项复验通过 | [DB-15 候选排除有效期补丁](#2026-08-03-1948-db-15-候选排除有效期与历史展示补丁) |
| 2026-07-31 08:00 | `db15_monthly_admin_rotation` / `db15_index_admin_foreign_keys` / `db15_harden_super_admin_helper` / `db15_expand_governance_audit` | development | 已应用；2 个旧普通管理员已转为过渡任期，fixture、权限、并发锁、Advisor、类型、真实登录角色回读及目标 PR 最新 Preview 业务验收通过 | [DB-15 普通管理员月度轮换](#2026-07-31-0800-普通管理员月度轮换第一版) |
| 2026-07-30 21:37 | `db14_create_last_month_leaderboard_snapshots` | development | 已应用；DB14 fixture、权限、定时任务、类型、schema、Advisor 与真实登录验收回读完成；已通过负责人业务验收 | [DB-14 上月榜冻结](#2026-07-30-2137-上月榜冻结) |
| 2026-07-29 23:17 | `db13_create_points_leaderboards` / `db13_bound_current_month_range` | development | 已应用；DB13 fixture、月末上界、权限、查询计划、类型、schema 和真实登录验收数据回读完成；Advisor 补丁后接口传输失败 | [DB-13 积分排行榜第一版](#2026-07-29-2317-积分排行榜第一版只读面) |
| 2026-07-29 12:08 | `db12_reopen_jury_for_new_correction_fields` | development | 已应用；DB12 回滚 fixture、权限、类型、schema 与 Advisor 回读完成 | [DB-12 投票中新报错字段退回补充](#2026-07-29-1208-投票中新报错字段退回补充) |
| 2026-07-29 11:32 | `require_correction_evidence_image` / `harden_correction_evidence_access` | development | 已应用；DB11 fixture、权限、类型、schema 与 Performance Advisor 回读完成；Security Advisor 接口传输失败 | [DB-11 图鉴图片凭证与权限加固](#2026-07-29-1132-报错图鉴图片凭证与权限加固) |
| 2026-07-28 23:08 | `route_corrections_directly_to_jury` | development | 已应用；自动验证完成，未验收业务场景已重新备数 | [报错直达陪审与验收稳定性补丁](#2026-07-28-2308-报错直达陪审与验收稳定性补丁) |
| 2026-07-28 11:39 | `db9_process_correction_requests` | development | 已应用；DB6～DB9 fixture、Advisor、类型、schema 与四组验收数据回读完成 | [DB-9 报错处理闭环](#2026-07-28-1139-正式图鉴报错处理转陪审与奖励) |
| 2026-07-28 10:01 | `db8_restrict_service_role_table_privileges` | development | 已应用；增强 fixture、权限、类型和 live catalog 回读完成 | [DB-8 报错受理](#2026-07-28-0934-正式图鉴报错受理与最小权限补丁) |
| 2026-07-28 09:34 | `db8_create_correction_request_intake` | development | 已应用；受理入口与最小权限 patch 均已收口 | [DB-8 报错受理](#2026-07-28-0934-正式图鉴报错受理与最小权限补丁) |
| 2026-07-27 23:05 | `db7_expand_jury_full_review_and_rewards` | development | 已应用并完成全字段审核、投票积分、兼容 fixture、Advisor 与生成物回读 | [DB-7 全字段补丁](#2026-07-27-2305-陪审团全字段审核与积分补丁) |
| 2026-07-27 20:53 | `db7_index_admin_user_fk` | development | 已应用并消除 DB-7 新增未索引外键告警 | [DB-7 常驻详情](#2026-07-27-2053-陪审团闭环db7_integrate_jury_voting--db7_index_admin_user_fk) |
| 2026-07-27 20:53 | `db7_integrate_jury_voting` | development | 已应用并完成规则、权限、事务、Advisor、生成物和前端回归 | [DB-7 常驻详情](#2026-07-27-2053-陪审团闭环db7_integrate_jury_voting--db7_index_admin_user_fk) |
| 2026-07-26 12:47 | `db6_integrate_missing_suit_review` | development | 已应用并完成结构、权限、事务、Advisor、生成物和前端回归 | [DB-6 常驻详情](#2026-07-26-0943---db6_create_re_review_items) |
| 2026-07-26 10:30 | `db6_restrict_candidate_insert_columns` | development | 已应用并完成最小权限、事务回滚和生成物验证 | [DB-6 常驻详情](#2026-07-26-0943---db6_create_re_review_items) |
| 2026-07-26 09:43 | `db6_create_re_review_items` | development | 已应用并完成权限、事务、本地全量重放和生成物验证 | [常驻详情](#2026-07-26-0943---db6_create_re_review_items) |
| 2026-07-22 20:30 | `db5_integrate_auto_entry` | development | 已应用、完成事务与并发验证，并通过 PR 合并前审计 | [常驻详情](#2026-07-22-2030---db5_integrate_auto_entry) |
| 2026-07-22 10:45 | `pr95_frontend_validation_seed` | development | 已完成并回读验证；待人工验收 | [常驻详情](#2026-07-22-1045---pr95_frontend_validation_seed) |
| 2026-07-21 17:52 | `db4_integrate_admin_arbitration` | development | 已完成并通过人工验收 | [常驻详情](#2026-07-21-1752---db4_integrate_admin_arbitration) |
| 2026-07-21 12:50 | `db3_integrate_existing_field_completion` | development | 已完成并通过人工验收 | [常驻详情](#2026-07-21-1250---db3_integrate_existing_field_completion) |
| 2026-07-17 13:18 | `db2_create_read_surfaces` | development | 已完成并验证 | [常驻详情](#2026-07-17-1318---db2_create_read_surfaces) |
| 2026-07-17 08:06 | `db1_merge_and_development_recheck` | develop / development | 已完成复查 | [常驻详情](#2026-07-17-0806---db1_merge_and_development_recheck) |
| 2026-07-16 22:35 | `db1_create_contributions_points_base_facts` | development | 已完成并验证 | [常驻详情](#2026-07-16-2235---db1_create_contributions_points_base_facts) |
| 2026-07-16 13:33 | `db0_merge_and_development_smoke` | develop / development | 已完成冒烟 | [常驻详情](#2026-07-16-1333---db0_merge_and_development_smoke) |
| 2026-07-15 19:27 | `db0_development_business_validation_seed` | development | 已完成；测试事实保留 | [常驻详情](#2026-07-15-1927---db0_development_business_validation_seed) |
| 2026-07-14 14:28 | `db0_secure_pending_clothes_access` | development | 已完成并验证 | [常驻详情](#2026-07-14-1428---db0_secure_pending_clothes_access) |
| 2026-07-12 10:56 | `seal100x_production_red_banquet_score_update` | production | 已完成并复查 | [归档-002](../archive/database/数据库变更记录-归档-002.md#2026-07-12-1056---seal100x_production_red_banquet_score_update) |
| 2026-07-12 10:39 | `seal100x_production_custom_tags_update` | production | 已完成并复查 | [归档-002](../archive/database/数据库变更记录-归档-002.md#2026-07-12-1039---seal100x_production_custom_tags_update) |
| 2026-07-12 10:24 | `seal100x_production_insert_partial_handoff` | production | 已停止；部分完成并完成交接 | [归档-002](../archive/database/数据库变更记录-归档-002.md#2026-07-12-1024---seal100x_production_insert_partial_handoff) |
| 2026-07-11 23:34 | `seal100x_production_system_user_scores_stars_update` | production | 已完成并复查 | [归档-002](../archive/database/数据库变更记录-归档-002.md#2026-07-11-2334---seal100x_production_system_user_scores_stars_update) |

| 2026-07-11 23:20 | `seal100x_production_numeric_scores_matrix_update` | production | 已完成并复查 | [归档-002](../archive/database/数据库变更记录-归档-002.md#2026-07-11-2320---seal100x_production_numeric_scores_matrix_update) |
| 2026-07-11 22:55 | `seal100x_development_business_pilot` | development | 已完成并验证 | [归档-002](../archive/database/数据库变更记录-归档-002.md#2026-07-11-2255---seal100x_development_business_pilot) |
| 2026-07-06 23:01 | `pr17_contributions_points_schema_audit` | 只读审计 | 已完成；未执行数据库 | [常驻详情](#2026-07-06-2301---pr17_contributions_points_schema_audit) |
| 2026-06-29 | `production_pending_batch_20260628` | production | 已完成；8 条转人工复核 | [归档-001](../archive/database/数据库变更记录-归档-001.md#2026-06-29---production_pending_batch_20260628) |
| 2026-06-27 17:20 | `update_profile_username_rpc` | development | 已完成并验证 | [归档-001](../archive/database/数据库变更记录-归档-001.md#2026-06-27-1720---update_profile_username_rpc) |
| 2026-06-27 16:50 | `add_profile_role_level` | development | 已完成并验证 | [归档-001](../archive/database/数据库变更记录-归档-001.md#2026-06-27-1650---add_profile_role_level) |
| 2026-06-27 14:30 | `sync_approved_clothes_to_submitter_wardrobes` | development | 已完成并验证 | [归档-001](../archive/database/数据库变更记录-归档-001.md#2026-06-27-1430---sync_approved_clothes_to_submitter_wardrobes) |
| 2026-06-27 09:30 | `require_numeric_game_id` | development | 已完成并验证 | [归档-001](../archive/database/数据库变更记录-归档-001.md#2026-06-27-0930---require_numeric_game_id) |
| 2026-06-25 22:20 | `apply_existing_migrations_to_development` | development | 已完成并验证 | [归档-001](../archive/database/数据库变更记录-归档-001.md#2026-06-25-2220---apply_existing_migrations_to_development) |
| 2026-06-25 22:07 | `dev_environment_connection_verified` | development | 已完成连接验证 | [归档-001](../archive/database/数据库变更记录-归档-001.md#2026-06-25-2207---dev_environment_connection_verified) |

## 3. 常驻详细记录

## 2026-08-12 19:04 DB-18 首页月度鸣谢第一版

| 项目 | 结果 |
| --- | --- |
| migration | `20260812110345_add_homepage_monthly_thanks.sql`；远端登记版本 `20260812110345`；SHA-256 `C91F68C4891608A2CA7AC9A99A701A4E7EF73B92698994B6ACD506E915C844DB` |
| fixture | `db18_homepage_monthly_thanks.sql`；SHA-256 `9EBB8EEC8B9719AA8B87DA404D278146E4D55A7E13B4473233AECC404CA8958B` |
| 环境与影响 | 固定 development `tfwejruvdahonacyldrg`；新增一个私有只读函数和一个公开只读视图，不新增业务表、不回填或修改积分、榜单与候选快照；production 未操作 |
| 业务规则 | 最近冻结月中至少 5 条排除 `level_bonus` 的正向有效行为用户，沿用已冻结候选顺序取最多 10 人；公开面只含月份、顺序和当前公开名称，注销账号跳过，无名单时首页隐藏 |

- 权限与隐私：`private_db2.homepage_monthly_thanks()` 为 `SECURITY DEFINER`、owner `postgres`、空 `search_path`，只向 `anon / authenticated` 开放执行；`public.homepage_monthly_thanks` 只向同两角色开放 SELECT，`service_role` 和客户端无写权限。公开列严格为 `month_start / display_order / display_name`，不返回用户 ID、积分、行为数、邮箱或管理员资格。
- 验证：本地 PostgreSQL 17 从零重放全部 migration 成功；本地与 development 回滚 fixture 均通过，覆盖严格 10 人、稳定顺序、当前名称、不可逆化名、注销账号与不足 5 次排除、匿名读取、私有底表拒绝和回滚零残留。development 类型已生成；目标对象未命中 Security / Performance Advisor。
- 当前数据：2026 年 7 月冻结候选共 12 人，但符合至少 5 条有效行为者为 0，因此 live 公开视图返回 0 行，页面按规则不渲染空鸣谢模块；没有为视觉验收伪造或改写候选审计事实。
- 业务验收：负责人于 2026-08-12 19:28（北京时间）确认 PR #124 提交 `5cd455bafc13045d447e778426055af3c56d1622` 对应 Preview 通过；真实 0 名单隐藏状态、桌面 / 390px populated 视觉证据和 reduced-motion 静态状态均无待修复项。其后的文档收口不改变用户可见代码或数据库行为，无需重复页面验收。
- rollback：若需停用，新增 patch 先撤销公开视图 SELECT 与私有函数 EXECUTE，再删除 `public.homepage_monthly_thanks` 视图和 `private_db2.homepage_monthly_thanks()` 函数；没有新增表或业务数据需要恢复。production 未应用，无 production rollback。

## 2026-08-10 20:33 DB-17 报错推翻原资料后的原贡献积分扣回

| 项目 | 结果 |
| --- | --- |
| migration | `20260810123006_reverse_overturned_contribution_points.sql`；远端登记版本 `20260810123006`；SHA-256 `642D0FD5C3DFE8D97EEB319834F913239A054617E2CED53FD744BA05B09C89B5` |
| fixture | `db17_correction_point_reversals.sql`；SHA-256 `FC66802ADAE5B97A024E962C1308877B0ACB2FAF3870C33BA313E0D4194C5ADD` |
| 环境与影响 | 固定 development `tfwejruvdahonacyldrg`；新增 nullable 审计关联、约束、部分索引和内部触发器函数，不回填历史，不修改既有积分、报错、贡献或正式资料；production 未操作 |
| 业务规则 | 只有最终批准的报错以不同采纳值替换非空旧值时，才按来源 pending 的被修正字段快照精确扣回命中的原贡献基础积分及关联等级奖励；其他字段贡献、投票、重审、报错奖励和公开贡献归属不变 |

- 结构与事务：`points_ledger.reversal_correction_request_id` 关联批准报错并限制为负数 `reversal`；`correction_requests_reverse_overturned_points` 与正式资料批准同事务运行，`reversal_of` 唯一约束和冲突忽略保证重复状态重放不重复扣回。
- 验证：本地 PostgreSQL 17 从零重放全部 migration 成功；本地和 development 的 DB9、DB10、DB12、DB16、DB17 均通过并 rollback。DB17 覆盖两名旧值贡献者全额 `-10 / -10`、其中一人的等级奖励 `-2`、空字段补全不扣回、无关字段 `+5` 保留、原正向流水保留、状态重试幂等和积分状态重算一致。
- 权限与回读：目标列、外键、检查、部分索引和触发器均由 live catalog 确认；两个内部函数为 `SECURITY DEFINER`、空 `search_path`，public / anon / authenticated / service_role 均无执行权，客户端仍无 `points_ledger` INSERT。回滚 fixture 后目标用户、pending、服装、报错和积分残留均为 0；应用后、验收备数前批准报错与关联扣回均为 0。
- 人工验收数据：批次 `DB17-PR123-20260810-A` 在 development 建立 1 个专用普通账号、2 条 pending、2 件测试服装、2 条贡献和 1 条已批准报错。账号先以 `+500` 达到 Lv1，再产生原贡献 `+10` 与等级奖励 `+1`；非空旧名称被推翻后追加 `-10 / -1`，权威总分回到 500。密码登录与 `get_my_level_benefits()` 已实测返回最近流水 `-1、-10、+10、+1、+500`；密码仅保存在 Git 忽略的本地验收清单。验收后默认保留积分、贡献和报错审计事实，如需清理登录能力只精确停用专用账号，不级联删除审计记录。
- Advisor：本任务对象未命中 Security 或 Performance Advisor；项目现有 48 条安全、35 条性能提示均为存量范围，本补丁未新增目标告警。参考：[Database Linter](https://supabase.com/docs/guides/database/database-linter)。
- rollback：如 development 需要停用，必须新增前向 patch 删除目标触发器、撤销两个内部函数的本次定义并移除仅在确认无引用时可安全移除的新增列 / 约束 / 索引；已经形成的积分扣回属于不可删除审计事实，默认仅停用后续触发，不删除或改写流水。production 未应用，无 production rollback。

## 2026-08-04 21:37 DB-16 等级功能权益第一版

| 项目 | 结果 |
| --- | --- |
| migration | `20260804133742_add_level_benefits_v1.sql`；远端登记版本 `20260804133742` |
| 目标环境 | 固定 development project `tfwejruvdahonacyldrg`；production / `main` 未操作 |
| 文件校验 | migration SHA-256 `B50CB9109E5DCB18F8BBA54FE9259F46DDFE4B4105B6C4FCCC5AC1298F8A4A7A`；fixture SHA-256 `9ADE255A7E86057A2DF049F20DEEFDF992CDC3154EB6F59BE1151E0B3C262252` |
| 持久数据影响 | migration 只从原有 29 条积分流水重建 17 条私有并发状态，17 条历史票统一冻结为 Lv0 / 1 票且未追补历史等级奖励；其后为人工验收单独新增 5 个专用用户、4 组积分载体与 4 条基础流水 |

- 积分：入库、补全、重审修正和报错采纳按事件发生前 Lv1～Lv4 追加唯一 `+1 / +2 / +3 / +5` 奖励流水；陪审 `+1` 不叠加。基础流水扣回时同步追加奖励负数流水；私有 `user_points_state` 只负责并发串行，可由权威流水重建。
- 陪审：投票冻结等级与 1 / 2 / 3 票权；通过仍要求至少 5 位不同赞成用户并加权领先，退回仍要求至少 3 位不同反对用户且加权反对差达到 3。Lv2 以上首次投票可附 200 字内不可修改复核意见。
- 管理员与数据：月度候选增加冻结等级，Lv2 及以上才具备候选资格，`level_bonus` 不重复计为有效行为；`get_my_level_benefits()` 按 Lv0～Lv4 返回本人流水、本人贡献 / 投票及匿名统计，不开放底表、他人身份或私密证据。
- 公开面：贡献者结果增加当前等级但保留原时间 / 名次顺序；三个排行榜增加当前累计等级，不改变各榜积分与名次口径；超级管理员治理读模型增加候选冻结等级。
- 验证：本地从零重放全部 migration 成功；DB-16、DB-7、DB-9、DB-13、DB-14、DB-15 关键 fixture 均通过并 rollback。development DB-16 fixture 覆盖等级边界、奖励幂等 / 扣回、加权票、三人退回、复核意见权限、Lv2 候选和分级数据拒绝，回读 fixture 残留为 0。DB-15 完整 development fixture 因既有专用验收 pending 占用原固定编号而未重复写入，等价的 Lv1 / Lv2 候选边界已由 DB-16 隔离 fixture 覆盖。
- 人工验收数据：批次 `DB16-PR122-20260804-A` 在 development 新建 5 个普通专用用户，公开名称为“等级验收 Lv0”至“等级验收 Lv4”，累计积分分别为 `0 / 500 / 2000 / 5000 / 10000`。Lv1～Lv4 使用 pending `816400001～816400004`、对应 `db16_acceptance_level_*_20260804` 服装和贡献建立 4 条基础流水；所有事件发生前均为 Lv0，因此额外奖励流水为 0，避免验收种子本身改变目标门槛。五组密码登录和 `get_my_level_benefits()` 分级返回均已实测通过；密码只保存在 Git 忽略的本地验收清单。回读 development 当前共 33 条积分流水、合计 17628 分，其中本批次 4 条、合计 17500 分；5 个账号、4 条 pending、4 件服装和 4 条贡献保留至业务验收，清理需精确授权且不得级联删除审计事实。
- 权限与 Advisor：新增特权函数均为空 `search_path`；anon 无等级 RPC / 投票 RPC 权限，authenticated 无积分、投票和私有状态底表权限。Security Advisor 对私有无 policy 状态表报告预期 INFO，对受控 authenticated `SECURITY DEFINER` RPC 报告预期 WARN；本次未新增 ERROR，既有 `stages` / `suits` RLS ERROR 不在范围。Performance Advisor 未报告本次新增问题。
- 执行异常：Supabase CLI dry-run 因直连 TLS EOF 未取得结果，切换一次到管理接口；首次管理接口调用因本地 SQL 读取结果被工具截断而在解析阶段原子失败，对象回读确认零应用后，以完整分块 SQL 成功应用，未盲目重复写入。
- Rollback：已应用 migration 不改写。若需停用，新增 patch 撤销新 RPC 的客户端执行权、恢复 1 票计票函数和旧候选函数；积分奖励审计与历史票快照保留，必要扣回只追加负数流水。

## 2026-08-03 19:48 DB-15 候选排除有效期与历史展示补丁

| 项目 | 结果 |
| --- | --- |
| migration | `20260803114823_db15_reject_expired_candidate_exclusion.sql` |
| 目标环境 | 固定 development project `tfwejruvdahonacyldrg`；production / `main` 不在范围内 |
| 文件校验 | SHA-256 `385F98A6...AA6526` |
| development 状态 | migration 已登记；原过期验收记录保留为审计历史，没有删除或改写业务事实 |

- 原因：验收时结束时间比提交时间早约一分钟；旧 RPC 只比较起止先后，允许写入已过期记录，旧前端又静默过滤过期记录，导致提交后无列表和撤销入口。
- 修复：`create_admin_candidate_exclusion(...)` 在写入前拒绝结束时间不晚于当前时间；特权函数继续使用空 `search_path`，仅 `authenticated / service_role` 可执行，函数内继续复核超级管理员身份。
- 前端：候选排除分为“当前及待生效”和“排除历史”，明确显示生效中、待生效、已过期、已撤销或时间异常；只有尚未结束且未撤销的记录显示撤销入口。
- development fixture：事务内验证过期区间失败关闭且不新增审计行、有效排除可在治理 RPC 中读取并撤销、撤销后仍保留历史；fixture 全部 rollback，残留为 0。
- Advisor：本补丁未新增表、RLS 或可调用函数；Security Advisor 无本补丁新增 ERROR。`admin_candidate_exclusions` 的 RPC-only 强制 RLS INFO 与受控 `SECURITY DEFINER` WARN 延续原设计。
- Rollback：使用后续 patch 恢复旧函数定义即可；前端历史展示可随代码回退，已存在的任期、排除与审核审计事实继续保留。

## 2026-07-31 08:00 普通管理员月度轮换第一版

| 项目 | 结果 |
| --- | --- |
| migrations | `20260730234625_db15_monthly_admin_rotation.sql`、`20260730234920_db15_index_admin_foreign_keys.sql`、`20260730235310_db15_harden_super_admin_helper.sql`、`20260731000250_db15_expand_governance_audit.sql` |
| 目标环境 | 固定 development project `tfwejruvdahonacyldrg`；production / `main` 不在范围内 |
| 文件校验 | 主 migration `2A9D6A8B...B48EDD`；外键索引补丁 `B4C9C6F6...5AD239`；helper 安全补丁 `E0525319...AFAE`；治理审计补丁 `1FA8388A...C0D75`；fixture `A3870397...F222` |
| development 状态 | 四个 migration 均已登记；2 个旧普通管理员精确转换为 31 天 `legacy_transition`，旧普通角色为 0，3 个超级管理员未改变；部署当月月度自动任期为 0 |

- 数据对象：新增任期、候选排除、月初候选快照、审核决定与来源审计；普通管理员能力只来自当前有效 `admin_terms`，`profiles` 迁移后禁止再写普通管理员角色值。
- 轮换：北京时间每月 1 日 00:10 在 DB-14 上月榜冻结后运行；候选至少有 5 次上月正向有效积分行为，按积分、行为数、最后行为时间和用户 ID 固定排序，最多五席且不降门槛。缺榜失败关闭，重复执行幂等，退出/撤销/新增排除自动补位。
- 权限：普通管理员不再拥有 `pending_clothes` 整表读取或状态直写，只能调用低风险候选和决定 RPC；高风险、套装、正式库补全、重审、报错和永久驳回继续由超级管理员处理。新增特权函数均为空 `search_path`，新表强制 RLS 且客户端无直写。
- 审核：数据库重新校验正式库不存在、五位不同用户多数、无最高票并列、无套装语义和审核者未参与。通过只处理多数 pending 并复用贡献/积分/衣柜原子链；驳回强制原因、少数意见保留、提交人可查看原因并重新提交。
- development fixture：覆盖五席、有效行为门槛、完全并列排序、超级/手动/排除跳过、少于五人保留空席、空缺补位、幂等、缺榜失败关闭、任期到期和手动 31 天上限、五人多数含少数意见、自审/不足/正式库已有隐藏、通过幂等、附原因驳回、贡献积分衣柜写回和客户端最小权限；所有数据回滚，DB15 测试用户、pending 和 clothes 回读均为 0。
- 并发与权限：独立连接持有轮换 advisory lock 时，补位调用等待锁释放后返回且没有重复授予；真实登录回读确认 super 可治理和低风险审核、monthly 为月度受限能力、manual 自审候选减少一组、普通用户无管理能力。旧过渡管理员只能读取自己的 pending，低风险 RPC 可用，高风险 helper 为 false。
- Advisor：外键索引补丁消除 7 个 DB15 新增未索引外键提示；剩余新索引未使用 INFO 属刚创建对象预期。安全侧无 DB15 ERROR；RPC-only 强制 RLS 表无 policy INFO 与 authenticated 可调用受控 `SECURITY DEFINER` WARN 为预期设计，函数内身份/任期/自审/行锁校验和精确 grants 已回读。参考 [RLS no-policy 说明](https://supabase.com/docs/guides/database/database-linter?lint=0008_rls_enabled_no_policy) 与 [SECURITY DEFINER RPC 说明](https://supabase.com/docs/guides/database/database-linter?lint=0029_authenticated_security_definer_function_executable)。
- 生成物：development TypeScript types 已更新；远程 `pg_dump` 在登录角色初始化后 EOF 并截断本地文件，已立即恢复原快照。随后通过系统目录、对象定义、权限、RLS、函数配置与 migration history 完成 schema 回读，未把不确定 dump 写入仓库。
- 验收数据：新建 4 个可登录角色账号及 5 个支持账号，准备多数通过、多数驳回和自审冲突三组专用 pending；登录和 RPC 能力实测通过。密码只存在 Git 忽略的 `tmp/验收清单.md`，不进入仓库或回传。
- 前端：按能力 RPC 决定入口；普通管理员只显示新增服装多数审核，超级管理员保留完整管理并增加任期、排除、候选快照和审核审计；个人中心显示任期、退出、自动授予提示和可重提驳回原因。
- Rollback：新增 patch migration 停止 `db15-rotate-monthly-admins`、撤销新增能力 RPC 的 authenticated execute、移除 `profiles_no_ordinary_admin_role_check` 并恢复旧权限判断；保留任期、排除、候选和审核审计。若恢复旧管理员，只按 `source = 'legacy_transition'` 精确回写，不删除历史。

## 2026-07-30 21:37 - 上月榜冻结

### 基本信息

| 项目 | 内容 |
| --- | --- |
| 目标环境 | Supabase development `tfwejruvdahonacyldrg` |
| migration | `20260730125038_db14_create_last_month_leaderboard_snapshots.sql`；远端登记版本同为 `20260730125038` |
| migration SHA-256 | `6647C574FD610E540D9D609840D67FD012D7F10D63E890A69CBF55FFD26B1A79` |
| fixture | `db14_last_month_leaderboard.sql`，SHA-256 `659C72BEF524A3ED7EC440773D0A2F685AE4C4071C83604D0A2D13C9E8B6D1FE` |
| 业务边界 | 冻结上一完整北京时间自然月的积分与 dense rank 名次；名称与注销状态实时读取；仅登录用户可见；不授予排行榜权益 |
| production | 未操作 |

### 应用、权限与事务验证

- 应用前再次通过固定脚本确认 development project ref；远端只读预检确认 DB14 migration、两张私有表、三个 helper、public view 和 `pg_cron` 均不存在，上一个完整自然月的 `awarded` 积分用户数为 0。
- Supabase CLI 临时登录与直连连续返回 EOF，未在不确定状态下重试写入；改用已登录 Dashboard，在同一事务内执行原 migration 并登记 `20260730125038`。回读确认登记只有 1 条 statement、`pg_cron` 位于 `pg_catalog`、定时任务唯一且 active，计划为 UTC `5 16 * * *`，即北京时间每日 00:05，由入口函数只在每月 1 日生效。
- 首次补冻生成 `2026-06-01` 空月标记，`row_count = 0`；没有写入、修改或删除 `points_ledger`。冻结标记能区分“尚未冻结”和“已冻结但无人上榜”。
- 两张私有表均启用并强制 RLS，anon、authenticated、service_role 均无底表读取权限；冻结函数只允许 postgres，authenticated 仅能执行只读 helper 并 SELECT `public.points_leaderboard_last_month`，anon 无读取或执行权限；三个 helper 的 `search_path` 均为空。
- development fixture 覆盖自然月校验、只允许已结束月份、首次冻结、重复冻结幂等、迟到流水不追改、当月流水隔离、同分 dense rank、改名实时显示、注销显示、当前用户标记、匿名拒绝、底表拒绝、cron 定义和故障回滚，最终返回 `passed` 且 fixture 身份残留为 0。

### development 真实登录验收数据

- 因上一完整自然月真实积分为空，仅在 DB14 派生快照中为既有专用用户 `DB13验收甲`、`DB13验收乙` 保留两条各 10 分、并列第 1 的 development 验收行；月标记 `row_count` 与快照行数均为 2，不新增或改写积分流水、投票或真实账号事实。
- 使用轮换后的专用测试密码完成真实登录与 Data API 页面读取；桌面和 390px 窄屏均显示“总榜 / 当月榜 / 上月榜”，两名用户同分并列，`DB13验收甲` 显示“这是你”，页面无横向溢出、控制台错误或范围外权益。
- 最终测试密码只保存在 Git 忽略的 `tmp/验收清单.md`。两条派生快照暂留 development 便于负责人复验；后续清理需按月份和两名专用用户精确授权。
- 负责人已完成业务验收；根据验收反馈，页面说明改为“这里展示上个月最终确定的积分和排名；积分相同的玩家会并列显示”，避免向普通用户展示“完整自然月、冻结”等技术口径。

### 生成物、Advisor 与 rollback

- development 类型已重新生成，`src/types/supabase.ts` 新增 `points_leaderboard_last_month`；远端 dump 在连接中断时失败，本地 `supabase/schema.sql` 先从当前分支基线精确恢复，再按 live catalog 回读同步新增 public view 与 authenticated SELECT 契约。
- Security Advisor 当前 2 个 ERROR 仍是既有 `public.stages` / `public.suits` 未启用 RLS，现有可见 WARN 均不涉及 DB14；Performance Advisor 为 0 ERROR，现有 WARN 同样无 DB14 对象。本次没有新增 Advisor ERROR / WARN。
- rollback 先按精确名称取消 `db14-freeze-previous-month-leaderboard`，再删除 DB14 public view、三个 helper 和两张私有表；保留 `pg_cron` 扩展，不删除或改写积分流水。已应用 migration 若需修正，使用新的 forward patch，不改写本文件。

## 2026-07-29 23:17 - 积分排行榜第一版只读面

### 基本信息

| 项目 | 内容 |
| --- | --- |
| 目标环境 | Supabase development `tfwejruvdahonacyldrg` |
| migration | 本地 `20260729150109_db13_create_points_leaderboards.sql` / `20260729154641_db13_bound_current_month_range.sql`；远端版本 `20260729151721_db13_create_points_leaderboards` / `20260729154839_db13_bound_current_month_range` |
| migration SHA-256 | 主 migration `BAD95DD92D68D4A9AF777CDE4C5BEFCC4DCC4B7D05B95504C2BEE581277F2431`；月末上界 patch `8C6B9AFCBE07D72A4D206302887DADA68218BC57654E950EF6E775CC2AC1CFE3` |
| fixture | `db13_points_leaderboards.sql`，SHA-256 `F80E1B40154A893DD2DDB0155B9303D7CA9D8BCD6284863D6FFD04FFD84A629B` |
| 业务边界 | 仅总榜与当月榜；登录后可见、同分同名次、北京时间自然月；不上月冻结、不发放排行榜权益 |
| production | 未操作 |

### 应用、权限与事务验证

- 应用前确认 development project ref、远端迁移基线、22 条既有积分流水及四个目标对象均不存在；相同 DDL 先在 development 事务内完整执行并 `ROLLBACK`，回读确认无对象残留后，再应用唯一一条 DB-13 migration。
- 新增 `public.points_leaderboard_total` 与 `public.points_leaderboard_current_month` 两个 `security_invoker + security_barrier` 只读面，公开字段仅为名次、名称、积分和当前用户标记；两个私有 helper 均为 `SECURITY DEFINER`、空 `search_path` 且只授予 authenticated 执行。
- development fixture 覆盖匿名拒绝、登录只读、底表继续拒绝、无会话零行、总榜与北京时间当月榜、同分 dense rank、不可逆化名、当前用户标记，以及上月与下月未来积分隔离，并以 `ROLLBACK` 结束。
- 首次 fixture 的绝对名次断言受到既有积分正常占位影响，已改为按全榜实际分数独立验证 dense rank；修正后本地与 development 均通过。最终测试用户、服装和积分残留均为 0，原有积分流水仍为 22 条。
- 审阅发现主 migration 只有月初下界，未来时间流水会误入本月；已使用新的 patch migration 增加严格下月上界。patch 应用调用返回 transport error 后没有盲目重试写入，远端 migration history、函数定义和增强 fixture 共同证明版本 `20260729154839` 已生效。

### development 真实登录验收数据

- 通过公开注册入口形成两名 DB-13 专用 Auth 测试用户；网络在响应阶段中断时均先回读 `auth.users` 确认远端事实，没有盲目重复创建。两名 profile 分别显示为 `DB13验收甲`、`DB13验收乙`。
- development 保留 1 条测试服装、2 条 approved pending、2 条贡献和 2 条各 `+10` 的当月积分流水；两名用户在总榜与当月榜均为 10 分并列，现有积分流水总数从 22 增至 24。
- 使用 `DB13验收甲` 完成真实密码登录并通过 Data API 读取两个 public view：总榜与当月榜各返回两名测试用户、同一名次，且每个榜单只有 `DB13验收甲` 的 `is_current_user = true`。
- 登录账号与随机密码只保存在 Git 忽略的 `tmp/验收清单.md`，未写入仓库、PR、数据库记录或聊天。验收完成后的账号和关联审计事实清理需要按精确对象单独授权。

### 查询计划、生成物与风险

- development `EXPLAIN` 对带严格月末上界的当月汇总返回低成本 `Seq Scan + HashAggregate`：底表当前 24 行，扫描成本 `2.88`、总成本 `3.10`。当前规模不支持新增专用索引或缓存，后续仅在实际增长和查询计划证明需要时再评估。
- development 类型已重新生成，`src/types/supabase.ts` 只新增两个排行榜 view；`supabase/schema.sql` 与 `docs/database/schema.md` 已同步两个 public view、权限和安全边界。
- migration 应用前 Security Advisor 仍报告 `stages`、`suits` 未启用 RLS，以及其他既有函数 / policy 告警；均非 DB-13 新增。补丁后的 Security / Performance Advisor 接口与 CLI 均多次返回 transport error，未将 catalog 和 `EXPLAIN` 回读冒充 Advisor 通过。
- rollback 使用新的 patch migration 先撤销两个 public view 的 SELECT 并删除 view，再撤销并删除两个私有 helper；不删除、不改写 `points_ledger` 或当前 24 条流水。production 未应用，无 production rollback。

## 2026-07-29 12:08 - 投票中新报错字段退回补充

### 基本信息

| 项目 | 内容 |
| --- | --- |
| 目标环境 | Supabase development `tfwejruvdahonacyldrg` |
| migration | 本地 `20260729115404_db12_reopen_jury_for_new_correction_fields.sql`；远端版本 `20260729120823_db12_reopen_jury_for_new_correction_fields` |
| migration SHA-256 | `910FE782796E7B680819F0C4764EB7C0A132AC3969248AE890692BCF339FCF0D` |
| fixture | `db12_reopen_jury_for_new_correction_fields.sql`，SHA-256 `F38D6C69F5BA2A924249DD69EE5DFED76B9F53588D603351D71B6684A8723817` |
| 业务边界 | 同一服装正在投票时若收到新的报错字段，复用原审核项并退回补充；不回填现有数据，不修改正式资料 |
| production | 未操作 |

### 应用与事务验证

- 应用前确认 development project ref、两个授权文件哈希和远端 migration 列表；仅应用 DB12，远端登记为 `20260729120823_db12_reopen_jury_for_new_correction_fields`。
- 路由函数现在会把投票中的当前候选标记为 `returned`、将审核项恢复为 `pending`，并合并原有问题与新报错字段；同一服装仍只有一个活动事项。旧投票和陪审 `+1` 积分保留，正式服装不变。
- DB12 fixture 在 development 返回 `db12_reopen_jury_for_new_correction_fields_passed` 并以 `ROLLBACK` 结束，覆盖旧 `missing_suit` 事项、投票中新增长星级报错、问题字段合并、候选退回、票与积分保留、证据路径、相同重试幂等、唯一活动事项和新补充者队列可见性。
- 回滚后 fixture 账号、服装、pending、审核项和 Storage 图片均为 0。既有短编号 `9900002` 仍为原 `missing_suit / voting` 状态且保留一份投票候选，证明 migration 未改写现有业务数据；用户重新提交新字段时才触发退回补充。

### 权限、Advisor、类型与 rollback

- live catalog 确认目标函数返回 `uuid`、`SECURITY DEFINER`、空 `search_path`；新逻辑存在、旧阻塞错误不存在，`PUBLIC` / `anon` / `authenticated` / `service_role` 均无直接执行权。
- Performance 与 Security Advisor 均成功回读；目标内部路由函数未被列为新增问题。返回的未索引旧外键、旧 RLS policy、旧 public 表和公开业务 RPC 提示均为既有项目事项，不在本补丁范围。
- `npm run db:types:safe` 通过并确认连接 development；函数签名未变化，`src/types/supabase.ts` 无 diff，文件为 33,014 字节，SHA-256 `DB1A5ADC2AF4358F239D3C2D96BE35697B27839EA4B441E41E73E04D381D03F0`。
- 已应用 migration 如需修正，只能新增前向 patch，不改写远端 history 或本地已授权文件。production 未操作，因此无 production rollback 动作。

## 2026-07-29 11:32 - 报错图鉴图片凭证与权限加固

### 基本信息

| 项目 | 内容 |
| --- | --- |
| 目标环境 | Supabase development `tfwejruvdahonacyldrg` |
| migration 1 | `20260729111217_require_correction_evidence_image.sql`，SHA-256 `228AD95108F888C24C29037581D8A3482226A39481CECA579E18331608E545BD` |
| migration 2 | `20260729112846_harden_correction_evidence_access.sql`，SHA-256 `4B914D6BE359901BA8FF869D9386892EA6D3C5D81C2324667ECF43D1E7999A7F` |
| fixture | `db11_correction_evidence_image.sql`，更新版 SHA-256 `01A2373606B8C3F7C9D94447BEF45F681BB240ADEFF5A1AA83162787C706A3DA` |
| 业务边界 | 新报错必须绑定本人上传的游戏内图鉴图片；图片只对报错者和有资格参与对应事项的陪审员开放 |
| 数据影响 | 新增 nullable 证据路径，不回填或修改现有业务记录；fixture 全部事务回滚 |
| production | 未操作 |

### 应用、权限与回读

- 两次应用前均通过 development project-ref 与授权 SHA-256 检查；只执行各自授权的单个 migration。Supabase 管理接口分别登记远端版本 `20260729112718_require_correction_evidence_image` 和 `20260729113213_harden_correction_evidence_access`，本地保留负责人授权的原始文件名和哈希，不改写已应用文件。
- 第一份 migration 创建私有 `correction-evidence` bucket、`evidence_image_path` 路径约束与唯一部分索引、三条 Storage policy、带图片提交 RPC 和带证据陪审队列 RPC。第二份前向补丁撤销 authenticated 对旧报错 RPC 的执行权，并把两项 Storage 权限判断移入非公开 `private` schema。
- 更新版 DB11 fixture 在 development 返回 `passed` 并以 `ROLLBACK` 结束，覆盖匿名拒绝、非本人目录拒绝、缺图片拒绝、MIME 校验、相同重试幂等、图片不可替换、报错者读取、合资格陪审员读取、无资格用户拒绝、已绑定图片不可删除、未绑定本人图片可删除及旧 RPC 绕过关闭；回读确认 fixture 无残留。
- live catalog 确认 bucket 为 private、上限 8 MB、MIME 白名单为 JPEG / PNG / WebP；目标列、约束、索引、policy、函数 owner、空 `search_path` 和 execute grants 均符合 migration。旧记录继续允许 `evidence_image_path is null`，未改写业务数据。

### Advisor、类型与 rollback

- Performance Advisor 成功，未发现 DB-11 新增缺失索引或查询计划问题；既有 `pending_clothes.suit_id`、`pending_suits.submitted_by`、旧 policy 和未使用索引提示不在本补丁范围。
- Security Advisor 在加固后连续返回远端传输错误，未取得最终 Advisor 列表；该异常与 SQL 执行、fixture 和 catalog 回读分离记录。已通过权限与 schema 回读确认 public helper 已移除、private helper 存在、旧 RPC 不再授权 authenticated、新 RPC 保持最小授权，但不把这些结果表述为 Security Advisor 通过。
- `npm run db:types:safe` 通过并确认连接 development；`src/types/supabase.ts` 为 33,014 字节，SHA-256 `DB1A5ADC2AF4358F239D3C2D96BE35697B27839EA4B441E41E73E04D381D03F0`，包含证据字段和两个新 RPC。
- 已应用 migration 不修改、不回退历史；后续调整只能新增前向补丁。production 未操作，因此无 production rollback 动作。

## 2026-07-28 23:08 - 报错直达陪审与验收稳定性补丁

### 基本信息

| 项目 | 内容 |
| --- | --- |
| 目标环境 | Supabase development `tfwejruvdahonacyldrg` |
| migration | `20260728135701_route_corrections_directly_to_jury.sql` |
| migration SHA-256 | `E28CA704850FF9EF7E3D2B3BC1D8B0E53A59DBC7B437EB83CDF281F3EAF10909` |
| 业务边界 | 新报错选择明确字段后直接进入同一服装唯一的社区陪审事项；管理员队列只兼容遗留记录；不直接修改正式资料 |
| 数据影响 | migration 不回填或修改业务数据；全部 fixture 事务回滚 |
| production / 网页 | 均未操作 |

### 前检、应用与异常收口

- development link、授权文件 SHA-256 和 dry-run 均通过；远端原末项为 `20260728025707_db9_process_correction_requests`，只计划应用本 migration，目标路由函数原先不存在。本机隔离 PostgreSQL 17 已先应用同一文件并通过 DB6～DB10 fixture。
- CLI apply 两次在连接阶段返回 `unexpected EOF`；每次均先用 dry-run 回读确认仍待应用后才继续，没有盲目重复写入。随后使用 Supabase Management API，在同一事务内执行授权 migration 原文并写入唯一 migration history。
- 首次 Management API 请求暴露出 Windows PowerShell 5 默认请求编码会把中文降级。该问题只影响函数提示和注释，不涉及业务行；在 UTF-8 MD5 往返值与本地完全一致后，再次强制校验授权 SHA-256，并以显式 UTF-8 字节重新执行同一 migration 原文。history 保持唯一 1 条，没有删除或改写版本记录。
- 最终回读确认版本 `20260728135701`、名称 `route_corrections_directly_to_jury`、四个目标函数、约束、owner、空 `search_path`、execute grants 和中文函数正文 / 注释均与 migration 一致。

### 事务 fixture 与零残留

- development 串行通过 `db6_missing_suit_review.sql`、`db7_jury_voting.sql`、`db7_full_review_rewards.sql`、`db8_correction_request_intake.sql`、`db9_correction_request_review.sql`、`db10_direct_correction_jury.sql`，每份均以 `ROLLBACK` 结束。
- 覆盖报错直达陪审、同服装活动事项复用、全部缺失字段合并、报错者自审拦截、只修改问题字段、过期正式资料保护、相同重试幂等、投票 `+1`、补充通过 `+8`、报错最终采用 `+5` 和正式资料写回。
- DB9 fixture 原先错误地要求全局管理员队列恰好为 5 条；development 已保留 4 条合法 DB9 验收记录，因此断言改为只统计本事务生成的 5 个 request ID，同时继续逐条校验分流。修正后通过。
- 最终只读回读为 `fixture_users=0`、`fixture_clothes=0`、`fixture_requests=0`、`fixture_review_items=0`；既有 DB7 / DB9 验收事实未清理、未改写。

### 未完成业务验收数据

- 2026-07-28 直接在 development 准备短编号 `9900096` 的新多字段场景，固定问题字段为服装名称、星级、第一组属性和所属套装；审核项 `2b0469d9-7f18-43c2-b2b4-4c8eb3e566a6` 当前为 `pending`，来源 5 条、候选 0 条。
- 本地忽略种子脚本成功执行版本 SHA-256 为 `536FCE168F82F1B0CAC34086B6637E16379F3A6C368813C498A7EF3F27EEF3FF`。首次脚本因错误限定 `coalesce` 在事务内失败并自动回滚；修正后只新增该场景，未删除、覆盖旧投票、积分或审核事实。
- 现有 `9900002` 继续用于“只修改套装后重新补充”，`9900003` 用于管理员投票/终审互斥，`9900001` 作为无活动报错或审核单的正式服装供直达陪审验收。当轮三账号最短操作顺序已通过本地验收包交付并完成，不在仓库长期保存账号凭据。
- 本轮仅准备 development 验收事实，不改 schema、migration、RPC、RLS 或账号权限；production 和网页均未操作。提交失败依赖数据库事务回滚，人工操作后产生的审计事实默认保留，清理需新的精确授权。

### Advisor、类型与 rollback

- Security Advisor 未出现本 migration 新增 mutable `search_path`、内部函数公开执行或 RLS 扩大。`submit_correction_request` 的 authenticated `SECURITY DEFINER` 提示是预期 RPC 面，已由登录、自审、状态、行锁、幂等和最小 grant fixture 覆盖；`stages` / `suits` RLS、旧函数与旧 policy 告警为既有项目事项。
- Performance Advisor 没有本 migration 新增未索引外键或查询计划项；既有 `pending_clothes.suit_id`、`pending_suits.submitted_by`、旧 policy 和空表未使用索引提示不在本补丁扩项。
- `npm run db:types:safe` 成功；`src/types/supabase.ts` 为 32,594 字节，SHA-256 `ACC189CCAAC5AFB4E41A5FA9D6973F8CE93351DDB2C08DAE6644B89ED1ADE7FB`，新增内部校验和路由函数类型。完整 schema dump 未覆盖；migration、live catalog、schema 摘要和生成类型为当前权威事实。
- 已应用 migration 如需调整，只能新增前向 patch；不得改写 history 或已应用文件。错误积分继续使用反向流水，不删除审计事实。production 无 rollback 动作。

## 2026-07-28 11:39 - 正式图鉴报错处理、转陪审与奖励

### 基本信息

| 项目 | 内容 |
| --- | --- |
| 目标环境 | Supabase development `tfwejruvdahonacyldrg` |
| migration | `20260728025707_db9_process_correction_requests` |
| migration SHA-256 | `6E023255AC5EC79FB3BCBA01016756DDEDF6247574A366A7D0F5179C01B9A1A0` |
| 验收种子 | 本地忽略文件 `db9_correction_review_acceptance_seed.sql`，SHA-256 `EF434271635E1A5C2C3EF642EA09A51E491A4A9AD5E32E47DC40731B73E520E6` |
| 业务边界 | 管理员不可自审；空字段可直接补全，非空争议只能转全字段陪审，不采纳不改正式资料；报错最终被采用时唯一 `+5` |
| production | 未操作 |

### 前检、应用与事务验证

- development link 检查通过；聚合前检确认远端末项为 `20260728013639_db8_restrict_service_role_table_privileges`，目标 migration、三列扩展、积分来源列和两个公开 RPC 均不存在，目标固定测试账号已存在。
- 标准 migration history 直连在写入前返回 `LegacyDbConnectError`；没有切换环境或扩大文件集合。改用受控 Management API，在同一事务内校验远端 head、执行与授权 SHA-256 一致的 migration 原文、写入 migration history 并完成对象后检；正式应用前的同内容 rehearsal 已以 `ROLLBACK` 结束。
- 应用后远端 head 精确登记 `20260728025707_db9_process_correction_requests`。DB6、原 DB7、DB7 全字段、DB8 与 DB9 五套 development fixture 全部返回 `passed` 并以 `ROLLBACK` 结束；覆盖禁止自审、直接补全、转陪审、拒绝、过期保护、同请求幂等和 `+5` 唯一结算。

### 结构、权限与 Advisor 回读

- `correction_requests` 新增 `accepted_patch`、`source_pending_id`、`re_review_item_id`；`points_ledger` 新增 `correction_request_id`。8 个目标约束均已验证，3 个目标索引有效，`sync_correction_requests_after_re_review` 触发器启用。
- `get_correction_review_queue()` 与 `review_correction_request(...)` owner 为 `postgres`、`search_path` 为空；anon 无执行权，authenticated / service_role 可执行。内部同步函数不向 anon / authenticated / service_role 开放。`correction_requests` 保持 RLS 开启且无 policy，客户端不能直接读写底表。
- Security Advisor 对两个公开 `SECURITY DEFINER` RPC 和 RPC-only 无 policy 表的提示属于预期设计，由函数内身份、管理员角色、自审、状态和行锁校验及最小 grant 约束；没有 DB-9 新增 ERROR。Performance Advisor 未报告 DB-9 新外键缺少索引；新索引因尚未产生业务访问仅有未使用提示，旧表既有 RLS / policy / 未用索引提示未扩项。
- development TypeScript 类型已重新生成：32,355 字节，SHA-256 `FA429217ECD062B1FBE1AFE0B7A9DD0BA9352275B1ED1848251D1FE38D95C238`。全量 `supabase/schema.sql` 未覆盖；migration、live catalog 和 `src/types/supabase.ts` 是 DB-9 当前权威事实。

### 验收数据与当前基线

- 精确授权种子首次返回“标识已存在”，未重试写入；随后的只读回查确认四件目标服装和四条对应待处理报错已完整存在，说明首次请求已完成而后续重复尝试被脚本的 fail-closed 守卫拒绝。
- 现有 DB7 测试账号下已准备：`【DB9验收】空字段直接补全`（992001）、`【DB9验收】非空争议转陪审`（992002）、`【DB9验收】不采纳`（992003）、`【DB9验收】管理员自审拦截`（992004）。四条报错均为 `pending`，报错奖励流水和关联重审项均为 0。
- 验收前积分基线：管理员账号 1 分、source 账号 0 分、voter01 账号 2 分。管理员队列回读确认四条分别路由为“直接补全”“转陪审”“仅不采纳”“本人报错不可处理”。业务验收通过当轮本地验收包执行，验收事实保留在 development，账号凭据不进入仓库。

### Rollback

- migration 已应用后不删除、不改写；发现缺陷时只创建新的前向 patch。测试 fixture 均已事务回滚；四组验收事实只有在另行取得固定身份、依赖和数量的精确 development 清理授权后才能删除。production 未操作。

## 2026-07-28 09:34 - 正式图鉴报错受理与最小权限补丁

### 基本信息

| 项目 | 内容 |
| --- | --- |
| 目标环境 | Supabase development `tfwejruvdahonacyldrg` |
| 主 migration | `20260728005705_db8_create_correction_request_intake` |
| 主 migration SHA-256 | `542308977CBF7D8780FBA7F39E949965161314B3A429B89A3E156C968BF2F034` |
| 权限 patch | `20260728013639_db8_restrict_service_role_table_privileges` |
| 权限 patch SHA-256 | `F69C9C3C8B06960114188F17D6748C62A5A4ECDF931DE34DC02E0E80BADDCE45` |
| 业务边界 | 第一阶段只受理登录用户对正式服装的单字段报错并展示本人进度；不修改正式服装、不创建重审项、不写积分，不接管理员处理 |
| production | 未操作 |

### 主 migration 前检与应用

- 本地 link 检查为 development `tfwejruvdahonacyldrg`，主 migration 文件名和授权 SHA-256 精确匹配。
- Management API 只读回查确认远端末项为 `20260727141009_db7_expand_jury_full_review_and_rewards`，`correction_requests` 和两个目标 RPC 均不存在；本地其后只有获授权的 DB-8 主 migration。
- 标准 CLI dry-run 分别因 migration history `unexpected EOF` 和 HTTPS DNS 无有效 IP 在写入前失败；未无限重试。改用已成功的 Management API 历史差异完成等价 dry-run，并在单事务中再次校验远端 head、目标对象不存在、执行 migration 原文和写入 migration history。
- 应用后远端 head 为 `20260728005705_db8_create_correction_request_intake`；表为 0 行，RLS 已启用、无 policy，共 6 个有效索引和 10 个约束。

### RPC、权限与事务验证

- `submit_correction_request(varchar,text,jsonb)` 只接受登录用户、现有正式服装、一个允许字段、1–500 字建议和 10–1000 字依据；同内容重试幂等，不同内容不得覆盖活动报错。
- `get_my_correction_requests()` 只返回当前用户本人记录；两个 `SECURITY DEFINER` RPC 均为空 `search_path`、anon 无执行权限、authenticated / service_role 可执行。
- DB-8 增强 fixture 在 development 通过匿名拒绝、活动唯一性、相同重试幂等、不同内容拒绝、独立字段、非法字段、缺失服装、跨用户隔离、账号删除后匿名审计、service_role 最小表权限，以及“不改正式服装、不建重审项、不写积分”；最终 `ROLLBACK` 后报错表、fixture 用户、服装、重审项和积分均为 0。
- `npm run db:types:safe` 成功生成 `correction_requests` 和两个 RPC 类型。完整 schema dump 仍在首条只读查询遇到 SSL EOF；零字节临时输出已删除，没有覆盖 `supabase/schema.sql`，live catalog、migration 和生成类型作为当前回读证据。

### Advisor 与前向权限修复

- 原生 Advisor 命令因远端传输错误未返回；改用 live catalog 逐项复核，三条外键均有索引，RLS 已启用，RPC 的空 `search_path` 和执行权限符合预期。
- 回读同时发现 development 的旧默认 grant 让 service_role 除显式需要的 SELECT / INSERT / UPDATE 外，仍保留 DELETE / TRUNCATE / REFERENCES / TRIGGER。已应用主 migration 不原地修改。
- 使用 CLI 创建并在 development 应用前向 patch `20260728013639_db8_restrict_service_role_table_privileges`，先撤销 service_role 全部表权限，再只授予 SELECT / INSERT / UPDATE；远端 migration head、角色授权和 0 行持久基线均已回读。
- 增强 fixture 已在 development 通过并以 `ROLLBACK` 结束；原生 Advisor 因 PostgreSQL 直连失败未返回，live catalog 等价检查确认 3 个外键均有索引、6 个索引有效、RLS 开启、0 个 policy、两个 RPC 权限和空 `search_path` 符合预期。

### Rollback

- 主 migration 已在 development 应用，后续调整只使用新的前向 migration，不修改 migration history 或已应用文件。
- 当前表为 0 行且用户入口尚未发布；若需隔离故障，优先以前向 patch 撤销 RPC EXECUTE 并保留表和审计结构。只有确认无下游依赖且另行授权时才讨论删除对象。
- production、`main`、真实业务数据回填、测试账号持久化、commit、push、PR 和 merge 均未操作。

## 2026-07-26 09:43 - db6_create_re_review_items

### 基本信息

| 项目 | 内容 |
| --- | --- |
| 变更类型 | schema / RLS / grants / index / development verification |
| 目标环境 | Supabase development `tfwejruvdahonacyldrg` |
| migration | `20260725151136_db6_create_re_review_items` |
| 业务边界 | 只建立社区重审池基础；不接自动建项 / 自动关闭、投票、最终处理、前端或历史回填 |
| production | 未操作 |

### 变更目的与内容

- 建立 `re_review_items`、`re_review_item_sources`、`re_review_candidates`，分别保存重审状态、全部来源审计和社区候选修正版。
- 原提交者、主 pending 提交者和任一来源参与者都不能查看或参与自己的重审项；未参与原始提交的登录用户可查看开放项并提交一份不可修改候选，管理员只作为后续兜底。
- 三张表均启用 RLS；撤销 `PUBLIC` / `anon` 默认权限，authenticated 只保留开放项读取、自己的来源标记读取和候选插入，service_role 只保留必要表 / 列权限。
- 活跃待补套装、主 pending + 原因和每项候选均有唯一性保护；所有外键列均由主键、唯一索引或独立索引覆盖。

### 前检、应用与后检

| 检查项 | 结果 |
| --- | --- |
| 前检环境 | 本地 link 与目标均为 development `tfwejruvdahonacyldrg`；远端 migration 原末项为 DB-5，三张目标表不存在 |
| 业务基线 | apply 前 `clothes=74`、`pending_clothes=7`、`profiles=7`、贡献 3、积分流水 3；本 migration 不回填业务数据 |
| dry-run | 只计划应用 `20260725151136_db6_create_re_review_items` |
| development apply | 首次 CLI 返回 `unexpected EOF`；回读证明 migration 和目标表均未写入后安全重试，第二次成功 |
| migration history | 已新增 `20260725151136_db6_create_re_review_items` |
| live catalog | 3 张表、14 个索引、18 个约束、4 条 policy；三表 RLS 均启用，持久行数均为 0 |
| migration 文件 | SHA-256 `04AA039008981AECBE9317712ACAFCFD5FA285754BD4955E185C772A2F4288C1` |

### 权限与事务验证

| 场景 | 结果 |
| --- | --- |
| 社区读取 | 未参与原始提交的登录用户可见目标重审项 |
| 候选提交 | 符合条件的登录用户可写入一份候选修正版 |
| 防自审 | 来源用户看不到自己的重审项；尝试提交候选被 RLS 拒绝 `42501` |
| 匿名拒绝 | anon 读取被表权限拒绝 `42501` |
| 事务回滚 | 验证期间可见重审项、来源和候选；rollback 后三张表均恢复 0 行 |
| policy 回读 | community item SELECT、own source SELECT、candidate SELECT / INSERT 共 4 条，与 migration 一致 |

### Advisor、生成物与回归

- Security Advisor 没有 DB-6 新增问题；仍报告既有 `stages`、`suits` 未启用 RLS，不属于本 migration，未擅自扩项。
- Performance Advisor 连续两次因连接通道中断未取得结果，明确记为未完成；已用 live catalog 复核外键索引和 RLS 索引覆盖，不把连接失败记为通过。
- `npm run db:types:safe` 成功，`src/types/supabase.ts` 已生成三张表类型；`npm test` 22 / 22 通过，`npm run build` 通过。
- 远程全量 dump 先因 Docker 未启动失败，启动 Docker 后直连与本机 SOCKS 转发均在首条只读查询遇到 EOF；所有空输出均已删除或恢复，没有提交半截生成物，该远程检查仍明确记为未完成。
- 随后仅启动本地 Supabase 数据库，从 `20260418113144` 到 DB-6 的全部 migrations 按顺序重放成功；本地 catalog 同样得到 3 张表、14 个索引、18 个约束、4 条 policy、3 张 RLS 表和三表 0 行。
- 本地全量 public dump 为 95,357 字节，SHA-256 `3FCC375DA36732E15BD930EF65648D9DC0E0596FD460E64892A65F817AA17BF9`。因本地服务默认权限版本与 development 既有对象不同，没有机械覆盖旧对象；`supabase/schema.sql` 保留最后一次 development DB-5 全量基线，只嵌入与 DB-6 migration 字节一致的验证段，最终 SHA-256 为 `F049DE845119A9035FE04749F76C3E7B26A35A8CD4D2E6AEC3CE5219DEEE281C`。

### 结论与 Rollback

- DB-6 基础已经在 development 生效并通过用户指定的五项验证、本地全量 migration 重放和生成物核对；尚不等于缺套装自动建项、自动关闭、陪审团投票或最终修改正式库已经完成。
- 如需停用，新增 patch migration 先撤销 authenticated 对三张表的入口并保留现有结构和审计数据；只有三表为空且无下游依赖时才讨论按依赖顺序删除，不改写已应用 migration history。
- production、`main`、历史回填、commit、push、PR 和 merge 均未操作。

### 2026-07-26 10:30 权限修复：db6_restrict_candidate_insert_columns

- 根因：原 migration 向 authenticated 授予 `re_review_candidates` 整表 INSERT，RLS 虽能约束用户和重审项，却仍允许客户端自行填写 `id`、`created_at`，削弱来源审计可信度。
- migration：`20260726022216_db6_restrict_candidate_insert_columns`，SHA-256 `0B926637C029B2DD1BB4D676D1BB33018E205C274A6FDBF56CF1F1180008B816`。撤销整表 INSERT，只重新授予 `re_review_item_id`、`payload`、`submitted_by` 三列 INSERT；不改表结构、policy、现有数据或原 migration。
- 前检：development `tfwejruvdahonacyldrg` 健康，原末项为 `20260725151136_db6_create_re_review_items`，三张表均为 0 行；修复前 authenticated 对候选表五列均有 INSERT。
- rehearsal：本地从零重放全部 migrations 至本 patch 成功；符合条件的社区用户可提交候选，`id` / `created_at` 伪造被权限拒绝，来源用户不能自审，anon 读取被拒绝，rollback 后用户、服装和 DB-6 三表 fixture 均为 0。
- development apply：dry-run 只包含本 patch；首次 CLI 在写入前返回 `unexpected EOF`，migration history 与五列权限回读均证明未生效后安全重试，第二次成功。
- development 后检：migration history 已新增本 patch；authenticated 只有三个业务列 INSERT，`id` / `created_at` 无 INSERT，候选 SELECT 保留且无 UPDATE / DELETE，anon 无 SELECT / INSERT；三张表仍为 0 行。
- development 事务验证：社区读取和候选提交成功，数据库生成候选 ID / 时间；审计字段伪造、防自审、匿名读取均被拒绝，rollback 后用户、服装和 DB-6 三表 fixture 均为 0。
- Advisor：Security / Performance Advisor 均已取得结果，没有 DB-6 权限修复新增告警；Security 仍有 `stages` / `suits` 未启用 RLS 等既有问题，Performance 仍有既有 policy / 外键告警及 DB-6 零数据下的 unused-index INFO，均未扩入本 patch。
- 生成物：安全类型生成成功且类型无变化；`supabase/schema.sql` 追加本 patch 后 SHA-256 为 `E8EBB40D289E513B1658196F283A0A4F936A01FD5347A9D71FBE4CD382FD69CF`。本次取得 development 全量 public dump（113,550 字节，SHA-256 `E1E12A8319FDC8121013B6BCC60C7A9011766818D405FAFA5BC470A445CF479D`）并确认三个候选业务列 ACL；因保留 pg_dump 注释会产生大范围纯格式差异，未机械覆盖当前快照。
- Rollback：如需停用本 patch 的社区写入口，新增 migration 撤销 authenticated 对三个业务列的 INSERT 并保留表、policy 和审计数据；不恢复允许伪造审计字段的整表 INSERT，不改写 migration history。
- production、`main`、PR merge、下一项自动建项 / 自动关闭功能均未操作。

### 2026-07-26 11:11 本地接入：db6_integrate_missing_suit_review

- migration：`20260726024734_db6_integrate_missing_suit_review`，SHA-256 `1DE8BF00F25A5A23BD3D9A31839688AA3DF10B1EC769857E4BAE023FB9533AA5`；已应用 development `tfwejruvdahonacyldrg`，production 未操作。
- 产品状态：新增用户文案“所属套装待确认”，通过 `pending_clothes.needs_suit_review = true` 显式保存；“无关联套装（纯散件）”与所有历史空套装行默认 `false`，不回填、不进入待补套装重审池。
- 自动入库：DB-5 RPC 增加可选布尔参数并保持旧调用兼容；第五位不同有效用户触发时，正式服装、唯一 `missing_suit` 重审项、全部一致来源、前 5 贡献和每人 10 分、衣柜及 pending 通过同事务完成。
- 管理员仲裁：公开 RPC 继续沿用原名称和旧参数默认值，由内部 DB-4 核心完成原子入库；包装层核对全部来源的待确认状态，混合纯散件、表单伪造或既有重审事实不完整时拒绝静默补写。
- 正式库已有：DB-3 补全 RPC 保持原参数和返回兼容，由包装层按 pending 来源事实决定是否同事务创建重审项；完成后重试只接受完整一致的既有重审项和来源。
- 自动关闭：正式服装 `suit_id` 从空值绑定正式套装后，触发器只关闭该服装活跃的 `missing_suit` 项，写入 `resolved_at / updated_at`；不关闭其他原因或其他服装的重审项。
- 权限：两个对外 RPC 继续只授权 authenticated / service_role，并在函数内校验登录或管理员；内部建项、DB-4 核心和自动关闭函数不向 anon / authenticated 开放，DB-1 底表和 DB-6 RLS / 候选列权限未放宽。
- 本地验证：从零重放全部 migrations 成功；DB-3、DB-4、DB-5 原 SQL fixture 继续通过；新增 DB-6 fixture 覆盖匿名拒绝、纯散件保持、第五人建项、全部来源、防自审、社区候选、管理员新建、正式库已有补全、状态伪造拒绝、绑定自动关闭、完成后重试和故障注入整体回滚，所有 fixture 最终 rollback。
- development 应用：首次写入因 `unexpected EOF` 未生效，独立回读确认 migration history 与目标对象均不存在后停止；重新前检和授权后，dry-run 仍只包含本 migration，第二次应用成功。远端 history、非空默认 `false` 字段、约束、触发器和目标函数均已回读，旧业务行数保持 `clothes=74`、`pending_clothes=7`、贡献 3、积分流水 3、衣柜 5，重审三表仍为 0。
- development 事务验证：DB-6 fixture 明确返回 `passed`，覆盖匿名拒绝、纯散件保持、第五人自动建项、全部来源、前 5 贡献与 50 分、衣柜写回、防自审、社区候选、管理员新建、正式库已有补全、绑定套装自动关闭、重试幂等和故障注入整体回滚；fixture 以 `ROLLBACK` 结束，没有测试用户、服装或重审事实持久化。
- Advisor：Security 为 2 ERROR / 14 WARN / 2 INFO；三个目标公开 RPC 的 `SECURITY DEFINER` 提示属于有意的 authenticated 入口，函数内继续校验身份且内部函数不开放。既有 `stages` / `suits` 未启用 RLS 等问题未扩入本 migration。Performance 已通过 CLI 备用入口取得，只有既有 RLS / policy 告警和重审池零数据下的 unused-index INFO，没有本接入新增的性能门禁问题。
- 生成物：development 安全类型生成成功且与已提交版本完全一致，SHA-256 保持 `863C702F9170FA480C9D96C5D651E4DF8C71AB9E5FF6565295A3DDCC8AD36CAE`。远端 public dump 为 113,897 字节，SHA-256 `B869E3B81451B217F3F77741CC4446FBD206DD8E00AA74B24AC8DD5B132F3CBA`，包含目标字段、建项函数和自动关闭触发器；因 dump 会带来无关格式差异，未机械覆盖快照。`supabase/schema.sql` 继续按 migration 原文维护，状态注释更新后 SHA-256 为 `621583D2CA227F5F8D69AB4DBF8820469747170DC4E4AF2CE746E43A30322348`。
- 前端验证：新增状态可在共用表单选择并在审核端恢复、与纯散件分组隔离；`npm test` 23 / 23、production build 通过。
- Rollback：如 development 应用后需停用，新增 patch migration 让 `needs_suit_review = true` 的提交重新只保留 pending、停用自动关闭触发器，并保留已形成的重审项、来源、贡献和积分审计事实；错误积分用等额负数流水反向处理，不删除历史、不改写 migration history。

### 2026-07-27 23:05 陪审团全字段审核与积分补丁

- migration：`20260727141009_db7_expand_jury_full_review_and_rewards`，SHA-256 `1169C3B0D27AE0C28F5A4813B6D9F4F1B973A4111B13FD5489B22DFA812A8552`；仅应用 development `tfwejruvdahonacyldrg`，history 精确登记一次，production 未操作。
- 前检与回填：应用前补丁对象不存在、活动事项无身份键冲突；已有 11 条有效 `jury_votes`、积分流水 4 条。migration 为 11 条历史票幂等补发 `+1`，应用后投票积分 11 / 11、无缺失，积分流水共 15 条。
- 全字段审核：同一服装只允许一个 `pending / voting / failed` 活动事项；五位不同用户仍有差异时建立一个审核单，正式服装非空字段冲突立即转入；队列统一返回基础资料、全部问题字段、分歧值、完整待审核内容和投票 / 终审权限。
- 完整资料与正式库边界：`submit_jury_candidate` 校验九个顶层字段、十个属性键和五组属性规则，只允许修改审核单列明字段；既有服装发生变化时拒绝旧内容，新服装按最终完整资料入库。旧 DB-6 / DB-7 单字段缺套装请求保持兼容。
- 积分与来源：`points_ledger.jury_vote_id` 外键和部分唯一索引保证每位陪审员对每份内容首次有效投票 `+1`；补充者通过后 `+8`；五人冲突单通过后仅与最终资料一致的最早五位原始提交者按 `jury_resolution` 获得 `+10` 和衣柜写回。
- 权限与事务验证：公开 RPC 为空 `search_path`、owner 为 `postgres`、只授权 authenticated / service_role；内部 helper 不向 authenticated / anon 暴露。全字段 DB-7、原 DB-7、DB-6 三套 development fixture 均返回 `passed` 并以 `ROLLBACK` 结束，固定 fixture 身份和业务数据无残留。
- Advisor：Security 对公开 `SECURITY DEFINER` RPC 的 WARN 是有意的 RPC-only 入口，由函数内身份 / 状态校验和最小 grant 约束；没有本补丁新增 ERROR。Performance 未报告 `points_ledger.jury_vote_id` 新外键缺少索引；其余旧外键、旧 RLS 和旧未用索引告警未扩项。参考：[Security Advisor 说明](https://supabase.com/docs/guides/database/database-linter)、[Performance Advisor 说明](https://supabase.com/docs/guides/database/database-linter)。
- 生成物：development TypeScript 类型成功生成，28,564 字节，SHA-256 `BB57EDEEAC37BBEDAA00BF90A86B9E2A3D2B84A44A911CCC337A8A53EB57168F`；public schema dump 成功，177,953 字节，SHA-256 `A5830245222B1CDC1E70F596D23CCE7E3AC7DEE09991D50F3F7FB18101F17158`。
- 验收数据：保留现有 8 个 development 测试账号和 4 个历史场景；直接通过数据库新增短编号 `9900095` 的五人多字段分歧场景，审核单 `e25e4e8e-9f3a-46e4-bfec-4353955fcd0f`，包含名称、星级、第一组属性和所属套装四项分歧。未通过网页造数。
- Rollback：migration 已应用后不删除、不改写；如需停用或修正，只创建新的前向 patch。错误投票积分以等额负数 reversal 追加冲正，不删除历史票或流水；验收数据清理需按固定身份、依赖和数量另行授权。

### 2026-07-27 20:53 陪审团闭环：db7_integrate_jury_voting / db7_index_admin_user_fk

- migration：`20260727121148_db7_integrate_jury_voting`，SHA-256 `20C43FDE2CC8C2384E655F40943271C743612FD6B5C4DAEA673087B18A3ED433`；性能修复 `20260727124555_db7_index_admin_user_fk`，SHA-256 `DDEBBABCDB6B4FF366296D0468F2562E0FFC1EE5F5472756F0E1748B620D38EA`。两者均已应用 development `tfwejruvdahonacyldrg`，production 未操作。
- 产品规则：同意票至少 5 且多于反对票时通过；`反对票 - 同意票 >= 3` 时只退回重审并要求修正候选快照，不修改正式服装、不发积分、不永久驳回或关闭审核项；其他情况继续投票。永久驳回只由未参投、未提交候选、未参与原始数据的超级管理员独立终审。
- 数据模型：候选增加 `voting / approved / returned / rejected` 轮次状态；每个重审项只允许一个活跃候选，旧候选和旧票保留。新增不可变 `jury_votes`、独立 `jury_admin_decisions`，以及 `points_ledger.re_review_candidate_id`；候选通过后向候选提交者记录唯一 `+8` 流水。
- RPC 与权限：新增候选提交、陪审团队列、投票、管理员终审四个 `SECURITY DEFINER` RPC，均为空 `search_path`、只授权 authenticated / service_role，并在函数内复核身份、自审冲突、候选轮次和正式服装旧值。两张新底表启用 RLS，但不给 authenticated policy 或 DML；anon 无 RPC 执行权。
- development 前检：原末项为 `20260726024734_db6_integrate_missing_suit_review`，DB-7 对象不存在；`clothes=74`、`pending_clothes=7`、贡献 3、积分流水 3，重审三表均为 0。主 migration 通过仓库 development 安全脚本应用成功，远端 history 精确登记 `20260727121148`，业务行数不变。
- development 事务验证：DB-6 兼容 fixture 和 DB-7 fixture 均返回 `passed` 并以 `ROLLBACK` 结束。DB-7 覆盖一人一票、同票重试幂等、不可改票、来源 / 候选提交者防自审、5 票且领先通过、净反对 3 退回、其他状态继续投票、正式库只在通过时修改、`+8` 积分、旧轮次隔离、管理员参投冲突和独立终审；回滚后 `clothes=74`、`pending_clothes=7`、贡献 3、积分流水 3，五张重审 / 陪审事实表仍为 0。
- Advisor：Security 对两张新表报告 RLS 无 policy INFO、对四个公开 RPC 报告 authenticated 可执行 `SECURITY DEFINER` WARN，均为有意的 RPC-only 入口，并由空 `search_path`、最小 grant 和函数内身份 / 状态校验约束；既有 `stages`、`suits` 未启用 RLS等项目级问题未扩项。Performance 首次报告 `jury_admin_decisions.admin_user_id` 外键无覆盖索引；第二个 migration 新增部分索引后，远端 Advisor 不再返回该目标告警。
- 索引 patch 应用：CLI 连续两次在读取远端状态时返回 `unexpected EOF`；每次均独立回读确认 migration history 和目标索引未变化。随后按已授权 SQL 在一个事务内创建索引、写注释并登记精确版本 `20260727124555`；远端回读得到 4 条原 migration statement、目标部分索引定义和注释，业务行数仍不变。
- 生成物：远端 TypeScript 生成的 CLI 与连接器通道均发生网络传输错误，没有保留错误输出；`src/types/supabase.ts` 由已重放至 DB-7 的本地数据库生成，并与远端 catalog 的两张表、新字段和四个 RPC 逐项核对。development 全量 public dump 在首条只读查询遇到 SSL EOF，保护逻辑恢复原快照；`supabase/schema.sql` 按两个 migration 原文嵌入，SHA-256 为 `91004C23062511813053A1462BC532FA5F41970C222187EC6268675BC5639D25`。
- 前端与本地回归：新增登录用户“陪审团”入口、候选套装选择、票数 / 本人投票状态、一次性投票和超级管理员独立终审；`npm test` 29 / 29、production build、`git diff --check` 均通过。本地运行时页面加载、正文渲染和错误 overlay 检查通过；登录态业务闭环由 development RPC fixture 覆盖，未在 development 创建持久验收账号或数据。
- Rollback：若需停用，新增 patch migration 先撤销四个 RPC 的 authenticated execute 并保留投票、候选、终审和积分审计事实；错误正向积分使用等额负数 reversal，不删除历史、不回写已应用 migration。管理员外键索引可由独立 patch 删除，但正常停用无需删除。production、`main` 和历史回填均未操作。

## 2026-07-22 20:30 - db5_integrate_auto_entry

### 基本信息

| 项目 | 内容 |
| --- | --- |
| 变更类型 | RPC / contribution facts / points ledger / verification |
| 目标环境 | Supabase development `tfwejruvdahonacyldrg` |
| migration | `20260722130313_db5_integrate_auto_entry` |
| 关联 PR | PR #97，目标 `develop` |
| 关联方案 | `docs/planning/开发文档.md` 第 0.3.3 节、`docs/planning/技术实现文档.md` 第 0.3 节 |
| production | 不在范围内，未操作 |

### 前检与当前状态

- 本任务开始前已只读确认 development 项目健康，远端 migration 末项为 `20260721094234_db4_integrate_admin_arbitration`；`auto_entry` 贡献为 0，既有 2 条管理员仲裁贡献、1 条正式库补全贡献和 3 条积分流水合计 25 分保持不变。
- live `submit_clothing_contribution` 仍来自 `20260627143000_sync_approved_clothes_to_submitter_wardrobes`：签名不变、仅 authenticated 可调用，但使用 `search_path=public`、当前提交不形成 pending，且自动入库会删除匹配来源。
- 本轮再次发起远端只读复核时，GitHub 审批通道和 Supabase MCP 均发生传输中断；没有执行或重试任何数据库写入。上列基线来自本任务计划阶段同日成功回读，apply 前必须重新确认。

### 本地变更

- 仅替换 `submit_clothing_contribution(text,text,text,integer,jsonb,uuid,text,text)`；使用 `SECURITY DEFINER`、空 `search_path`、全限定对象，撤销 `PUBLIC` / `anon` 默认执行权，只授权 `authenticated` / `service_role`。
- 当前提交先写 pending；同一用户完全一致的 pending 重试返回原 ID。满足 5 位不同有效用户后，全部一致来源保留为 `approved`，前 5 位写 `auto_entry` 贡献、每人 10 分并写回衣柜。
- 分类 + 短编号和名称 + 分类业务键在事务内串行化；完成后重试必须核对正式服装、来源、事件、贡献、积分和衣柜完整一致，禁止静默补写。
- 无正式套装和临时套装的提交保持 pending，不创建重审池；未修改表、索引、RLS policy、DB-1 底表 grants、DB-2 只读面、前端或历史数据。

### 验证状态

- 已新增并执行 `supabase/tests/db5_auto_entry.sql` 事务 fixture，覆盖匿名拒绝、重复提交、第五人入库、稳定前 5、来源保留、每人 10 分、衣柜、DB-2 视图、完成后重试、冲突拒绝、无套装保留和积分故障回滚；第五 / 第六次并发另以真实登录身份验证。
- 本地 SQL 不变量检查通过：migration 只有一组函数 dollar quote、无 pending DELETE、包含两组事务级 advisory lock、空 `search_path`、`auto_entry` 写入与最小 execute grants；fixture 以 `BEGIN` / `ROLLBACK` 包裹。
- `npm test` 16 / 16 通过，`npm run build` 与 `git diff --check` 通过；development rehearsal 已成功执行完整 migration 并 rollback，SQL 语法由目标 Postgres 17.6 证明。
- migration 已应用；live function / owner / grants、函数行为断言、Security Advisor、持久基线、事务 fixture、真实角色和并发调用均已通过。
- RPC 签名未变化，`src/types/supabase.ts` 预期无接口差异；`supabase/schema.sql` 和本 schema 摘要只能在 development apply 后按 live definition 刷新。

### 2026-07-22 20:51 apply 尝试与失败确认

- 用户已明确授权仅向 development `tfwejruvdahonacyldrg` 应用本 migration；不包含 fixture、production、历史回填或 PR merge。
- apply 前重新确认项目为 `ACTIVE_HEALTHY` / Postgres 17.6，远端 migration 末项为 DB-4，并回读旧 RPC definition、owner、`search_path=public`、execute grants 和持久行数基线。
- rehearsal、CLI dry-run 和 Dashboard 降级入口先后遭遇连接器传输中断、数据库 TLS / DNS 失败和 Management API 加载失败，均未取得可记为通过的执行结果。
- 正式 `apply_migration` 只提交一次，响应在传输层中断；未重复写入。连接恢复后只读回查确认 migration history 仍以 `20260721094234_db4_integrate_admin_arbitration` 为末项，live RPC 仍无 DB-5 advisory lock 且 `search_path=public`。
- 持久数据基线保持不变：`auto_entry=0`、全部贡献 3 条、积分流水 3 条合计 25、服装 74、pending 7、衣柜 5；本次没有留下 migration、函数或业务数据变更。
- 因门禁失败，本轮停止；再次 apply 需要负责人重新确认，且不得把本次失败记为 development 验证通过。

### 2026-07-22 21:03 重试成功与回查

- 负责人重新授权后，apply 前再次确认项目 `ACTIVE_HEALTHY`、远端仍以 DB-4 为末项，旧 RPC 与贡献 / 积分基线未变。
- 为避开连接器的大载荷传输故障，仅移除 SQL 注释和多余空白；所有字符串字面量保持逐项一致。完整 migration 在 development rehearsal 中成功解析并 rollback，返回 `DB5_REHEARSAL_ROLLED_BACK`。
- 正式 `apply_migration` 仍发生响应传输中断，未重复写入；只读回查确认 migration history 新增 `20260722130313_db5_integrate_auto_entry`，live RPC 已切换到 DB-5。
- 连接器登记的远端版本号为 `20260722130313`；本地 migration 文件同步改名为同一版本，避免本地 / 远端 migration history 漂移。SQL 内容仍为原 `20260722122345` 文件，apply 前 SHA-256 为 `38988B205C59C3DBA5F87CCC097FB65B66AE2A7B9202D091762E0ED11CF02DA6`。
- live RPC owner 为 `postgres`，配置为空 `search_path`，包含两组事务级 advisory lock 和 `auto_entry` 写入；`anon` 无 execute，`authenticated` / `service_role` 有 execute，DB-1 底表 DML 权限未放宽。
- apply 后持久行数与前检一致：`auto_entry=0`、全部贡献 3 条、积分流水 3 条合计 25、服装 74、pending 7、衣柜 5；migration 未回填或改写业务事实。
- 函数行为定义回查确认：当前提交写 pending、匹配来源更新为 `approved`、每人 10 分、衣柜 helper、DB-5 comment 均存在；无 pending DELETE 或重审池引用。
- Security Advisor 未把目标 RPC 报为 mutable `search_path`；其对 authenticated 可执行 `SECURITY DEFINER` 的 warning 与本任务有意暴露的特权入口一致，已由 `auth.uid()`、空 `search_path`、最小 execute grants 和底表默认拒绝共同约束。其余 ERROR / WARN 为本 migration 之前已有的项目级事项，本任务未扩大。
- development TypeScript 类型重新生成后与 `src/types/supabase.ts` 完全一致。2026-07-22 22:23（北京时间）通过本机 SOCKS 明确转发到 development IPv4 pooler，官方 `pg_dump` 完成全量 public schema dump；原始输出 81,924 字节，SHA-256 为 `F85C5DFC8F115F26450EE4BDBD6701381D802C4E878D859D771E32A0195D7243`。对象计数、目标 RPC、空 `search_path`、权限与无 pending DELETE / 重审池写入断言通过，`supabase/schema.sql` 已由该全量快照机械替换，仅移除末尾多余空行。
- 该次 apply 授权不含 fixture；事务 fixture 与并发验证已于 2026-07-23 取得独立授权并完成。production、历史回填和 PR merge 均未操作。

### 2026-07-23 22:30 事务 fixture 与真实并发验收

- 完整执行 `supabase/tests/db5_auto_entry.sql`：匿名拒绝、重复用户不计数、第五位触发、稳定前 5、来源保留、每人 10 分、衣柜、DB-2 读取面、完成后重试、候选冲突、无套装保留和积分故障整体回滚全部通过；事务结束后 fixture 用户与业务行均为 0。
- 并发验收先提交 4 位固定测试用户的匹配 pending，再以第 5 / 第 6 位两个真实登录身份同时调用 development Data API。两个响应均为 `auto_approved=true`，返回同一 `clothes_id`、`matched_pending_count=5` 和 `wardrobe_updated_count=5`。
- 并发后的持久图只包含 1 件正式服装、5 条保留为 `approved` 的来源 pending、5 条 `auto_entry` 贡献和 5 条每人 10 分的流水，总积分 50；前 4 位固定来源与第 5 / 第 6 位中的一个胜出来源组成稳定前 5。
- 并发 fixture 因需要让第二个请求看到第一个请求的正式事件而短暂提交；验证后按固定 UUID、邮箱前缀、服装名与短编号精确删除。清理复核为 `fixture_users=0`、`fixture_rows=0`，持久基线恢复为 `auto_entry=0`、全部贡献 3、积分流水 3 条合计 25、服装 74、pending 7、pending suits 0、衣柜 5。
- 本机数据库第二连接与 Supabase MCP 多次发生传输中断，均未记为通过；每次已提交的并发 fixture 都由 `finally` 清理并复核零残留。最终通过两个并行 HTTPS RPC 完成真实并发验证。production、`main`、历史回填和 PR merge 均未操作。
- 验证后的 Security / Performance Advisor 复查在 Supabase API 返回 `unexpected EOF` 后停止，未取得新结果，明确记为“本次复查未完成”；fixture 未执行 DDL，apply 后已取得的 DB-5 Security Advisor 证据未被结构变更影响，Performance Advisor 留待独立 PR 审计重试。

### 2026-07-23 22:50 合并前独立审计

- PR #97 的目标、差异、权限边界、回滚、生成物、自动化检查和 development 验证证据已独立复核，无阻断问题。
- Security Advisor 重试成功：目标 RPC 未出现 mutable `search_path`，authenticated 可执行 `SECURITY DEFINER` 的 warning 与本任务有意暴露的特权入口一致；登录校验、空 `search_path`、最小 execute grants 和底表默认拒绝均已复核。其余提示均为本 migration 之前已有的项目事项。
- Performance Advisor 重试仍因连接器传输中断未取得结果；本 migration 只替换函数，不涉及表、索引或查询计划，因此不作为本次合并门禁。
- development migration 末项仍为 `20260722130313_db5_integrate_auto_entry`；持久基线保持 `auto_entry=0`、贡献 3 条、积分流水 3 条合计 25 分、服装 74、pending 7、衣柜 5，fixture 用户与业务行均为 0。

### Rollback

- apply 前 rollback 是不执行 migration；仓库变更可回退本任务提交，不影响 development。
- apply 后如需停用，新增 patch migration 将 RPC 切换为只保留 pending、暂停自动入库；不改写 migration history，不恢复删除来源 pending 的旧函数。
- 已形成的贡献与积分事实不得删除；错误积分追加等额反向流水，保留来源 pending、贡献、event 和衣柜审计线索。production 未执行，无 production rollback 动作。

## 2026-07-22 10:45 - pr95_frontend_validation_seed

### 基本信息

| 项目 | 内容 |
| --- | --- |
| 变更类型 | development test seed / business validation |
| 目标环境 | Supabase development `tfwejruvdahonacyldrg` |
| migration | 无 |
| 关联 PR | 前端集中修复 Draft PR #95 |
| production | 未操作 |

### 目的、前检与执行

- 为 BUG-20260721-018 人工回归一次准备纯散件、已有套装和临时套装三类 pending，避免负责人进入 Preview 后因无数据无法验收。
- 写入前确认项目 `nikki-calculator-dev` 为 `ACTIVE_HEALTHY`，固定普通测试用户仍存在且为 `role=user / role_level=0`；三条固定名称及发型短编号 `951 / 952 / 953` 在 pending 与正式库均无冲突。
- 使用一条事务写入三条记录；事务内再次校验测试用户、已有套装和冲突数量，任一条件变化或插入数量不是 3 即整体失败。
- 连接器在提交响应时发生传输失败，未直接重试；按固定名称回读确认事务已完整提交且没有重复或半完成状态。

### 回读与验收事实

| pending ID | 名称 | 套装事实 | 状态 |
| --- | --- | --- | --- |
| `55` | `PR95验收-纯散件-0722` | `suit_id = null`、`temp_suit_name = null` | `pending` |
| `56` | `PR95验收-已有套装-0722` | 关联已有套装“仲夏星辰”、`temp_suit_name = null` | `pending` |
| `57` | `PR95验收-临时套装-0722` | `suit_id = null`、临时套装名 `PR95验收临时套装-0722` | `pending` |

- 三条记录使用同一固定普通测试用户，`created_at` 一致，说明它们来自同一原子事务；管理员页面按 `status = 'pending'` 倒序查询，可作为三组独立候选显示。
- 本次只新增验收数据，未修改 schema、migration、RPC、RLS、账号角色、正式服装、贡献、积分或衣柜。

### Rollback / 清理

- 人工验收前如需撤销，必须先按 pending ID `55 / 56 / 57`、固定名称及 `status = 'pending'` 三重预检，只有精确命中 3 条时才可在单一事务删除；清理属于新的数据库删除操作，需另行明确授权。
- 若负责人已执行审核，必须停止使用上述删除方案；正式服装、pending、贡献、积分和衣柜会形成审计链，后续清理或反向积分需单独设计并授权，不得直接删除事实。

## 2026-07-21 17:52 - db4_integrate_admin_arbitration

### 基本信息

| 项目 | 内容 |
| --- | --- |
| 变更类型 | RPC / contribution facts / points ledger / verification |
| 目标环境 | Supabase development `tfwejruvdahonacyldrg` |
| migration | `20260721094234_db4_integrate_admin_arbitration` |
| 关联方案 | `docs/planning/开发文档.md` 第 0.3.3 节、`docs/planning/技术实现文档.md` 第 0.3 节 |
| production | 未操作 |

### 变更与影响

- 只替换 `approve_pending_clothes_arbitration(...)`，参数、返回类型和前端调用方式不变；函数改为 `SECURITY DEFINER`、空 `search_path` 和全限定对象，只向 `authenticated` / `service_role` 授予 execute。
- 选中 pending 在服务端逐项核对名称、分类、短编号、星级、属性、套装 / 临时套装和标签；任一不一致即拒绝，不能依赖客户端聚类决定奖励。
- 同一真实用户只取最早来源，再按来源时间、pending ID、用户 ID 稳定排序，最多 5 人；同一事务新建正式服装、写 `admin_arbitration` 贡献、每人追加 10 分、写回同批衣柜，并将全部选中 pending 保留为 `approved`。
- 事件 ID 由正式服装 ID 与排序后的 pending IDs 稳定派生；只有正式服装、贡献、积分、衣柜和 pending 事实完整一致时，重试才返回幂等成功。旧的已批准但事实不完整记录拒绝静默回填。
- 未修改表结构、RLS policy、DB-1 底表 grants、DB-2 只读面、DB-3 既有事实或前端业务代码。

### 前检、执行与异常记录

| 检查项 | 结果 |
| --- | --- |
| development link | project ref 为 `tfwejruvdahonacyldrg`，远端项目 `ACTIVE_HEALTHY` |
| apply 前事实 | `clothing_contributions = 1`、`points_ledger = 1`、`admin_arbitration = 0`；均为既有 DB-3 验收事实，未删除或改写 |
| 旧 RPC | `search_path=public`；anon 无 execute，authenticated / service_role 可 execute；旧定义来自 `20260627143000_add_admin_arbitration_rpc.sql` |
| rehearsal | migration 单独事务 rehearsal 后 rollback；完整 DB-4 测试在同一事务运行并 rollback，旧函数与 migration history 均保持未写入状态 |
| CLI dry-run | development 安全门禁通过，Supabase CLI 在 TLS / EOF 处失败且未写入；没有切换 production |
| apply | 连接器首次响应传输失败；远端回读确认 migration `20260721094234`、新函数定义和 grants 已实际生效，因此没有重复 apply |
| 安装后复核 | migration list、owner、空 `search_path`、函数注释、execute grants、底表拒绝和持久行数均与预期一致 |
| schema / types | CLI dump 两次 EOF 且均未保留失败产物；schema 函数快照以已应用 migration 和 live definition 更新。CLI 与连接器类型生成均传输失败；RPC 签名不变，保留原类型快照并将失败记入审计 |

### 角色、业务与事务验证

`supabase/tests/db4_admin_arbitration.sql` 使用合成账号、1 件测试服装和 8 条 pending，在单一 development 事务中验证后 rollback：

| 场景 | 结果 |
| --- | --- |
| 普通用户 | RPC 拒绝；不能直接操作 `clothing_contributions` / `points_ledger` |
| 候选一致性 | 任一候选字段与 pending 不一致时整体拒绝，无服装、pending、贡献、积分或衣柜半完成状态 |
| admin / super_admin | admin 首次仲裁成功；super_admin 反序传入同一 pending IDs，返回幂等成功 |
| 前 5 规则 | 匿名来源、同用户重复来源和 6 位真实用户中，仅稳定排序去重后的前 5 位形成贡献 |
| 积分与衣柜 | 5 条贡献、5 条各 `+10` 流水、总计 50 分；衣柜只写回同一 5 人，第 6 人为 0 分 |
| pending | 8 条选中来源全部保留为 `approved` |
| 故障回滚 | 在积分写入点注入失败后，正式服装、pending、贡献、积分和衣柜全部回滚 |
| 旧事实门禁 | 已批准但缺少完整 DB-4 事实的旧记录拒绝静默补写 |
| fixture rollback | 合成 auth users、服装和 pending 均恢复为 0；持久贡献与积分仍各为既有 1 条，DB-4 新增为 0 |

### 2026-07-21 人工验收账号准备

- development Auth 中未发现既有 `db4-acceptance-user-*` 专用账号后，通过项目公开注册入口创建 2 个独立账号；邮箱确认仅对这两个精确测试邮箱执行，未查询、重置或修改任何既有用户凭据。
- 两个账号均已完成真实密码登录；对应 `profiles.role = 'user'`、`role_level = 0`，登录态调用 `is_admin_or_super_admin()` 均返回 false。
- 本步骤只形成 Auth 用户、email identity 和普通 profile；当前未生成 `user_quotas` 行，也未创建 pending、正式服装、衣柜、贡献或积分。密码不写入仓库、PR、数据库变更记录或命令日志。
- 两个账号保留到 DB-4 人工验收完成。清理属于新的数据库删除操作，需另行确认精确账号及其 pending / 贡献引用；不得为清理测试账号删除贡献或积分审计事实。

### 2026-07-21 人工验收结果

- 负责人已明确确认 PR #91 Preview 仲裁操作成功。两组普通测试账号提交同一件 `DB4验收-0721-A`（连衣裙，短编号 `0721`），development pending `53 / 54` 最终均为 `approved`。
- development 回读确认正式服装 `custom_1784641726927` 已入库，形成 2 条 `admin_arbitration` 贡献、2 条各 `+10` 积分流水（合计 20 分）和 2 份提交者衣柜写回；五类事实一致，DB-4 人工验收通过。
- 本次测试选择“无关联套装（纯散件）”；pending 与正式服装均正确保存为 `suit_id = null`、`temp_suit_name = null`。审核表单没有把该双空事实还原为明确的“纯散件”状态，而显示为空；该问题作为独立前端缺陷登记，不改变 DB-4 通过结论。
- 上述 pending、正式服装、贡献、积分和衣柜属于 development 验收审计链，默认保留；清理或反向积分仍需单独授权。

### Advisor、风险与 rollback

- Security Advisor 未出现 DB-4 mutable search path、anon execute 或底表授权扩大；authenticated `SECURITY DEFINER` 提示为预期 API 面，函数内 admin 校验已由普通用户拒绝测试覆盖。
- Security Advisor 继续报告 `stages`、`suits` 未启用 RLS，以及既有宽松 policy / 旧函数提示；这是独立安全风险，不在本 migration 混改。
- Performance Advisor 多次因连接器 HTTP 传输失败，未取得结果，明确记为“未完成”，不写成已通过；以 live catalog、稳定锁顺序、短事务和约束幂等检查补充本次范围证据。
- rollback 必须新增 patch migration，恢复 `20260627143000_add_admin_arbitration_rpc.sql` 的旧 RPC 定义与 execute grant；不改写已应用 migration 历史。
- 若已产生错误积分，先停止仲裁入口，不删除贡献或流水，按原流水追加等额反向记录；保留来源 pending 和审计链。production 未执行，无 production rollback 动作。

## 2026-07-21 12:50 - db3_integrate_existing_field_completion

### 基本信息

| 项目 | 内容 |
| --- | --- |
| 变更类型 | RPC / contribution facts / points ledger / verification |
| 目标环境 | Supabase development `tfwejruvdahonacyldrg` |
| migrations | `20260721042832_db3_integrate_existing_field_completion`、`20260721044338_db3_fix_retry_array_matching` |
| 关联方案 | `docs/planning/开发文档.md` 第 0.3.3 节、`docs/planning/技术实现文档.md` 第 3.2～3.3 节 |
| production | 未操作 |

### 变更与影响

- 只替换 `complete_existing_clothes_from_pending(...)`，不改参数、返回类型或前端调用方式；函数继续只允许登录角色调用，并在函数内校验 admin / super_admin。
- 正式库确有空字段时，按来源 `created_at`、pending ID、用户 ID 稳定排序；同一用户只取最早来源，最多 5 位真实用户。
- 同一事务补全正式库、写入 `existing_field_completion` 贡献、每人追加 5 分流水、写回同一批用户衣柜，并将全部选中 pending 保留为 `approved`。
- 事件 ID 由正式服装 ID 与排序后的 pending IDs 稳定派生；重复请求只在贡献、积分、衣柜和 pending 事实完全一致时返回幂等成功，不自动回填旧的已批准记录。
- 函数改为 `SECURITY DEFINER` + 空 `search_path` + 全限定对象；撤销默认执行权限后只授予 `authenticated`、`service_role`。DB-1 底表 grants / RLS / policy 未放宽。

### 前检、执行与异常记录

| 检查项 | 结果 |
| --- | --- |
| development link | `scripts/check-dev-link.mjs` 通过，project ref 为 `tfwejruvdahonacyldrg` |
| apply 前事实 | `clothing_contributions = 0`、`points_ledger = 0`；旧 RPC 为 `search_path=public`，旧定义已由 live `pg_get_functiondef` 与 migration `20260701093000` 留存 |
| migration dry-run | 首次仅列出 `20260721042832`；修正后仅列出 `20260721044338` |
| EOF 处理 | 第一次 apply 返回 `unexpected EOF`；先用独立 public schema dump 确认远端仍为旧函数，再安全重试并成功，不盲目重复执行 |
| patch 原因 | 首轮事务测试发现 schema-qualified 多数组 `unnest` 不可用；保留已应用 migration，新增 patch 改用 `generate_subscripts` |
| migration 状态 | Local / Remote 均包含 `20260721042832` 与 `20260721044338` |
| schema / types | development public schema dump 与 TypeScript types 已刷新；RPC 签名未变化 |

### 人工验收数据

- development 验收来源：pending ID `23`，名称 `Codex草稿验收-20260713-231920`，分类 `连衣裙`；正式服装 ID `db3_acceptance_20260721_01`。
- 修复版 Preview 真实复验后，pending 为 `approved`，正式服装已补全游戏 ID、五星和标准 10 属性分值；写入 1 条 `existing_field_completion` 贡献、1 条 `+5` 积分流水，并将正式服装写回提交者衣柜。
- 贡献、积分和衣柜事实属于已完成的 development 验收审计链，不删除或改写；是否清理夹具需单独确认，错误积分只能追加反向流水。

### 2026-07-21 人工验收异常与修正

- 负责人在 PR #87 Preview 中连续三次触发补全，API 均返回 `400`；Postgres 原始错误为“存在与本次正式库补全最终数据不一致的待审核记录，已拒绝自动通过”。三次失败均整体回滚，无贡献、积分、衣柜或 pending 半完成数据。
- 根因不在 RPC 权限或事务：提交端以 10 属性 JSON 保存 pending，审核端只重建 5 个非零属性键；数值语义相同但 JSON 结构不同，触发数据库严格全等核对。
- 修正仅统一提交端与审核端的前端标准分值构造，并补充真实 10 属性回归测试；本次不替换 RPC、不新增 migration、不放宽数据库一致性或权限门禁。
- 使用同一 pending `23`、正式服装 `db3_acceptance_20260721_01` 和真实管理员身份执行 development 事务 rehearsal：临时生成 1 条贡献、1 条 `+5` 流水、1 次衣柜写回并将 pending 通过，断言全部通过后整体 rollback；回读恢复为 pending、空正式服装、贡献 0、积分 0、衣柜无夹具。
- 修复版 Preview 复验已通过且负责人确认页面操作成功；最终回读为 pending `approved`、正式服装已补全、贡献 1、积分流水 1 条且合计 `+5`、衣柜包含该服装，未发现半完成状态；production 未操作。

### 角色、业务与事务验证

`supabase/tests/db3_existing_field_completion.sql` 使用合成账号、2 件测试服装和 9 条 pending，在单一 development 事务中验证后 rollback：

| 场景 | 结果 |
| --- | --- |
| 普通用户 | RPC 拒绝；不能直接读写 `clothing_contributions` / `points_ledger` |
| admin / super_admin | admin 首次补全成功；super_admin 使用逆序 pending IDs 重试返回幂等成功 |
| 前 5 规则 | 6 位真实用户、1 条同用户重复来源和 1 条匿名来源中，仅按最早时间去重后的前 5 位形成贡献 |
| 积分与衣柜 | 5 条贡献、5 条各 `+5` 流水、总计 25 分；衣柜只写回同一 5 人，第 6 人与匿名来源不获奖励 |
| 正式库 / pending | 只补空字段，既有非空标签与临时套装名保留；8 条选中 pending 均为 `approved` |
| DB-2 汇总 | 获奖用户读到 5 分，第 6 位用户读到 0 分 |
| 故障回滚 | 在积分写入点注入失败后，服装、pending、贡献、积分和衣柜均无半完成状态 |
| fixture rollback | 合成 auth users、clothes、pending 均为 0；两张 DB-1 事实表总行数恢复为 0 |

### Advisor、风险与 rollback

- Security Advisor 未出现 DB-3 mutable search path、公开执行或底表授权扩大；`complete_existing_clothes_from_pending` 的 authenticated `SECURITY DEFINER` 提示为预期 API 面，函数内 admin 校验已由普通用户拒绝测试覆盖。
- Performance Advisor 无 DB-3 新项；`stages` / `suits` RLS、旧函数、旧 policy、既有索引提示保持原范围，不在本 PR 混改。
- rollback 必须新增 patch migration，恢复 `20260701093000_fix_complete_existing_clothes_empty_field_rules.sql` 的旧 RPC 定义和旧 grant；不改写 migration 历史。
- 如已产生错误积分，不删除贡献或流水，按原流水追加等额反向记录；production 未执行，无 production rollback 动作。

## 2026-07-17 13:18 - db2_create_read_surfaces

### 基本信息

| 项目 | 内容 |
| --- | --- |
| 变更类型 | schema / function / view / grants |
| 目标环境 | Supabase development `tfwejruvdahonacyldrg` |
| migration | `20260717050640_db2_create_read_surfaces` |
| production | 未操作 |

### 变更与影响

- 新建非 exposed schema `private_db2`，包含 `current_user_points()` 与 `public_initial_contributors()` 两个只读 `SECURITY DEFINER` helper；均为 `STABLE`、固定空 `search_path`、全限定引用且无 DML。
- 新建 `public.user_points_summary` 与 `public.clothing_contributors_public` 两个 `security_invoker + security_barrier` 不可写 view。
- `user_points_summary` 仅向 authenticated 授予 SELECT，只返回 `auth.uid()` 对应的 `total_points`；admin / super admin 不获得跨用户积分查询。
- `clothing_contributors_public` 向 anon / authenticated 授予 SELECT，只返回初始入库稳定前 3 的安全字段；补全贡献不公开，账号删除显示“已注销用户”。
- DB-1 两张底表的 RLS、policy、grants、数据和索引均未修改；现有 RPC、前端与历史记录未修改。

### 前检、执行与失败记录

| 检查项 | 结果 |
| --- | --- |
| development link | `scripts/check-dev-link.mjs` 通过，project ref 为 `tfwejruvdahonacyldrg` |
| exposed schemas | Data API 对 `private_db2` 返回 `PGRST106 / 406`；仅 `public, graphql_public` 暴露 |
| migration / 对象冲突 | apply 前 DB-2 schema、helper、view 均不存在；两张事实表均 0 行 |
| rehearsal | development 事务内完整创建、目录断言后 rollback 通过 |
| CLI dry-run | 两次均未写入：一次 migration rows `unexpected EOF`，一次 HTTPS DNS 无有效 IP；停止 CLI 重试 |
| apply | Supabase 连接器成功应用单一 migration，远端 version `20260717050640` |
| schema / types | public schema dump 与 development types 已刷新；private schema 以 migration 和 live catalog 为权威 |

- expanded `public,private_db2` dump 受已知连接池假 IP / EOF 影响失败；失败产生的空本地文件已从 Git 基线恢复，并按成功 public dump 的实际差异复核，未提交空文件或临时产物。
- Security / Performance Advisor 首次并发请求发生 transport error；改为逐项执行后成功。DB-2 无新增 WARN / ERROR；既有 `stages` / `suits`、旧函数与旧 policy 告警不在本次范围。

### 角色矩阵与事务 fixture

使用现有 development 隔离角色账号和测试服装 `db2_fixture_20260717_01`，在单一短事务中验证后 rollback：

| 角色 / 场景 | 结果 |
| --- | --- |
| anon | 可读公开贡献者稳定前 3；读取积分返回权限拒绝；private schema 不可访问 |
| 普通用户 | `+10 / -3` 汇总为 7，只能读取自己的积分；不能读取 `points_ledger` 底表 |
| admin | 只读取自己的 20 分，不获得跨用户能力 |
| super admin | 只读取自己的 30 分，不获得跨用户能力 |
| 公开展示 | 补全贡献排除；username / 稳定匿名代号与“已注销用户”显示正确 |
| rollback 回读 | 测试 clothes、pending、contribution 均 0；两张 DB-1 事实表总行数均恢复为 0 |

### Advisor 与 rollback

- Security Advisor 中 DB-1 仅保留 2 项预期 `RLS Enabled No Policy` INFO；DB-2 helper 未出现 exposed `SECURITY DEFINER` 或 mutable search path 告警。
- Performance Advisor 没有 DB-2 新项；现有空表 unused index 与旧 policy 告警保持原状。
- rollback 必须新增 migration：先撤销并删除 `public.user_points_summary`、`public.clothing_contributors_public`，再删除两个 `private_db2` helper；仅在 schema 为空且无依赖时删除 `private_db2`。不改写 migration history，不修改或删除 DB-1 数据。

## 2026-07-17 08:06 - db1_merge_and_development_recheck

### 基本信息

| 项目 | 内容 |
| --- | --- |
| 变更类型 | merge verification / development read-only recheck |
| 目标范围 | GitHub `develop` / Supabase development `tfwejruvdahonacyldrg` / Vercel Preview |
| PR / merge commit | PR #80 / `4965c1dce0f008d37a1f38426b5ae21edcafb8e9` |
| production | 未操作 |

### 合并与远程状态

- PR #80 于 2026-07-17 07:52（北京时间）合入 `develop`；GitHub、`origin/develop` 与本地 `develop` 均指向 merge commit `4965c1d`。
- merge commit 与 PR head `39a6645` 的 Git tree 相同，没有合并时新增的文件差异。
- merge commit 对应的 Vercel Preview 起初受 Vercel 官方“GitHub-linked deployments and authentication affected”事件影响停在 `INITIALIZING`，未产生项目 build 错误；08:04 恢复为 `READY`，alias 无错误，GitHub 的 Vercel 与 Preview Comments 检查均为 success。

### development 数据库复查

| 检查项 | 结果 |
| --- | --- |
| 项目状态 | `tfwejruvdahonacyldrg` / `nikki-calculator-dev`，`ACTIVE_HEALTHY`，Postgres 17 |
| migration history | `20260716132522_db1_create_contributions_points_base_facts` 已记录 |
| 数据行数 | `clothing_contributions` 与 `points_ledger` 均为 0 行 |
| 结构 | 每表 9 个字段；共 17 个约束、11 个索引 |
| RLS / Policy / Trigger | 两表 RLS 已启用，policy 与 trigger 均为 0 |
| 角色权限 | `anon` / `authenticated` 无直接表权限；`service_role` 仅 `SELECT / INSERT` |
| 既有函数链 | `submit_clothing_contribution`、`approve_pending_clothes_arbitration`、`add_clothes_to_submitter_wardrobes`、`is_admin_or_super_admin` 均存在 |

- `supabase migration list --linked` 首次只读回读在申请临时登录角色时因远端 `EOF` 失败，并提示当前非交互 shell 无数据库密码；未输出、保存或重新索取密码，也未产生数据库写入。随后通过已授权 Supabase 连接器成功回读同一 development 项目的 migration history 与 live catalog。
- Security Advisor 对 DB-1 仅有 2 项预期的 `RLS Enabled No Policy` INFO；Performance Advisor 仅有 2 项空表未使用索引 INFO。DB-1 无新增 WARN / ERROR，其他告警均属于既有对象。

### 生成物、自动验证与结论

- `npm run db:types:safe` 通过，重新生成的 `src/types/supabase.ts` 与 merge commit 内容哈希一致。
- `node --test tests/*.test.js` 4 / 4 通过；`npm run build` 通过。
- DB-1 合并后 development 复查全部通过，可以正式收口并进入独立 DB-2 最小只读面的设计 / 只读预审；DB-2 migration、Supabase 写入和 PR merge 仍需分别取得明确授权。
- DB-1 rollback 不变：仅在两表为空且无下游依赖时新增 rollback migration，先删除 `points_ledger`，再删除 `clothing_contributions`；否则停止，不直接删表，不改写已应用 migration 历史。

## 2026-07-16 22:35 - db1_create_contributions_points_base_facts

### 基本信息

| 项目 | 内容 |
| --- | --- |
| 变更类型 | schema / RLS / grants / index |
| 目标环境 | Supabase development `tfwejruvdahonacyldrg` |
| migration 文件 | `supabase/migrations/20260716132522_db1_create_contributions_points_base_facts.sql` |
| 业务边界 | 仅建立两张空事实表；不接入 RPC、view、trigger、前端或历史回填 |
| production | 未操作 |

### 变更目的与范围

- 建立 `clothing_contributions` 与 `points_ledger`，为后续可追溯、可重算的贡献和积分链提供最小事实层。
- 两表均启用 RLS 且不创建 policy，默认拒绝客户端访问；`service_role` 仅保留 `SELECT / INSERT`。
- 不修改现有服装提交、审核入库、衣柜写回 RPC 和业务数据，不创建 `clothes` 业务唯一索引，不进入 DB-2。

### 预检与执行

| 检查项 | 结果 |
| --- | --- |
| development 项目 | `tfwejruvdahonacyldrg`，`ACTIVE_HEALTHY` |
| 迁移基线 | 本地与远端原有 13 个 migration 一致，目标对象不存在 |
| 引用类型 | `clothes.id = varchar`、`pending_clothes.id = bigint`、`auth.users.id = uuid` |
| 影响现有数据 | 0 行；无批量更新、RPC 替换或历史回填 |
| 事务预演 | 完整 migration 以 `ROLLBACK` 预演通过，目标对象未残留 |
| dry-run | 仅计划应用 `20260716132522` 一个 migration |
| development apply | 第一次因连接 `unexpected EOF` 失败；确认 migration history 与目标表均未写入后安全重试，第二次成功 |

### development 后检

| 检查项 | 结果 |
| --- | --- |
| migration history | `20260716132522` 已记录 |
| 数据行数 | 两表均为 0 行 |
| 结构 | 每表 9 个字段；共 17 个约束、11 个索引，与 migration 一致 |
| 外键索引 | 6 个外键列均有可用索引或唯一索引 |
| RLS / Policy | 两表 RLS 已启用，policy 数均为 0，符合默认拒绝设计 |
| anon / authenticated | 两表均无 `SELECT / INSERT / UPDATE / DELETE` |
| admin / super_admin | 使用 `authenticated` 数据库角色，不能直接操作两表 |
| service_role | 两表仅有 `SELECT / INSERT`，无 `UPDATE / DELETE` |

约束矩阵在单一事务内覆盖有效贡献、正向奖励、扣回，以及来源重复、事件排名重复、事件用户重复、初始奖励重复、非法类型 / 排名、零积分、非法状态 / 流水形态、重复奖励 / 扣回和父记录删除受限；所有预期成功与失败均符合设计，事务回滚后两表仍为 0 行。

### Advisor、生成物与回归

- Security / Performance Advisor 已通过 CLI 完整返回：两张新表仅有 2 项预期的 `RLS Enabled No Policy` INFO，以及 2 项因空表尚无查询产生的 `Unused Index` INFO；没有 DB-1 新增 WARN / ERROR，全部外键均有覆盖索引。其他告警均为既有对象，不属于本次范围。
- `npm run db:types:safe` 成功，`src/types/supabase.ts` 已生成两表类型；`node --test tests/*.test.js` 4 / 4 通过，`npm run build` 通过。
- `docs/database/schema.md` 已按 live catalog 更新；完整 dump 最终通过 development session pooler 的真实 IPv4 生成，密码仅在交互窗口内存中使用。`supabase/schema.sql` 仅新增 131 行 DB-1 内容，SHA-256 为 `FD068E9FB20B460C0E4CA1A179B2C60DE85AB3CC2AC8A26FAC0EE778735EC8CB`，没有既有对象差异。

### 结论、风险与 rollback

- DB-1 已在 development 应用并完成结构、权限、约束、Advisor、生成物和基础回归验证；独立 database PR 可转为 ready，merge 仍需用户再次明确确认，合并前不进入 DB-2。
- 本阶段没有 UI 或业务写入入口，不要求用户执行页面业务验证；从 DB-3 需要业务验证时，先提供完整说明文档和操作步骤，再等待用户反馈。
- 如需回退且两表仍为空、无下游依赖，新增 rollback migration，先删除 `points_ledger`，再删除 `clothing_contributions`；若出现数据或依赖则立即停止，不直接删表。不得改写已应用 migration 历史。
- production 未操作；development 项目 ref、验证结果、失败记录和测试记录均保留。

## 2026-07-16 13:33 - db0_merge_and_development_smoke

### 基本信息

| 项目 | 内容 |
| --- | --- |
| 变更类型 | merge verification / development smoke |
| 目标环境 | GitHub `develop` / Supabase development `tfwejruvdahonacyldrg` / Vercel Preview |
| 关联 PR | PR #76 database；PR #77 business |
| production | 未操作 |

### 合并与验证结果

- PR #76 先合并，merge commit 为 `da7d6ce91f0850901a1cc2e5267b85f4dc91d95e`；PR #77 后合并，merge commit 为 `51368186aab6a1fcdc6cf981ee175ee4e09594f4`。
- 两个 merge commit 均已确认属于 `origin/develop`；最新 merge commit 的 Vercel deployment 与 Preview Comments 状态均为 success。
- 最新 `develop` 上 `node --test tests/*.test.js` 4 / 4 通过，`npm.cmd run build` 通过。
- development 后检：migration `20260714061353` 存在；`pending_clothes` RLS 已启用、3 条 policy 存在、anon 无表访问。
- 测试申请 `27 / 28 / 29` 仍为 `rejected / rejected / approved`；正式服装与普通提交者衣柜写回仍存在。
- 页面完成数据加载；普通提交者衣柜显示 `1/72` 和测试服装 `#99071501`；浏览器控制台无 warning / error。
- Security Advisor 专用接口仍返回连接层传输错误；沿用合并前已取得的 16 项明细，并以实时 catalog、角色矩阵和函数守卫复查作为 DB-0 等价证据。

### 结论与 rollback

- DB-0 已完成，可以进入 DB-1 的独立审定与授权阶段；本记录不代表已授权 DB-1 implementation。
- PR #76 如需回退必须新增 rollback migration；PR #77 可独立 revert。测试账号和测试记录继续保留，清理需另行授权。

## 2026-07-15 19:27 - db0_development_business_validation_seed

### 基本信息

| 项目 | 内容 |
| --- | --- |
| 变更类型 | development test seed / Auth / business validation |
| 目标环境 | development：`tfwejruvdahonacyldrg` |
| migration 文件 | 无 |
| 关联 PR | database draft PR #76；business draft PR #77；测试记录更新在 docs PR #75 |
| 执行人 | Codex |
| 确认人 | 用户授权直接新增测试用户并简化流程 |

### 目的与范围

- 创建普通用户、管理员、最高站长三个隔离测试账号，不修改现有角色账号。
- 使用测试服装 `DB0验证服装-202607151910`（连衣裙，短编号 `99071501`）验证“提交 → 驳回 → 重提 → 入库 → 衣柜写回”链路。
- 只在 development 写入测试数据；无 schema、migration、RPC、RLS、production 或 DB-1 变更。

### 测试账号

| 角色 | Auth user id | 结果 |
| --- | --- | --- |
| 普通用户 | `a8838d2e-4910-46c4-821e-f4aa0eb1e9d5` | Auth 登录成功；`role=user`、`role_level=0` |
| 管理员 | `f4cdaa70-ad52-4b2f-acd3-bda69824c846` | Auth 登录成功；`role=admin`、`role_level=1` |
| 最高站长 | `04083261-a475-4dcb-b906-6e2802b09022` | Auth 登录成功；`role=super_admin`、`role_level=2`；查看全部申请并完成状态操作 |

- 三个账号均通过 Supabase Auth 创建，profile 由既有触发器生成；没有直接 `INSERT auth.users`。
- 邮箱确认仅更新对应三条新 Auth 用户；密码未写入仓库，测试完成后已随机轮换并注销旧会话。

### 执行与验证结果

| 角色 / 步骤 | 结果 | 页面提示或数据证据 | 北京时间 |
| --- | --- | --- | --- |
| 普通用户登录 | 通过 | 本地页面顶部显示对应 development 测试账号 | 2026-07-15 19:10 |
| 普通用户提交 | 通过 | 图鉴缺失 1；完善资料后提交成功 | 2026-07-15 19:14 |
| 普通用户 RLS | 通过 | 可见总数 1、自己的 1、他人的 0；测试记录状态 `pending` | 2026-07-15 19:15 |
| 管理员登录 | 通过 | 出现“图鉴管理”入口 | 2026-07-15 19:22 |
| 管理员查看全部 | 通过 | “全部 2”，包含测试服装和既有 pending | 2026-07-15 19:24 |
| 管理员首次驳回 | 通过 | PR #77 页面出现“驳回当前服装申请（1 条）”；确认后提示成功并清空选择 | 2026-07-15 21:54 |
| 普通用户首次重提 | 通过 | 同一服装重新提交，生成 `pending_clothes.id = 28` | 2026-07-15 21:55 |
| 最高站长查看全部 | 通过 | 页面显示“全部 2”，包含测试服装和既有 pending | 2026-07-16 12:43 |
| 最高站长状态操作 | 通过 | 确认并驳回测试服装第二次申请；结果提示出现，选择清空 | 2026-07-16 12:44 |
| 普通用户再次重提 | 通过 | 同一服装再次提交，生成 `pending_clothes.id = 29` | 2026-07-16 12:45 |
| 管理员审核入库 | 通过 | 选择“无关联套装”后完成一键入库，测试候选从列表移除 | 2026-07-16 12:47 |
| 提交者衣柜同步 | 通过 | 普通用户“我的衣柜”显示测试服装，总收集 `1/72` | 2026-07-16 12:49 |
| 数据与 DB-0 复查 | 通过 | pending 为 `rejected / rejected / approved`；正式 ID `custom_1784177220092`；衣柜同步 `true`；RLS 与 RPC 角色矩阵保持 | 2026-07-16 12:50 |

### 最终状态

- 测试服装保留 3 条申请历史：`id = 27 / 28 / 29`，状态依次为 `rejected / rejected / approved`。
- 正式 `clothes` 保留 1 条：`custom_1784177220092`；普通提交者衣柜已包含该 ID。
- 三个隔离测试账号和全部测试记录按用户要求保留；账号密码已最终随机轮换并注销旧会话。
- production 未触碰，未进入 DB-1。

### Rollback / 清理

- 当前不清理测试账号、pending 历史、正式服装或衣柜引用；任何清理需用户另行授权。
- 如后续授权清理，顺序为：停止测试会话 → 移除衣柜引用 → 删除正式服装 → 删除 3 条 pending → 通过 Supabase Auth 管理入口删除三个固定 user id → 复查 profile 级联删除。
- 删除 Auth 用户不会立即使已签发 JWT 失效；清理时必须同时结束会话，并等待现有 token 过期后再视为严格失效。
- 清理前按固定 user id、服装名和短编号预检影响行数；任一数量不符合预期时停止，不执行扩大范围的删除。

## 2026-07-14 14:28 - db0_secure_pending_clothes_access

### 基本信息

| 项目 | 内容 |
| --- | --- |
| 变更类型 | RLS / function grants / index |
| 目标环境 | development：`tfwejruvdahonacyldrg` |
| migration 文件 | `supabase/migrations/20260714061353_db0_secure_pending_clothes_access.sql` |
| 业务边界 | 仅 DB-0；不创建贡献、积分、陪审团表，不进入 DB-1 |
| 数据影响 | 不修改现有业务数据 |

### 变更目的

- 让待审核服装申请只对正确的提交人和审核角色可见、可操作。
- 阻止匿名请求、普通登录用户直接调用内部高权限函数。
- 保留普通用户提交申请、管理员审核入库和最高站长管理的既有业务能力。

### 变更内容

- 为 `pending_clothes` 启用 RLS：普通用户只能提交、查看自己的申请；管理员和最高站长可查看全部并更新审核状态。
- 移除匿名角色的表、序列和高权限 RPC 权限；登录角色只能读取、提交及更新 `status`。
- 审核入库、补全正式库等对外 RPC 保留登录调用，但继续在函数内校验管理员身份。
- 衣柜同步、额度扣减、用户初始化及角色同步等内部函数仅允许 `service_role` 直接执行。
- 为 `pending_clothes.submitted_by` 新增索引，并为两个内部函数固定 `search_path = public`。

### 执行与数据核对

| 检查项 | 结果 |
| --- | --- |
| 执行前数据 | 2 条：`pending` 1 条、`approved` 1 条 |
| 事务演练 | migration 全量在事务内执行并通过断言，随后 `ROLLBACK`；旧状态恢复成功 |
| development 应用 | 已应用；远程 migration 版本为 `20260714061353` |
| 应用过程异常 | 写入接口返回传输错误；未盲目重试，改用只读 migration list 和 catalog 核实已成功落库 |
| 执行后数据 | 仍为 2 条：`pending` 1 条、`approved` 1 条 |
| 角色数据 | 管理员 1 个、最高站长 1 个，测试后未改变 |
| schema dump | 成功；首次因 Docker 未启动失败，启动后一次 SSL EOF，重试成功 |
| types 生成 | `npm.cmd run db:types` 成功，目标为 development |
| 前端 build | `npm.cmd run build` 成功 |

### 角色矩阵

| 角色 | 业务动作 | 结果 |
| --- | --- | --- |
| 匿名访客 | 查看待审核申请 | 拒绝，`42501` |
| 匿名访客 | 调用管理员审核 RPC | 拒绝，`42501` |
| 普通用户 | 提交自己的 `pending` 申请 | 允许；测试事务已回滚 |
| 普通用户 | 查看自己的申请 | 允许 |
| 普通用户 | 查看他人申请 | 不可见 |
| 普通用户 | 代他人提交或伪造 `approved` 状态 | 拒绝，`42501` |
| 普通用户 | 修改审核状态、删除申请、调用内部衣柜同步函数 | 拒绝或影响 0 行 |
| 管理员 | 查看全部申请、更新审核状态 | 允许；2 条可见，测试更新 1 条后已回滚 |
| 管理员 | 调用审核入库 RPC | 权限检查通过，并进入“服装 ID 不能为空”的业务参数校验 |
| 最高站长 | 查看全部申请、更新审核状态 | 允许；2 条可见，测试更新 1 条后已回滚 |
| 最高站长 | 调用正式库补全 RPC | 权限检查通过，并进入“正式库服装 ID 不能为空”的业务参数校验 |
| `service_role` | 表操作及内部函数 | catalog 权限检查通过 |

### Advisor 复查

- Security Advisor：由 35 项（4 ERROR / 31 WARN）降至 16 项（2 ERROR / 14 WARN）。DB-0 目标项已消失：`pending_clothes` 不再报 RLS 缺失，内部 `SECURITY DEFINER` 函数不再对匿名或普通登录角色开放，`deduct_user_quota` 与 `handle_new_user_quota` 的 mutable search path 告警已消失。
- 剩余 2 个 ERROR 为既有 `stages`、`suits` 未启用 RLS；其余告警属于 `pending_suits`、`app_errors`、其他函数和 Auth 配置，不在本次范围。
- Performance Advisor 专用接口连续返回连接层传输错误。已用 development catalog 对 DB-0 相关检查逐项复核：`submitted_by` 外键已由新索引覆盖；RLS 中 `auth.uid()` 使用 init-plan 写法；同一角色与动作没有重复 permissive policy。既有 `pending_clothes.suit_id` 外键仍无索引，属于本次前已存在的 INFO 级事项，不在 DB-0 扩项处理。

### 业务验证说明

验证文档：本记录的“业务验证说明”；业务入口与预期以 `docs/requirements/需求文档.md` 为准。

环境与前提：

- 只使用 development / preview，不进入 production。
- 准备 1 个普通用户账号、1 个管理员账号；最高站长可按相同步骤复查管理员能力。
- 使用一条不会与正式服装库重复的测试服装信息，并记下名称、分类和短编号。

操作步骤：

1. 普通用户登录，提交一条缺失服装申请；预期页面提示提交成功。
2. 普通用户查看自己的申请；预期能看到刚提交的记录，看不到其他用户的待审核记录。
3. 退出后以管理员登录，打开待审核列表；预期能看到该申请及其他待审核申请。
4. 管理员先执行一次驳回；预期状态更新成功，列表可正常刷新。
5. 普通用户重新提交测试申请，管理员按既有流程完成审核入库；预期正式服装库出现记录，提交人的衣柜同步成功。
6. 最高站长登录，确认可查看全部待审核记录并执行一次状态操作；预期能力与管理员审核路径一致。
7. 完成后记录每一步的账号角色、时间、页面结果和异常提示；若失败，不重复提交或直接改库，先反馈。

反馈格式：

```text
环境：development / preview
角色：普通用户 / 管理员 / 最高站长
步骤：1-7 中的编号
结果：通过 / 失败
实际提示：
测试服装名称与短编号：
发生时间（北京时间，精确到分钟）：
是否已停止后续操作：是 / 否
```

清理：验证通过后，由管理员删除或标记测试记录；如页面没有安全清理入口，保留记录并反馈，不直接执行 SQL。

### 回滚方案

- 当前仅应用于 development，且没有业务数据变更；无需恢复数据快照。
- 若业务验证失败，停止后续验证和 DB-1，创建新的 rollback migration，不修改已应用 migration 历史。
- rollback migration 将恢复变更前的 `pending_clothes` policy、表/序列 grant 和函数 grant，移除本次索引，并撤销本次函数 `search_path` 设置；该操作会重新引入已知安全风险，只用于 development 故障隔离，执行前必须再次确认范围。
- production 未执行，无 production rollback 动作。

## 2026-07-06 23:01 - pr17_contributions_points_schema_audit

### 基本信息

| 项目 | 内容 |
| --- | --- |
| 变更类型 | docs / read-only audit |
| 目标环境 | 无数据库执行；仅记录 PR #17 审计结论 |
| migration 文件 | `supabase/migrations/20260705145500_create_contributions_points_schema.sql` |
| 关联 PR | PR #17：数据库：新增贡献与积分 schema 草案 |
| 执行人 | Codex |
| 确认人 | 待用户确认 |

### 审计结论

- PR #17 是 database migration 草案，当前不应按现状继续 merge。
- PR #17 当前 base 为 `main`，不符合 database migration 先在 `develop` / development Supabase 验证的治理流程。
- 当前建议将 PR #17 标记为 blocked / paused。
- 下一步应先处理 Supabase 安全 / RLS / `SECURITY DEFINER` advisor 风险，再推进贡献与积分 schema。
- 如后续继续该 schema，应关闭当前 PR 后从 `develop` 重开，或 retarget 到 `develop` 后保持 blocked，并拆分多个 database PR。

### 主要风险

- `clothes(category, game_id)` partial unique index 可能因现有重复键导致 migration 失败，必须先在 development 做重复数据预检。
- `security_invoker` view + revoke 底层表权限可能导致 `clothing_contribution_public_view` / `get_clothing_contributors` 对 anon / authenticated 不可读，需要单独验证。
- `clothing_contributions` 未保存 `source_pending_id`，会影响贡献来源追溯、rollback 和争议处理；后续设计需补齐或明确放弃。
- 当前仓库 schema 摘要仍记录部分 grant 较宽、`pending_clothes` RLS 状态需复查；积分 schema 不宜叠加在未收口的安全面上。
- 单个 migration 同时包含唯一索引、基础表、RLS / grant、public view 和 RPC，风险面过大，应拆分。

### 必须补齐的前置项

- Supabase advisor 风险梳理：RLS、table grant、function grant、`SECURITY DEFINER`。
- development 重复键预检：`clothes(category, game_id)`。
- development SQL / RLS / RPC 验证记录。
- rollback 方案。
- 是否需要 `source_pending_id` 的产品和审计口径确认。
- PR 拆分方案：index / base tables / RLS-grants / views-RPC / write RPC integration / backfill。

### 执行结果

| 项目 | 结果 |
| --- | --- |
| 数据库命令 | 未执行 |
| Supabase 命令 | 未执行 |
| psql / SQL | 未执行 |
| migration apply | 未执行 |
| production | 未触碰 |
| Vercel | 未触碰 |

### 回滚方案

- 本次仅记录文档，无数据库 rollback 需求。
- 如本记录需要修正，追加 docs-only 修正 commit。
- 如未来该 migration 已在 development 执行，应通过新增 patch migration 修正，不原地改已执行 migration。
