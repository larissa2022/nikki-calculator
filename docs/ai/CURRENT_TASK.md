# 当前任务看板

> 最后更新：2026-07-21 16:23（北京时间）

## 当前任务

- 减少工作流中的重复查询、重复读取和无效重试，并为每个独立任务补齐统一废弃数据收尾检查。

## 为什么做

- 本次任务出现远端状态重复核对、等待决策期间重复查询、长文档重复读取和删除命令重复尝试；任务结束时也缺少数据库、分支和文件三类统一收尾入口。

## 已经完成

- 已加入“一次确认、按事件刷新”的远端事实复用规则，以及明确的“等待负责人决定”停止状态。
- 已加入长文档渐进读取、工具拒绝分类与单次等价路径切换规则，避免重复加载和同类命令重试。
- 已建立数据库、任务分支和临时文件三类统一收尾清单，并保留原有敏感操作单独授权门禁。

## 当前进度

- 完成阶段：流程优化已写入现有工作流，未新增入口或修改强规则，可以进入下一任务。

## 下一步

- 负责人提出下一项业务目标；新任务按优化后的证据刷新和统一收尾规则执行。

## 需要负责人决定

- 不需要决定。

## 通用边界

- 不修改 `RULES.md`、业务代码、数据库、配置、`main` 或 production。
- 统一收尾不自动扩大权限；数据库写入、PR merge 和分支删除仍需单独确认。

## Rollback

- 如流程优化不符合预期，revert 本次 docs-only commit；不回退既有强规则或业务实现。
