# 当前任务看板

> 最后更新：2026-07-21 15:08（北京时间）

## 当前任务

- 让管理员补全正式库空字段时，同步形成可追溯的贡献记录和权威积分流水，且不破坏现有审核与衣柜写回流程。

## 为什么做

- 用户中心此前已读取权威积分汇总，但正式库补全尚未写入贡献与积分事实，实际完成有效补全的用户无法获得应有的 5 分。

## 已经完成

- DB-3 已在 development 接入正式库空字段补全：有效提交者按最早时间去重后最多 5 人，每人 5 分，并与衣柜写回、pending 通过保持同一事务。
- 首次真实验收暴露 5 属性与 10 属性 JSON 表示不一致；提交端与审核端现已共用标准 10 属性分值构造，数据库严格一致性门禁未放宽。
- 负责人已确认修复版 Preview 页面操作成功；development 回读为 pending `23` 已批准、正式服装已补全、贡献 1 条、积分 `+5`、衣柜写回均已落库，本地 8 项测试、构建、事务矩阵和 PR 检查均通过。

## 当前进度

- 完成阶段：实现、自动验证和 development 真实业务验收均已完成，DB-3 开发闭环已达到目标，可以进入下一任务。

## 下一步

- 负责人提出下一项业务目标；新任务重新确认目标、范围、验收、环境和 rollback。

## 需要负责人决定

- 不需要决定；如需清理 development 验收夹具，另开独立数据任务确认范围和 rollback。

## 通用边界

- 不操作 `main`、production、Vercel、env，不接入管理员仲裁、自动入库、重审、排行榜或复杂权益。
- 不开放贡献与积分底表客户端权限，不回填历史，不删除或改写既有积分流水。

## Rollback

- RPC 异常时通过新 patch migration 恢复上一版函数定义；不改写已应用 migration 历史。
- 错误积分以反向流水修正，不删除历史；development 测试数据按验证方案回滚或显式清理。
