# 当前任务

当前任务：docs-only 文件治理盘点与入口收口。

## 本次任务边界

- 只盘点和收口 `docs/` 范围内的文档结构。
- 不开发业务功能。
- 不修改数据库、Supabase、Vercel 或构建配置。
- 不操作 `main` / production。
- 不修改 `docs/ai/RULES.md`。

## 当前状态

- 工作分支：`docs/file-governance`。
- 目标 base：`develop`。
- 允许更新当前任务状态、docs 入口、文件治理说明、工作流入口和复盘经验。
- 如发现需要修改 `RULES.md`、数据库安全流程正文或业务 / 配置文件，应记录为未确认事项，不在本任务内越权修改。

## 本次重点

- 明确 `docs/ai`、`docs/governance`、`docs/database` 与 `docs/README.md` 的职责边界。
- 补充 docs 文件治理入口，避免 `RULES.md`、`WORKFLOWS.md`、`CURRENT_TASK.md`、`CODEX_REPORT.md` 混用。
- 检查过期状态与重复入口，并只做 docs-only 收口。
