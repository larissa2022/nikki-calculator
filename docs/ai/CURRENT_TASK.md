# 当前任务看板

> 最后更新：2026-07-20 13:29（北京时间）

## 业务目标

- 减少连续任务中的重复确认、重复回读和短时状态修正，让用户能清楚区分必要门禁、指令歧义与执行遗漏。

## 技术目标

- 将本轮经验收口到 `LESSONS.md`，并在 `AGENTS.md`、`WORKFLOWS.md` 固化停止点、授权检查点、异常回读、看板稳定性和本地链接规则。

## 当前阶段

- 协作复盘与工作流优化已完成，等待用户审阅和决定是否合并文档变更。

## 最近完成

- DB-0、DB-1、DB-2 已在 development 完成并合入 `develop`；PR #82 / #83 / #84 的合并后远端事实已确认。
- 已完成重复工作归因：指令边界、必要治理门禁、执行遗漏、仓库并发与工具传输问题分别记录。

## 下一步任务

- 用户审阅本轮复盘与流程优化；如接受，单独确认合并对应 docs PR。

## 阻塞与待确认

- 不预设新的业务主线；文档 PR merge 仍需用户单独明确确认。

## 通用边界

- 不预设下一条业务主线；新任务开始前重新确认目标、范围、验收、环境和 rollback。
- 不自动操作 `main`、production、database / Supabase、Vercel、env、PR merge、历史改写或分支删除。

## Rollback

- 本轮文档变更如需撤销，关闭对应 docs PR；合并后通过 revert 文档提交恢复。
- DB-2 数据库回退仍须新增 rollback migration，不改写已应用 migration 历史；具体顺序以数据库变更记录为准。
