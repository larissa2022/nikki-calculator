# 当前任务看板

> 最后更新：2026-07-25 22:39（北京时间）

## 当前任务

- 完成积分与前 3 位贡献者的前端展示，让数据库中已经形成的积分和贡献事实对用户可见。

## 为什么做

- DB-1 至 DB-5 已完成积分与贡献事实的写入和安全读取，但此前只有用户中心积分可见，服装贡献者仍没有前端入口。

## 已经完成

- 用户中心继续读取权威积分汇总；新增“贡献名录”，按服装展示最早 3 位有效贡献者，并支持搜索、分页、注销用户、空数据和读取失败状态。
- 已把用户中心的旧投票权重文案改为“一人一票、等级只用于展示”，与已确认产品规则一致。

## 当前进度

- 完成阶段：积分和前 3 贡献者展示已完成自动化测试、构建以及桌面 / 手机页面验证；没有修改数据库、权限、production 或 `main`。

## 下一步

- 建立 `re_review_items` 重审池，承接待补套装、字段冲突、字段缺失和报错转重审。

## 需要负责人决定

- 启动下一项数据库任务时，需要负责人单独授权 development migration 的本地创建与远端应用；production 和 `main` 继续不在范围内。

## 通用边界

- `main`、production、database / Supabase / Vercel 写入、migration、env、PR merge、历史改写、分支删除和 `RULES.md` 修改仍需分别明确确认。

## Rollback

- 如需撤销本次前端展示，回退对应业务提交即可；现有积分、贡献、数据库权限和业务事实不受影响。
