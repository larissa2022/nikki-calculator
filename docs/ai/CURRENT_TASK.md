# 当前任务看板

> 最后更新：2026-07-15 18:55（北京时间）

## 业务目标

- 在不破坏现有“提交 → 审核入库 → 提交者衣柜写回”主流程的前提下，让有效贡献者与积分奖励可追溯、可重算。

## 技术目标

- 先完成并验证现有提交、审核权限的安全收口；用户确认主流程正常前，不启动贡献与积分数据结构。

## 当前阶段

- DB-0 安全修复已应用到 development，独立 database draft PR #76 已创建；未进入 production。
- 自动角色矩阵和 Security Advisor 复查已通过 DB-0 目标门禁，现有 2 条申请及管理员角色数据未改变。
- 2026-07-15 18:52（北京时间）启动业务验证后在账号与环境前检停止：development 仅有 1 个管理员和 1 个最高站长，缺少普通用户；保留的页面账号信息又与 development 账号不一致。
- 未执行测试服装提交、驳回、审核入库或状态变更；通过前不进入 DB-1。

## 最近完成

- 普通用户只能提交、查看自己的待审核申请，不能代提交、伪造审核状态、删除或调用内部高权限能力。
- 管理员和最高站长可查看全部申请、更新审核状态并正常进入审核入库业务校验。
- Security Advisor 从 35 项降至 16 项，DB-0 目标告警已清除；schema、类型和前端构建均已验证。
- 业务验证前检确认 development 角色数为普通用户 0、管理员 1、最高站长 1；测试服装名和短编号未占用，现有申请仍为待审核 1 条、已通过 1 条。

## 下一步任务

1. 先确认用于验证的页面实际连接 development project ref `tfwejruvdahonacyldrg`，并准备普通用户、管理员、最高站长三个独立账号；不要发送密码。
2. 普通用户在 development 提交一条可识别的测试服装，确认能看到自己的申请且不能看到他人的申请。
3. 先确认“驳回”是指服装申请还是套装申请：当前页面仅有套装申请的驳回按钮，服装申请的驳回函数尚未接入页面；口径确认后再执行驳回、重新提交、审核入库和衣柜同步验证。
4. 最高站长确认能查看全部申请并完成一次状态操作；按“角色、步骤、结果、页面提示、测试服装、北京时间”保留每一步记录。
5. 全部通过后再决定 PR #76 是否可合并；任一步失败都停止后续操作并反馈，不进入 DB-1。

## 阻塞与待确认

- DB-0 业务验证阻塞于缺少普通用户账号，以及当前保留页面无法证明连接 development；需先由用户在正确 development 页面准备或登录三个角色账号。
- 验证说明要求管理员驳回后重新提交，但当前前端仅为套装申请提供驳回入口；服装申请的驳回口径待用户确认。
- Performance Advisor 专用接口持续传输失败，已完成 DB-0 相关 catalog 等价检查并保留限制说明。
- PR #75、PR #76 merge 均未授权。

## 通用边界

- 不操作 `main`、production、Supabase 配置、Vercel 或 env；用户验证反馈前不继续 database 写入、不启动 DB-1，开发 PR 不自动 merge。

## Rollback

- DB-0 未修改业务数据；验证失败时停止后续操作，以新的 development rollback migration 恢复旧权限，不改写已应用历史，不进入 production。文档更新可独立 revert。
