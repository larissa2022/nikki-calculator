# 当前任务看板

> 最后更新：2026-07-21 17:52（北京时间）

## 当前任务

- 完成 DB-4 管理员仲裁入库：将有效贡献者、每人 10 分、衣柜写回和 pending 通过纳入同一数据库事务。

## 为什么做

- 旧仲裁 RPC 只完成正式服装、衣柜和 pending 写入，既没有形成贡献 / 积分事实，也没有在服务端证明所选 pending 与最终入库数据一致，存在错误奖励和重试重复写入风险。

## 已经完成

- development `tfwejruvdahonacyldrg` 已应用 DB-4 migration；production 未操作。
- 已按最早来源稳定去重并限制前 5 位真实用户，同一批用户同时获得贡献、每人 `+10` 和衣柜写回；匿名、重复用户及第 6 位之后不获奖励。
- 服务端候选一致性、普通用户拒绝、admin / super_admin、重复请求幂等、DB-2 积分汇总和故障整体回滚已通过 development 事务测试，测试夹具全部回滚。
- 旧 DB-3 验收形成的 1 条贡献和 1 条积分流水保持不变；未删除或改写既有业务事实。

## 当前进度

- 验证阶段：自动化与远端目录复核已通过，待负责人通过本 PR Preview 完成人工业务验收。

## 下一步

- 负责人在本 PR Preview 使用 development 专用管理员账号完成一次“管理员仲裁”业务验收，并按验收说明回传结果。

## 需要负责人决定

- 仅需反馈 DB-4 人工验收是否通过；未通过时提供页面提示或截图，不需要授权 merge。

## 通用边界

- 本任务只修改 DB-4 仲裁 RPC、数据库测试、生成快照和直接支持文档；不进入 DB-5、历史回填、前端业务改造、`main` 或 production。
- migration 已在 development 应用；如需回退必须新增 patch migration，不改写历史，不删除贡献或积分事实。
- PR merge、production、分支删除和任何额外数据库写入仍需分别确认。

## Rollback

- 先停用仲裁入口，再以新 patch migration 恢复 DB-4 前的 RPC 定义与 execute grant；若已形成错误积分，只追加等额反向流水并保留贡献审计链。
