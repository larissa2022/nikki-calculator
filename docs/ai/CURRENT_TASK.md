# 当前任务

当前任务：docs-only 文档瘦身 Phase 1。

## 边界

- 删除低使用 AI 模板文件。
- 只改允许范围内的既有 docs 文件。
- 不修改 `docs/ai/RULES.md`。
- 不删除 `docs/ai/REJECTED_IDEAS.md`。
- 不处理 `docs/ai/CODEX_REPORT.md`、`docs/ai/CONVERSATION_HANDOFF.md`、`docs/ai/TEST_CHECKLIST.md`。
- 不碰业务代码、数据库、Supabase、Vercel、`main` 或 production。
- PR base 为 `develop`。
