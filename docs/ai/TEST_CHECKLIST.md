# 测试清单

用途：记录需要人工或自动验证的测试项，帮助确认改动是否满足预期且未引入回归。

## 待负责人统一验收总览

- 目标环境：只使用 development / Preview，Supabase project ref 为 `tfwejruvdahonacyldrg`；不得在 production 重放以下数据。
- 已完成基线：下方第 1～8 项已经人工通过，不需要重复执行；后续如发现回归，再按原步骤复查。
- 当前待验收：第 9 项 DB-7 陪审团全字段审核与积分、第 10 项 DB-8 正式图鉴报错受理，以及第 11 项 DB-9 管理员处理、转陪审与报错奖励。
- 推荐顺序：先完成 DB-7 的 9.1～9.5，再完成 DB-8，最后执行 DB-9；不要跳过场景顺序，以免提前关闭后续仍需使用的审核事项。
- 测试账号：development 已保留以下 8 个专用账号；密码沿用此前准备测试账号时的统一 development 测试密码，本仓库不保存密码。若密码不可用，停止验收并反馈“需要重置 DB7 测试账号密码”，不要注册临时替代账号。

| 用途 | 邮箱 | 角色 |
| --- | --- | --- |
| 原始来源 | `db7-jury-source@example.com` | 普通用户 |
| 补充资料 | `db7-jury-candidate@example.com` | 普通用户 |
| 陪审员 1 | `db7-jury-voter01@example.com` | 普通用户 |
| 陪审员 2 | `db7-jury-voter02@example.com` | 普通用户 |
| 陪审员 3 | `db7-jury-voter03@example.com` | 普通用户 |
| 陪审员 4 | `db7-jury-voter04@example.com` | 普通用户 |
| 陪审员 5 | `db7-jury-voter05@example.com` | 普通用户 |
| 独立终审 | `db7-jury-admin@example.com` | 超级管理员 |

- 统一异常反馈格式：

```text
环境：development / Preview
验收项：9.x、10.x 或 11.x
账号邮箱与角色：
服装 / 审核事项：
操作前票数与积分：
实际结果与页面提示：
刷新或离开重进后是否恢复：是 / 否
发生时间（北京时间，精确到分钟）：
是否已停止后续操作：是 / 否
```

- 测试数据处置：现有 DB7 场景和后续 DB8 报错继续保留为 development 验收事实。页面不提供清理入口；清理、账号密码重置或重建场景均属于新的 development 写入，必须另行精确授权。

## Preview / Production 人工回归测试清单

### 1. 衣柜保存与刷新后数量稳定

- 测试目标：确认用户录入大量衣服后，刷新页面不会从几千件异常缩水到一百多件。
- 操作步骤：
  1. 使用已登录账号进入“我的衣柜”。
  2. 记录当前总收集数量。
  3. 连续新增或取消若干衣服，触发云端同步。
  4. 等待同步完成提示消失。
  5. 刷新页面并重新进入“我的衣柜”。
  6. 对比刷新前后的总收集数量和最近操作的衣服状态。
- 预期结果：
  - 刷新后总收集数量稳定。
  - 最近新增或取消的衣服状态正确保留。
  - 页面不长时间卡在“正在同步到云端”。
- 是否通过：通过

### 2. 按分类 + 短编号录入衣柜

- 测试目标：确认用户可通过选择分类并输入短编号批量录入已有图鉴衣服。
- 操作步骤：
  1. 进入“录入衣柜”。
  2. 选择一个分类，例如“发型”或“连衣裙”。
  3. 输入多个短编号，例如 `0001, 0002, 0003`。
  4. 提交录入。
  5. 查看录入结果报告。
  6. 返回“我的衣柜”确认对应衣服已加入。
- 预期结果：
  - 正式库存在的编号进入“本次成功录入”。
  - 未匹配到图鉴编号的项目进入缺失提示。
  - 成功录入的衣服刷新后仍保留在衣柜中。
- 是否通过：通过

### 3. 管理员审核卡片文案

- 测试目标：确认管理员审核卡片不再出现技术感文案，并按确认口径展示状态。
- 操作步骤：
  1. 使用管理员账号进入“图鉴管理”。
  2. 查看“自动入库候选”中的不同卡片。
  3. 分别检查全部一致、多数一致但有不同意见、未达 5 人、人数不足的展示。
  4. 打开卡片详情或入库表单，查看提示文案。
- 预期结果：
  - 页面不出现“版本 / 变体 / 字段组合 / 冲突版本 / 一致提交”。
  - 使用“资料一致情况 / 最多人认可 / 不同意见 / 可按多数入库 / 需要继续收集”等文案。
  - 全部一致显示“可入库”。
  - 多数一致但有不同意见显示“可按多数入库”。
  - 有不同意见但未达 5 人显示“需要继续收集”。
  - 人数不足显示“人数不足”。
- 是否通过：通过

### 4. 正式库已有但资料不全的补全路径

- 测试目标：确认正式库已有同名同分类衣服但资料不全时，管理员可从 pending 补全缺失资料。
- 操作步骤：
  1. 在管理员审核页面找到“正式库已有但资料不全”的待处理项。
  2. 打开补全表单。
  3. 检查名称、分类、短编号、星级、五组属性、套装状态、特殊标签等字段。
  4. 补齐缺失字段并提交。
  5. 返回正式库或前台图鉴页面检查该衣服资料。
- 预期结果：
  - 提交不因唯一约束重复而失败。
  - 正式库已有记录被补齐缺失资料。
  - 已有的非空资料不被意外覆盖。
  - 页面给出清晰的成功或失败提示。
- 是否通过：通过

### 5. Pending 状态变化

- 测试目标：确认审核或补全后，相关 pending 记录状态按预期变化。
- 操作步骤：
  1. 在管理员页面记录待处理项数量。
  2. 对一个可入库或可补全的项目执行处理。
  3. 处理完成后刷新管理员页面。
  4. 查看该项目是否仍出现在原待处理列表。
  5. 如页面提供状态信息，检查状态是否从 pending 变为 approved、re_review 或其他预期状态。
- 预期结果：
  - 已处理项目不再重复出现在原待处理列表。
  - 未入库的不同意见提交不会被误标为已入库。
  - 状态变化和页面展示一致。
- 是否通过：通过

### 6. 提交者衣柜写回

- 测试目标：确认用户提交的缺失衣服入库或补全后，会自动进入提交者衣柜。
- 操作步骤：
  1. 使用普通用户提交一个缺失图鉴项。
  2. 使用管理员账号审核通过或补全该项目。
  3. 回到普通用户账号。
  4. 进入“我的衣柜”搜索该衣服。
  5. 刷新页面后再次检查。
- 预期结果：
  - 审核通过或补全后，该衣服出现在提交者衣柜中。
  - 刷新后仍保留。
  - 不会误加入未参与提交的用户衣柜。
- 是否通过：通过

### 7. 套装选择与刷新

- 测试目标：确认所属套装加载稳定，选择套装或无关联套装后不会卡死，刷新后展示一致。
- 操作步骤：
  1. 进入缺失图鉴补录或管理员补全表单。
  2. 点击“所属套装”输入框。
  3. 搜索已有套装并选择。
  4. 再测试选择“无关联套装（纯散件）”。
  5. 提交或关闭后刷新页面。
  6. 重新打开同一表单或相关列表检查展示。
- 预期结果：
  - 套装列表可以正常出现。
  - 选择已有套装后展示套装名称。
  - 选择无关联套装时在输入框内清晰展示，不出现框外重复提示。
  - 页面不假死、不长时间空白。
- 是否通过：通过

### 8. 生产环境不显示 Vue DevTools

- 测试目标：确认 production 构建不会加载 Vue DevTools。
- 操作步骤：
  1. 打开 production 站点。
  2. 打开浏览器开发者工具。
  3. 查看控制台和扩展面板。
  4. 刷新页面后再次确认。
- 预期结果：
  - 生产环境不出现 Vue DevTools 注入提示。
  - 不加载开发环境专用调试工具。
  - 页面正常渲染，无构建相关报错。
- 是否通过：通过

### 9. DB-7 陪审团全字段审核与积分

- 验收状态：待负责人按以下顺序验证。
- 测试范围：页面重进稳定性、自然语言提示、全字段补充、一人一票、退回重新补充、独立终审，以及 +1 / +8 积分；自动 SQL fixture 已通过的并发、权限和事务回滚不要求人工重复。
- 当前场景基线：
  - `【DB7验收】通过路径`：已 5 票赞同并通过，用于核对历史 +1 / +8 和正式资料结果。
  - `【DB7验收】退回重审路径`：上一轮 0 赞同 / 3 反对，当前等待重新补充。
  - `【DB7验收】继续投票与终审路径`：当前 1 赞同 / 2 反对；超级管理员已经以普通陪审员身份投过反对票。
  - `【DB7验收】提交候选路径`：已有完整补充内容，当前 0 票。
  - `【DB7验收】多字段资料甲`：等待补充，问题字段为服装名称、星级、第一组属性和所属套装。

#### 9.1 全字段问题提示与完整补充

1. 使用 `db7-jury-candidate@example.com` 登录并进入“陪审团”。
2. 找到 `【DB7验收】多字段资料甲`。
3. 确认页面明确分组显示“存在分歧：服装名称、星级、第一组属性、所属套装”，且不出现“候选快照、冻结快照”等内部术语。
4. 确认未列为问题的字段只读，列为问题的字段可修改；页面提示为“请补充或核对上述字段，并提交一份完整资料供其他用户审核”。
5. 提交一份完整资料，确认事项进入“审核中”；刷新并离开页面后重新进入，内容仍可读取且页面不卡死。

预期：一个审核单覆盖全部冲突字段，不只显示套装；提交不修改正式图鉴，也不立即奖励 +8。

#### 9.2 首次投票、即时 +1 与页面重进

1. 记录 `db7-jury-voter01@example.com` 当前积分。
2. 使用该账号打开 `【DB7验收】提交候选路径`，点击赞同并在页面内确认框确认“投票后不可修改”。
3. 确认票数立即变为赞同 1，结果区只显示“你已投：赞同”，不显示“一票定稿”。
4. 确认积分即时增加 1；刷新页面、返回首页再进入陪审团，票数、已投结果和积分保持一致，页面不空白、不假死。
5. 重复点击或重试同一赞同票，确认不新增票、不重复加分；尝试改投反对时应被拒绝。

#### 9.3 达到通过门槛与 +8 结算

1. 在 9.2 完成后，依次使用 `db7-jury-candidate@example.com`、`db7-jury-voter03@example.com`、`db7-jury-voter04@example.com`、`db7-jury-voter05@example.com` 对 `【DB7验收】提交候选路径` 投赞同票。
2. 每个首次有效投票账号都应即时 +1；第 5 票后事项通过并从开放队列消失。
3. 使用候选提交者 `db7-jury-voter02@example.com` 查看积分，确认通过后增加 +8；正式服装只更新该审核内容允许修改的字段。
4. 再查看 `【DB7验收】通过路径` 的相关正式服装和账号积分，确认历史通过结果仍保持，不被本轮重复结算。

#### 9.4 退回后重新补充

1. 使用 `db7-jury-candidate@example.com` 打开 `【DB7验收】退回重审路径`。
2. 确认状态为“需要重新补充”，上一轮反对票不会继续显示为当前轮票数。
3. 重新提交完整资料，确认建立新一轮“审核中”；旧候选和旧票不能覆盖新一轮内容。
4. 暂停继续投票，保留该新候选供 9.5 的独立终审验证。

#### 9.5 普通投票与管理员终审互斥

1. 使用 `db7-jury-admin@example.com` 打开 `【DB7验收】继续投票与终审路径`。
2. 因该管理员已投过普通反对票，页面应说明不能同时参与普通投票和终审，且不显示可执行终审按钮。
3. 打开 9.4 新提交的 `【DB7验收】退回重审路径`；该管理员未参与该轮普通投票，应可填写明确理由并执行独立终审驳回。
4. 确认终审不新增 `jury_votes`、不奖励 +1，事项永久关闭；候选提交者和原来源用户均不能终审自己的数据。

- DB-7 通过标准：9.1～9.5 全部符合；任一步发生卡死、票数 / 积分重复、字段越界修改或自审时立即停止后续操作并按总览格式反馈。

### 10. DB-8 正式图鉴报错受理

- 验收状态：主 migration 与最小权限 patch 已在 development 应用并通过回滚 fixture，待负责人验证页面业务行为。
- 测试范围：只验证用户提交报错、本人记录、刷新 / 重进和幂等提示；不验证管理员处理、转陪审、正式库修正或报错积分。
- 目标环境：仅 development / Preview，项目 ref `tfwejruvdahonacyldrg`；不得进入 production。
- 前置条件：
  1. 使用一个 development 普通用户账号登录。
  2. 任选一件已存在的正式服装，记录其名称、短编号、星级和当前积分。
  3. 建议内容使用明显的测试标记，例如“DB8验收：建议改为四星”，不要填写真实用户隐私。
- 操作步骤：
  1. 进入“图鉴报错”，用服装名称或短编号搜索并选择记录。
  2. 依次切换“服装名称、短编号、分类部位、星级、属性分值、所属套装、特殊标签、其他资料”，确认页面能明确显示当前记录。
  3. 选择一个问题字段，填写 1–500 字建议和 10–1000 字判断依据后提交。
  4. 确认成功提示明确说明提交不会直接修改正式图鉴，并在“我的报错记录”看到同一服装、字段、建议、依据和“等待处理”。
  5. 刷新页面并离开后重新进入“图鉴报错”，确认记录仍可读取，页面不空白、不假死。
  6. 对同一服装、同一字段再次提交完全相同的建议和依据，确认提示已有记录且不新增第二条。
  7. 对同一服装、同一字段改写建议或依据后再次提交，确认系统拒绝覆盖待处理报错。
  8. 返回正式图鉴和用户中心，确认服装原资料与积分均未变化；如可使用第二个 development 账号，确认其“我的报错记录”看不到第一账号的记录。
- 预期结果：
  - 仅登录用户可以提交；匿名用户没有入口，数据库也拒绝调用。
  - 同一活动报错相同重试幂等、不同内容不可覆盖。
  - 刷新、离开重进和请求回读不会丢失已有记录或锁死页面。
  - 受理动作不修改正式服装、不创建陪审事项、不发放积分。
- 异常反馈格式：

```text
环境：development / Preview
账号角色：普通用户
步骤：10.x
服装名称与短编号：
问题字段：
结果：通过 / 失败
页面实际提示：
发生时间（北京时间，精确到分钟）：
刷新或重进后是否恢复：是 / 否
是否已停止后续操作：是 / 否
```

- 测试数据处置：报错记录作为 development 验收事实保留，便于后续管理员处理功能复用；不复制到 production。
- 清理 / rollback：当前页面不提供删除报错；若必须清理，停止验收并单独申请精确 development SQL 授权，不直接改表、不删除 migration history。已应用 schema 如需撤销，只能使用新的前向 migration。

### 11. DB-9 管理员处理、转陪审与报错奖励

- 验收状态：DB-9 migration 已在 development 生效；DB6～DB9 回滚 fixture、Advisor、权限、类型和 schema 回读均通过，四组验收数据已就绪。当前只等待负责人按本节进行业务验收。
- 测试范围：管理员不可自审、空字段直接补全、非空争议转陪审、不采纳、请求结果回读，以及报错最终被采用时唯一 +5。
- 目标环境：仅 development / Preview，项目 ref `tfwejruvdahonacyldrg`；不得进入 production。
- 前置条件：
  1. development 已存在四件服装及对应四条待处理报错：`【DB9验收】空字段直接补全`（992001）、`【DB9验收】非空争议转陪审`（992002）、`【DB9验收】不采纳`（992003）、`【DB9验收】管理员自审拦截`（992004）。如缺少任一场景，停止并反馈“DB9 验收数据缺失”，不要自行补造，也不要用 production 或真实用户数据替代。
  2. 前两条由 `db7-jury-source@example.com` 提交，第三条由 `db7-jury-voter01@example.com` 提交，第四条由 `db7-jury-admin@example.com` 本人提交；处理账号使用 `db7-jury-admin@example.com`。
  3. 验收前数据库基线已回读：四条报错均为待处理，报错奖励流水为 0、关联重审项为 0；`db7-jury-admin@example.com` 为 1 分、`db7-jury-source@example.com` 为 0 分、`db7-jury-voter01@example.com` 为 2 分。开始前如页面显示不符，停止并反馈，不继续操作。

#### 11.1 管理队列与自审拦截

1. 使用普通用户进入前台，确认看不到管理端报错队列。
2. 使用 `db7-jury-admin@example.com` 进入“图鉴管理 → 图鉴报错”。
3. 确认每张卡片显示服装、问题字段、正式图鉴当前内容、用户建议、判断依据和自然语言分流：“可直接补全 / 需要陪审 / 仅可不采纳”。
4. 打开由该管理员本人提交的报错，确认卡片明确显示“不能处理本人报错”，不显示核对输入框、处理说明或结案按钮；数据库回滚 fixture 已另行验证绕过页面调用仍会被拒绝。
5. 刷新并离开管理页后重新进入，队列仍可读取，页面不空白、不假死。

#### 11.2 空字段直接补全与即时 +5

1. 打开“正式字段缺失”的报错，填写结构化核对结果和 10～1000 字处理说明。
2. 确认页面只显示“补全正式图鉴”，不显示“转交陪审团”；在页面内确认框确认执行。
3. 确认报错从队列消失、正式图鉴只补上目标字段，其他字段不变化；报错用户积分立即增加 5。
4. 刷新页面并重复回读，确认不新增第二条积分流水、不重复修改数据。

#### 11.3 非空争议转交全字段陪审

1. 打开“正式字段已有内容”的报错，填写核对后的结构化结果和处理说明。
2. 确认页面只允许“转交陪审团”，不能直接覆盖正式图鉴。
3. 在页面内确认后，确认报错显示为“已转交复核”，正式图鉴和报错用户积分暂时不变化。
4. 使用未参与原始来源的普通用户进入“陪审团”，确认同一服装只出现一个审核单，问题字段包含该报错字段；再次转交或请求重试不创建重复事项。
5. 提交完整内容并达到通过门槛，确认正式图鉴采用核对结果后，报错记录变为“已确认”，报错用户唯一获得 +5；若陪审最终未采用该结果，则报错变为“未采纳”且不奖励。

#### 11.4 不采纳不改数据、不发积分

1. 打开“不应采纳”的报错，填写明确处理说明并选择“不采纳”。
2. 在页面内确认框确认后，确认报错从待处理队列消失并在提交者本人记录中显示“未采纳”。
3. 核对正式图鉴、陪审队列和报错用户积分，确认三者均无新增业务结果。

- DB-9 通过标准：11.1～11.4 全部符合；任何自审成功、已有字段被直接覆盖、重复陪审事项、错误 +5 或操作后页面锁死都必须立即停止，并按总览格式反馈。
- 测试数据处置：报错、处理结果和陪审事项作为 development 验收事实保留；需要重建或清理时另行精确授权，不能直接改表或删除 migration history。
